name: Container Build

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    name: Build and Push Container
    runs-on: titan
    steps:
      - name: Debug info
        run: |
          echo "Starting build..."
          docker --version
