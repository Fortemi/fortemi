name: Container Build

on:
  push:
    branches: [main]

jobs:
  test-workflow:
    name: Test Workflow
    runs-on: titan
    steps:
      - name: Echo test
        run: echo "Hello from container workflow"
