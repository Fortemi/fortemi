# v2026.7.16 Release Announcement

Fortemi v2026.7.16 is the corrective publication for v2026.7.15. It preserves
the same Knowledge Shard contracts, exact `2.0.0/full-v1` evidence, supported
platform cells, and receipt-bound consumer revisions.

## Fixed

- Mutsu host-key acquisition now permits three bounded 15-second attempts.
  The release still verifies the exact pinned Ed25519 fingerprint before
  sending credentials or starting a build.
- Mutsu jobs remain pinned to the dedicated `titan` coordinator, with SSH and
  downloaded artifacts isolated under each job's temporary directory.
- Linux arm64 Colima state, cache, and temporary files remain on mutsu's
  `/Volumes/build` volume.

## Compatibility Boundary

The evidence remains limited to the Fortemi authority-to-React/core-to-HotM
contract surface on Linux x86_64, Linux arm64, and native macOS arm64 in run
6393. Windows remains deferred to issue #1096, and issue #1081 remains
`NO-GO` pending independent audit.
