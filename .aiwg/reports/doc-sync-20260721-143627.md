# Documentation Sync Report - 2026-07-21

## Scope

- Direction: `code-to-docs`
- Baseline: `v2026.7.1`
- Audited code: Knowledge Shard version registry, migration chain, profile
  selector, `full-v1` server route, conformance matrix, and release signing
- Audited docs: public shard migration guide, ADR-102, canonical contract
  README, conformance README, changelog, and release-facing documentation

## Findings And Fixes

1. The public shard migration guide still identified schema `1.1.0` as current
   and described `full-v1` as reserved and unsupported. The implementation is
   at schema `1.2.0`, with registered `1.0.0 -> 1.1.0 -> 1.2.0` migrations and
   a complete server `full-v1` route backed by signed self-roundtrip evidence.
   The guide now matches that behavior while explicitly retaining the default
   `core-v1` boundary and the pending external `full-v1` consumer evidence.

## Files Changed

- `docs/content/shard-migration.md`
- `.aiwg/reports/doc-sync-20260721-143627.md`
- `.aiwg/reports/doc-sync-last-run.json`

## Validation

- `git diff --check`
- `DOCS_CONTRACT_MODE=blocking npm run docs:contract -- --profile=hosted_strict`
- `npm run docs:build`
- `python3 scripts/ci/verify-knowledge-shard-matrix.py --verify-remotes --require-complete`

## Remaining Review Items

None. Cross-repository `full-v1` evidence remains explicitly pending in the
canonical ADR and is not advertised as completed by this release.
