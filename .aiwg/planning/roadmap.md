# Fortemi Delivery Roadmap

> **Status:** Active — reference until all planned phases are complete.
> **Created:** 2026-06-24 · **Last updated:** 2026-06-27
> **Tracker:** Gitea `Fortemi/fortemi` (authoritative). Issue numbers below are Gitea issues.
> **Source:** Synthesized from the 2026-06-21→23 open-issue audit sweep (#746–#1006), the milestone structure, and the 2026-06-23 interactive product-decision Q&A (31 decisions recorded on-issue).

## How to use this document

- This is the **living plan of record**. Update the checkboxes and status as work lands.
- Future agents should treat this as the **main operating project plan** until all phases are complete or the operator explicitly replaces it. For "what next" / "continue the plan" / "advance roadmap" requests, run `aiwg discover "advance roadmap"` and use `fortemi-roadmap-skill`.
- Phases are sequenced by **dependency**, not dates. (Velocity here is human+AI non-scalar; do not add time estimates — express scope as issues/sequence.)
- Each phase lists its **gating dependency** and the **issues** in it. Issue lists are anchors, not exhaustive — the milestone is the full set.
- The **strategic pivot is #853**: the open BSL desktop build ships *without* the advanced-auth/multi-tenant stack, so **Phase 1 (open-build GA) is NOT blocked by Phase 2 (hosted/licensed)** — they parallelize.

### Status legend
`[ ]` not started · `[~]` in progress · `[x]` complete · `[!]` blocked (note blocker)

### Tier labels (Gitea)
- `tier/licensed-server` (#574) — advanced auth + multi-tenant hosting; omitted from the open build (per #853).
- `tier/open-build` (#575) — single-user desktop; basic MCP; local/API-key auth.

### Infrastructure prerequisites (`roctinam/devops`)

Phases 2–3 (licensed-server / bridge) require the EE org, repos, and private Cargo registry per **ADR-095** (CE/EE distribution) and **ADR-096** (private registry). Repo-creation requests filed 2026-06-24 in `roctinam/devops`:
- **devops#33** — stand up `Fortemi-Enterprise` private Gitea org (gates all EE repos).
- **devops#34** — stand up private Cargo registry (ADR-096; also migrates `fortemi-auth` off the SSH-git dep).
- **devops#35–#43** — 9 EE private repos: auth-providers, rbac, audit-sinks, billing, search-backends, job-backends, kms, mcp-gate, distribution.
- **devops#44** — `Fortemi/aiwg-fortemi-skills` public/MIT (CE Tier-1; was "proposed" in ADR-095).

Near-term EE repos gating **Phase 2**: kms (devops#41 → #897), mcp-gate (devops#42), rbac (devops#36 → #956–963), audit-sinks (devops#37 → #910), billing (devops#38 → #877+), distribution (devops#43). Existing CE repos: `Fortemi/fortemi`, `HotM`, `fortemi-react`, `fortemi-auth` (private), `licensing` (private).

---

## Phase 0 — Foundation contracts (cross-cutting; unblock everything)

**Gating:** none — start here. Most other phases consume these.

- [!] **#710** AuthorizationPolicy route/action inventory — single source of truth for the auth-exempt set, object policy, docs exposure (#1000), and admin-gating. (2026-06-25: executable route policy inventory added with registered-route coverage tests; auth public/admin helpers now delegate to the inventory for the current credential-management gate. Core `AuthorizationPolicy` / `Action` / `Resource` / `Decision` / `Obligation` contract added in `matric-core` with `AllowAllPolicy`, `RoleBasedPolicy`, tenant matching, and MCP-vs-REST scope-family tests. API route inventory now builds concrete policy inputs and attaches them to protected request extensions. API stack invokes `AuthorizationPolicy` from those request extensions with `AllowAllPolicy` preserving current behavior and deny/indeterminate/error decisions failing closed as 403. Multi-tenant startup now selects `RoleBasedPolicy`; personal mode keeps `AllowAllPolicy`; representative REST write, credential-management admin, backup-restore admin, and realtime transport allow/deny coverage exercises the real policy bridge. Realtime transport policy inputs require MCP transport scope. The in-process `RoleBasedPolicy` evaluation target is documented and covered by a regression budget test. Auth exemption consumers now keep system-health and realtime provider callbacks public while leaving inline-proof webhooks method-aware. Incoming webhook receiver policy inputs now mirror that method boundary: public signed `POST` stays inline-proof while GET/PATCH/DELETE management on the same slug route carries admin `webhook_control` scope metadata. Route-param resource IDs now carry explicit unnormalized-candidate metadata so future object-policy and audit consumers do not mistake path parameters for backing-store verified resource bindings. Authorization allow/deny/error decisions now emit best-effort `fortemi.audit` records with policy id/version, sanitized subject/resource/action metadata, and deny reason codes. Hosted `RoleBasedPolicy` now fails closed for route-param resources marked as requiring backing-store normalization, while personal `AllowAllPolicy` preserves current local behavior. Note route UUIDs now run a backing-store existence lookup before hosted policy evaluation and only clear the unnormalized-resource guard when the note exists; missing/invalid/lookup-error paths remain fail-closed. Attachment route UUIDs now resolve through file storage and carry parent note metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error paths remain fail-closed and note-scoped attachment routes do not misread note IDs as attachment IDs. Archive/memory route names now resolve through the archive registry and carry archive id/schema/default metadata before clearing the hosted unnormalized-resource guard; missing/lookup-error paths remain fail-closed and collection routes skip name lookup. Collection route UUIDs now resolve through the collection repository and carry id/name/parent/note-count metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/root-route paths remain fail-closed or lookup-free, and note move routes do not misread note IDs as collection IDs. Template route UUIDs now resolve through the template repository and carry safe id/name/format/default-tag-count/collection metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/collection/unrelated document-type routes remain fail-closed or lookup-free. Document-type route names now resolve through the document-type catalog and carry id/category/system/active/attachment metadata before clearing the hosted unnormalized-resource guard; missing/lookup-error paths remain fail-closed and collection routes skip name lookup. Job route UUIDs now resolve through the job queue and carry job type/status/parent-note/cost-tier metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error paths remain fail-closed and collection routes skip job lookup. Outbound webhook-control route UUIDs now resolve through the webhook repository and carry safe webhook state/retry metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error paths remain fail-closed, collection routes skip lookup, and incoming receiver slugs are not treated as control IDs. Inbound-source route names now resolve through the inbound source registry and carry safe source id/name/kind/enabled metadata before clearing the hosted unnormalized-resource guard; missing/lookup-error paths remain fail-closed and collection routes skip name lookup. PKE keyset `name_or_id` routes now resolve through the keyset repository, carry safe id/name/address/label metadata while keeping key material out of policy metadata, and keep missing/lookup-error/collection/unrelated inbound routes fail-closed. Credential-management API-key route UUIDs now resolve through the OAuth repository and carry safe id/prefix/name/scope/activity/expiration/rate-limit metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/collection/unrelated PKE routes remain fail-closed or lookup-free. Taxonomy concept route UUIDs now resolve through the SKOS repository and carry safe concept id/primary-scheme/status/count metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/collection/scheme/secondary-target routes remain fail-closed or lookup-free until their own bindings are explicit. Taxonomy scheme route UUIDs now resolve through the SKOS repository and carry safe scheme id/notation/version/activity/system/date-presence metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/collection/concept routes remain fail-closed or lookup-free. Taxonomy collection route UUIDs now resolve through the SKOS repository and carry safe collection id/scheme/ordered/date-presence metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error/root/concept/scheme routes remain fail-closed or lookup-free. Taxonomy relation delete routes now verify both source and target SKOS concepts and carry safe endpoint ids/schemes/relation-type metadata before clearing the hosted unnormalized-resource guard; missing/invalid/lookup-error and relation collection routes remain fail-closed or lookup-free. The transitional credential-management admin helper has been retired; credential/API-key admin decisions now flow through the AuthorizationPolicy path and preserve sanitized problem responses. Archive routing now runs before auth/authorization, selected archive context is carried on policy inputs, and archive-scoped collection/template normalization uses the selected schema before hosted policy evaluation. MCP scope cleanup removed the legacy `AuthPrincipal::has_scope` behavior where bare `mcp` implied REST read/write and aligned docs/scope seed text with the transport-vs-resource scope split.)
  - 2026-06-26 blocker: remaining per-tool MCP authorization wiring is waiting on #718/#893 tool/action metadata and ADR alignment; continue other Phase 0 contracts meanwhile.
- [x] **#967** RFC 9457 error contract (`application/problem+json`) — *decided: clean pre-GA break.* Centralized `ProblemType` registry at `ApiError::into_response()`; internal-cause vs public-detail split; `request_id` extension; one `assert_problem()` test helper. Evaluate `problem_details` crate (axum 0.8). (2026-06-25: first boundary slice added in-house RFC 9457 `ProblemType` / `ProblemDetails` mapping for `ApiError`, returns `application/problem+json`, redacts raw `Database`/`Internal` details and blob storage paths from client bodies, and covers representative regressions. Request-id slice adds middleware that copies `x-request-id` into RFC 9457 bodies and exposes the header to browser clients. OAuth server-error slice maps `OAuthApiError` database/internal server failures to generic problem responses without raw OAuth/database details. Auth/rate-limit middleware slice maps 401/403/429 middleware failures to problem responses and keeps scope/policy reasons out of client bodies. Response-class security-header slice adds central headers for API/problem responses, browser docs/OAuth surfaces, public probes, and download/media responses, including no-store on errors and tests that downloads avoid document CSP. Backup `pg_dump` failure slice adds a stable `operation-failed` problem type and keeps raw command stderr/DSNs/paths out of backup error responses. PKE direct-error slice routes crypto/keyset validation, database, and operation failures through central `ApiError`/RFC 9457 problem responses and removes legacy `error_description` payloads from PKE handlers. Audio/vision provider slice adds a stable `provider-failure` problem type, maps backend failures to generic 502 details, and replaces multipart/read parser diagnostics with safe validation problem details. Upload multipart slice replaces parser/read diagnostics in attachment uploads with safe validation problem details and adds validation-shape coverage. 2026-06-26: inference-config slice routes the local error helper and provider probe failures through RFC 9457 problem responses, removes legacy `error`/`failures` payloads, and avoids echoing submitted provider URLs or raw probe diagnostics. Chat/stream slice routes chat validation/capacity/provider HTTP errors through problem responses, preserves `Retry-After` as a header, removes raw model-discovery/provider diagnostics, and makes SSE generation failures generic. Job API slice projects list/detail responses through a hosted-safe `JobResponse` that redacts payload/result secrets, paths, URLs, raw text, stderr/stdout, and genericizes `error_message`. Inference-complete slice routes HTTP validation/provider failures through central problem responses and makes `/api/v1/inference/stream` SSE provider failures generic instead of backend-text-bearing. Ingest-stream pre-stream slice adds a central `gone` problem type and routes cursor/token HTTP failures through problem responses instead of legacy `{error, code}` JSON.)
  - 2026-06-26 continuation: stored job generation-failure slice logs raw backend errors server-side while storing a generic AI generation failure message for common AI revision/context/related-concept/metadata paths.
  - 2026-06-26 continuation: stored job status slice replaces raw model-resolution and graph-maintenance step errors with generic diagnostics, with raw detail kept out of stored job result payloads.
  - 2026-06-26 continuation: backup/restore follow-up routes filesystem, compression, psql spawn/task, and partial-restore diagnostic failures through generic operation-failed/client messages instead of raw local or command text.
  - 2026-06-26 continuation: docs/catalog slice adds `docs/content/api-error-contract.md`, links it from API docs, and updates the API style guide to describe RFC 9457 problem responses instead of the legacy `{error}` shape.
  - 2026-06-26 continuation: OpenAPI problem-contract slice adds `ProblemDetails`/catalog schemas and a generated `x-fortemi-error-contract` extension carrying the public problem-type catalog.
  - 2026-06-26 continuation: MCP upstream-error parsing slice teaches the MCP server to summarize Fortemi RFC 9457 problem details while suppressing raw legacy JSON/text upstream bodies in tool errors.
  - 2026-06-26 continuation: TUS resumable-upload slice routes staging-file, upload-record, offset, delete, and finalize internal failures through redacted operation-failed problem responses instead of raw filesystem or database diagnostics.
  - 2026-06-26 continuation: query datetime validation now returns fixed invalid-date and relative-time-unit problem details without echoing submitted date strings.
  - 2026-06-26 continuation: attachment media/download slice routes variant, thumbnail, sprite, streaming open/seek, and non-missing file-read internals through generic operation-failed problems while preserving blob-missing and protocol validation behavior.
  - 2026-06-26 continuation: Twilio recording download slice routes HTTP client, download, status, and body-read failures through generic provider-failure problems without exposing recording URLs, provider hosts, tokens, local paths, or raw network diagnostics.
  - 2026-06-26 continuation: archive-routing middleware slice routes invalid memory headers, missing memories, and lookup failures through RFC 9457 problem responses instead of legacy `{error}` JSON, and stops echoing requested memory names.
  - 2026-06-26 continuation: health diagnostics redaction slice removes provider base URLs from public `/health` capability output and replaces live dependency exception text with stable bounded reason codes.
  - 2026-06-26 continuation: live route-boundary coverage now exercises the actual Axum middleware stack for RFC 9457 problem content type, propagated `request_id`, no legacy error envelope, no raw internal detail, no-store cache, and API security headers.
  - 2026-06-26 continuation: representative problem-status route-boundary coverage now verifies 400/401/403/404/409/429/500/503 responses carry stable problem type URIs, propagated `request_id`, no legacy error fields, no-store cache, API hardening headers, and no raw internal detail.
  - 2026-06-26 continuation: ingest-stream protocol-frame slice now sanitizes per-line ack errors and fatal stream initialization frames, preserving safe validation classes while replacing storage/schema/internal details with generic diagnostics.
  - 2026-06-26 continuation: Twilio realtime WebSocket invalid-envelope protocol frame now uses a fixed helper payload with a stable code and regression coverage proving malformed provider/frame details are not serialized back to clients.
  - 2026-06-26 continuation: health live/readiness dependency diagnostics now use stable `reason_code` fields instead of legacy `error` fields, with regression coverage proving raw DSNs, credentials, and hostnames are not serialized.
  - 2026-06-26 continuation: knowledge-shard export/archive construction failures now route through the generic operation-failed problem boundary, and multipart shard upload parser errors use fixed validation messages instead of raw parser diagnostics.
  - 2026-06-26 continuation: backup and shard import result `errors[]` now use fixed client-safe diagnostics instead of raw note import database or JSON parser details.
  - 2026-06-26 continuation: backup knowledge-archive and metadata handlers now route filesystem/tar/write internals through generic operation-failed problems, while archive multipart/parser validation uses fixed client messages instead of raw IO diagnostics.
  - 2026-06-26 continuation: knowledge-shard import/swap file and tar parser failures now use generic operation-failed or fixed validation problem messages instead of raw filesystem/tar/path diagnostics.
  - 2026-06-26 continuation: backup JSON export serialization and memory archive lookup failures now route through generic operation-failed problem responses instead of raw serde/database lookup diagnostics.
  - 2026-06-26 continuation: incoming webhook body parser failures now use fixed validation problem messages for JSON/form/envelope parsing instead of raw serde/parser diagnostics.
  - 2026-06-26 continuation: shard import, attachment upload, and database backup upload base64 decode failures now use fixed validation problem messages instead of raw decoder offset/detail text.
  - 2026-06-26 continuation: search `strict_filter` JSON parse failures now use fixed validation problem text instead of raw serde parser line/column diagnostics.
  - 2026-06-26 continuation: EXIF attachment job failures now store fixed client-safe diagnostics in job and attachment status surfaces instead of raw parser, transaction, attachment id, path, or storage errors.
  - 2026-06-26 continuation: shared job schema-context failures now store a fixed diagnostic instead of raw schema names or backend/schema resolution errors.
  - 2026-06-26 continuation: related-concept AI parse, metadata extraction, and document-type inference job failures now store fixed client-safe diagnostics instead of raw parser, provider, transaction, or database details.
  - 2026-06-26 continuation: embedding job generation and storage failures now store a fixed client-safe diagnostic instead of raw provider, transaction, or database details.
  - 2026-06-26 continuation: title-generation provider and title-save failures now store a fixed client-safe diagnostic instead of raw provider, transaction, or database details.
  - 2026-06-26 continuation: context-update link-fetch, note-fetch, and save failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: AI revision note-fetch, media-check, and revision-save failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: AI contextual revision phase-1 fetch and contextual-save failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: embedding note-fetch transaction failures now store the fixed embedding diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: title-generation note-fetch transaction failures now store the fixed title diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: linking note-fetch and embedding-lookup transaction setup failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: purge-note transaction, delete, and commit failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: embedding note-fetch commit failures now store the fixed embedding diagnostic instead of raw transaction commit details.
  - 2026-06-26 continuation: concept-tagging and related-concept transaction/fetch setup failures now store fixed client-safe diagnostics instead of raw transaction or database details.
  - 2026-06-26 continuation: reference-extraction transaction/fetch setup failures now store a fixed client-safe diagnostic instead of raw transaction or database details.
  - 2026-06-26 continuation: re-embedding and embedding-set refresh list/lookup failures now store fixed client-safe diagnostics instead of raw database, set, or missing-embedding details.
  - 2026-06-26 continuation: final `jobs.rs` raw-format cleanup removed stored AI revision empty-output pipeline text and purge note-id echoing; `JobResult::Failed(format!(...))` no longer appears in job handlers.
  - 2026-06-26 continuation: search and federated-search internal setup/query failures now route through generic operation-failed problems instead of raw schema, memory, or database diagnostics.
  - 2026-06-26 continuation: attachment JSON/multipart upload and delete commit failures now route through generic operation-failed problems instead of raw transaction diagnostics.
  - 2026-06-26 continuation: note/tag validation errors now use stable validation problem details without echoing submitted tag values or tag prefixes.
  - 2026-06-26 continuation: attachment JSON/multipart content-type validation now uses a stable validation problem detail without echoing submitted MIME strings.
  - 2026-06-26 continuation: note revision-mode and document-type validation now use stable problem details without echoing submitted mode or document-type strings.
  - 2026-06-26 continuation: incoming webhook schema-validation failures now use a stable validation problem detail instead of serializing raw validator paths or payload values.
  - 2026-06-26 continuation: chat model validation now uses fixed RFC 9457 validation details without echoing requested model slugs.
  - 2026-06-26 continuation: archive duplicate-name validation now uses a fixed validation problem detail without echoing submitted archive names.
  - 2026-06-26 continuation: job type validation now uses a fixed validation problem detail without echoing submitted job type strings.
  - 2026-06-26 continuation: embedding-config lookup validation now uses a fixed validation problem detail without echoing submitted config IDs.
  - 2026-06-26 continuation: attachment document-type validation now uses a fixed validation problem detail without echoing submitted document type IDs.
  - 2026-06-26 continuation: archive and backup-archive not-found responses now use fixed problem details without echoing submitted archive names or filenames.
  - 2026-06-26 continuation: memory not-found responses now use a fixed problem detail without echoing submitted memory names.
  - 2026-06-26 continuation: webhook not-found responses now use fixed problem details without echoing outbound webhook IDs or incoming receiver slugs.
  - 2026-06-26 continuation: call-session not-found responses now use a fixed problem detail without echoing submitted call UUIDs.
  - 2026-06-26 continuation: collection not-found responses now use a fixed problem detail without echoing submitted collection UUIDs.
  - 2026-06-26 continuation: note not-found responses now use a fixed problem detail without echoing submitted note UUIDs.
  - 2026-06-26 continuation: embedding set/config not-found responses now use fixed problem details without echoing submitted slugs or config UUIDs.
  - 2026-06-26 continuation: inbound-source not-found responses now use a fixed problem detail without echoing submitted source names.
  - 2026-06-26 continuation: TUS upload not-found responses now use a fixed problem detail without echoing submitted upload UUIDs.
  - 2026-06-26 continuation: note version/revision not-found responses now use fixed problem details without echoing submitted version numbers.
  - 2026-06-26 continuation: backup restore/archive/metadata not-found responses now use a fixed problem detail without echoing submitted filenames.
  - 2026-06-26 continuation: attachment media variant/sprite not-found responses now use fixed problem details without echoing submitted variant names, sprite indexes, or attachment UUIDs.
  - 2026-06-26 continuation: document-type not-found responses now use a fixed problem detail without echoing submitted type names; `ApiError::NotFound(format!(...))` no longer appears in `matric-api/src`.
  - 2026-06-26 continuation: attachment sprite/TUS upload validation now uses fixed problem details without echoing submitted sprite indexes, upload lengths, offsets, or chunk sizes.
  - 2026-06-26 continuation: incoming webhook idempotency conflicts now use a fixed problem detail without echoing submitted idempotency keys.
  - 2026-06-26 continuation: memory-search coordinate validation now uses fixed problem details without echoing submitted latitude, longitude, or radius values.
  - 2026-06-26 continuation: PKE public-key length validation now uses fixed problem details without echoing decoded byte lengths.
  - 2026-06-26 continuation: archive live-memory-limit validation now uses a fixed problem detail without echoing live archive counts or configured limits.
  - 2026-06-26 continuation: note chunking validation now uses fixed problem details without echoing submitted chunk sizes or bulk note indexes.
  - 2026-06-26 continuation: inference-config database failures now use a fixed problem detail without constructing raw database error response strings.
  - 2026-06-26 continuation: linking job schema/search/commit step failures now store fixed diagnostics instead of raw transaction or database details.
  - 2026-06-26 continuation: ingest-stream NDJSON parser failures now return a fixed validation class without raw serde line/column diagnostics.
  - 2026-06-26 continuation: ingest-stream tag validation now uses fixed length/depth classes without exposing submitted tag text or configured limits.
  - 2026-06-26 continuation: inference provider probe diagnostics now use stable timeout/connect/status/invalid-response/provider classes instead of preserving backend HTTP or parser text.
  - 2026-06-26 continuation: contextual revision empty-output jobs now store the fixed contextual-revision diagnostic instead of pipeline-control detail.
  - 2026-06-26 continuation: ad-hoc audio, vision, and inference completion provider failures now pass fixed provider details instead of backend error text.
  - 2026-06-26 continuation: PKE key generation, encryption, and keyset creation failures now pass fixed operation details instead of crypto/storage error text.
  - 2026-06-26 continuation: model and chat model discovery failures now log fixed diagnostic classes plus bounded lengths instead of raw provider discovery errors.
  - 2026-06-26 continuation: chat generation and streaming startup failures now log fixed diagnostic classes plus bounded lengths instead of raw provider errors or model slugs.
  - 2026-06-26 continuation: inference config override and audit failures now log fixed diagnostic classes plus bounded lengths instead of raw database, validation, or schema details.
  - 2026-06-26 continuation: PKE keyset audit emission failures now log a fixed diagnostic class plus bounded length instead of raw audit sink errors.
  - 2026-06-26 continuation: job ancillary chunk-merge, follow-up queue, contextual-discovery, revision-note, and provenance logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors.
  - 2026-06-26 continuation: embedding/title/linking provenance, title escalation, link creation, and graph-maintenance queue logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors.
  - 2026-06-26 continuation: linking tag-boost, threshold/wiki link creation, purge cleanup, and context-update link-fetch logs now use fixed diagnostic classes plus bounded lengths instead of raw errors or IDs/names.
  - 2026-06-26 continuation: concept-tagging and reference-extraction GLiNER/LLM/tag/provenance logs now use fixed diagnostic classes plus bounded lengths instead of raw provider/parser errors or concept IDs.
  - 2026-06-26 continuation: related-concept, metadata, document-type provenance, and re-embed queue logs now use fixed diagnostic classes plus bounded lengths instead of raw provider/parser errors, concept notations, or note IDs.
  - 2026-06-26 continuation: EXIF attachment status/provenance/metadata update logs now use a fixed diagnostic class plus bounded lengths instead of raw storage/provenance errors or attachment IDs.
  - 2026-06-26 continuation: remaining job-handler escalation, concept-resolution, AI revision, and parser fallback logs now use fixed diagnostic classes plus bounded lengths; `jobs.rs` no longer has raw `error = %e` structured log fields.
  - 2026-06-26 continuation: top-level attachment/NLP pipeline job enqueue failures now log fixed diagnostic classes plus bounded lengths instead of raw queue errors, note IDs, or attachment IDs.
  - 2026-06-26 continuation: operational audit, pause-state, job-progress, and webhook dispatch logs now use fixed diagnostic classes plus bounded lengths instead of raw sink/database/outbox errors or webhook slugs.
  - 2026-06-26 continuation: authorization normalization, policy, and decision-audit logs now use fixed diagnostic classes plus bounded lengths instead of raw lookup/policy/audit sink errors.
  - 2026-06-26 continuation: remaining `main.rs` related-notes, OAuth, attachment missing-blob, and central database logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors, paths, or OAuth descriptions.
  - 2026-06-26 continuation: ingest fatal and chat generation terminal SSE error frames now use RFC 9457-style problem payloads instead of legacy `{error, code}` envelopes.
  - 2026-06-26 continuation: OAuth token/introspection/revocation handler errors now use RFC 9457 problem responses instead of legacy `{error, error_description}` JSON, and public auth docs now show problem details.
  - 2026-06-26 continuation: central ApiError internal, operation, provider, and blob-missing logs now use fixed diagnostic classes plus bounded lengths instead of raw details, paths, storage backends, or attachment IDs.
  - 2026-06-26 continuation: attachment streaming missing-blob and EXIF job outcome telemetry now uses fixed classes, presence flags, and bounded lengths instead of raw attachment IDs, filenames, content types, or storage paths.
  - 2026-06-26 continuation: AI revision deferral, chunking, follow-up queue, budget, and completion logs now use fixed diagnostic classes and presence flags instead of raw note/job IDs.
  - 2026-06-26 continuation: contextual revision fallback, related-note discovery, phase-2 chunking, budget, and completion logs now use fixed diagnostic classes and presence flags instead of raw note IDs.
  - 2026-06-26 continuation: embedding, title-generation, linking, and purge completion logs now use fixed diagnostic classes, presence flags, and bounded title length instead of raw note IDs or generated titles.
  - 2026-06-26 continuation: remaining job-handler linking, context-update, concept/reference extraction, related-concept, metadata, document-type, and EXIF tier/completion telemetry now uses presence flags and fixed diagnostic metadata instead of raw note IDs.
  - 2026-06-26 continuation: Twilio realtime WebSocket/ASR telemetry now uses fixed diagnostic classes, provider-call-id lengths, call-id presence flags, and bounded error/reason lengths instead of raw CallSid, call UUID, backend, or error text.
  - 2026-06-26 continuation: Twilio translated event, media-stream adapter, webhook event, and call-event outbox Debug output now reports payload lengths, event classes, provider-call-id lengths, and JSON classes instead of raw CallSid values, media payloads, phone-like remote parties, recording URLs, metadata JSON, socket-close reasons, or token-shaped values.
  - 2026-06-26 continuation: Core realtime MediaFrame, CallControlEvent, ActiveCallSession, and CallSessionManager Debug output now reports payload lengths, event metadata classes, provider-call-id lengths, and redacted session-map presence instead of raw media bytes, DTMF digits, recording URLs, phone-like remote parties, provider call IDs, metadata JSON, or socket-close reasons.
  - 2026-06-26 continuation: Core realtime ASR session config, transcript event, and mock backend Debug output now reports transcript/metadata/language/speaker/reason lengths, JSON classes, timestamps, confidence, and event counts instead of raw transcripts, metadata JSON, speaker labels, provider URLs, DSNs, phone-like values, emails, or token-shaped values.
  - 2026-06-26 continuation: Realtime mock adapter and builder Debug output now reports provider-call-id length, frame counts, aggregate payload bytes, DTMF counts, drop settings, codec metadata, and seed presence instead of raw provider IDs, media fixture payloads, DTMF digits, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Realtime ASR error outbox payloads now persist stable reason classes and reason lengths instead of raw provider/backend reason text that can contain URLs, tokens, host paths, transcripts, or diagnostics.
  - 2026-06-26 continuation: Caption segment Debug output now reports timing, duration, transcript length, and speaker-label length instead of raw caption/transcript text, speaker labels, emails, or token-shaped values.
  - 2026-06-26 continuation: Core Job Debug output now reports job IDs as presence flags plus payload/result JSON classes and lengths, error/progress message lengths, status, scheduling, retry, timestamp, and tier metadata instead of raw job payloads, results, backend errors, progress text, DSNs, URLs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Core note aggregate Debug output now reports note ids as presence flags plus title/source/format/content/hash/model lengths, metadata JSON classes/lengths, and tag/concept/link counts instead of raw note content, revised content, titles, tags, metadata JSON, AI metadata, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core note revision and summary Debug output now reports concept/revision/summary fields as lengths, counts, classes, timestamps, booleans, or presence flags instead of raw concept labels/notations/sources, revision content, summaries, rationales, note titles/snippets/tags, metadata JSON, model names, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core link and search-result Debug output now reports URLs, kinds, snippets, titles, tags, metadata, warnings, and result collections as lengths/counts/classes instead of raw external URLs, note snippets, titles, tags, warning text, metadata JSON, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core transcript segment Debug output now reports call ids as presence flags plus transcript and speaker-label lengths, timing presence, confidence, sequence, and timestamps instead of raw transcripts, speaker labels, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core attachment Debug output now reports blob storage, filenames, extracted text, extracted metadata, AI descriptions/models, note titles, document type names, and detection methods as lengths/classes/presence flags instead of raw storage paths, hashes, filenames, extracted content, metadata JSON, model names, titles, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core TUS upload Debug output now reports upload ids, filenames, content types, sizes, offsets, staging paths, metadata, and timestamps as lengths/classes/presence flags instead of raw upload filenames, MIME strings, staging filesystem paths, metadata JSON, URLs, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core call-session Debug output now reports provider, provider-call-id, ASR backend, remote party, end reason, archive, metadata, and timing fields as lengths/classes/presence flags instead of raw provider identifiers, phone-like values, URLs, paths, emails, backend names, metadata JSON, or token-shaped values.
  - 2026-06-27 continuation: Core attachment/TUS/call/transcript Debug metadata now uses character-count lengths for filenames, content types, storage paths, extracted/generated text, model names, call providers, parties, reasons, speaker labels, and transcripts while preserving raw content, URL, path, phone, email, provider-id, and token-shaped value redaction.
  - 2026-06-26 continuation: Core entity and fine-tuning Debug output now reports extracted entities, normalized entities, graph vectors, entity types, model names, dataset metadata, source identifiers, generated queries, and error text as lengths/counts/presence flags instead of raw content-derived strings, embeddings, model identifiers, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-27 continuation: Core entity/fine-tuning Debug metadata now uses character-count lengths for entity text, normalized text, entity types, model names, dataset/source metadata, generated queries, query types, and error text while preserving raw content-derived strings, vector values, model identifiers, URLs, paths, emails, phone-like values, and token-shaped value redaction.
  - 2026-06-27 continuation: Core collection/archive/user-metadata Debug metadata now uses character-count lengths for collection/tag/archive names, archive schema names, descriptions, labels, colors, and config keys while preserving raw collection names, archive/schema labels, config values, URLs, paths, emails, phone-like values, and token-shaped value redaction.
  - 2026-06-26 continuation: Core embedding-set and coarse-search Debug output now reports membership filters, agent metadata, tag strategies, document composition, provider config JSON, set profiles, summaries, member changes, and coarse vectors as lengths/counts/classes/presence flags instead of raw tags, queries, provider URLs, API-key fields, model names, dataset names, usage hints, vector values, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-27 continuation: Core embedding Debug metadata now uses character-count lengths for embedding text/model fields, embedding-set criteria tags/queries, and agent metadata while preserving raw text, model, tag, query, provider-config, vector, and token-shaped value redaction.
  - 2026-06-26 continuation: Core document-type Debug output now reports agent prompts, revision chunking, document type metadata, detection rules, chunking/extraction JSON, creation/update requests, summaries, and detection results as lengths/counts/classes/presence flags instead of raw prompts, section names, validation JSON, MIME strings, filename patterns, model/config values, detection methods, paths, URLs, emails, phone-like values, or token-shaped values.
  - 2026-06-27 continuation: Core document-type Debug metadata now uses character-count lengths for document names, descriptions, detection rules, content types, tree-sitter languages, agent prompts/sections, and detection methods while preserving raw prompt, pattern, config, URL, path, email, and token-shaped value redaction.
  - 2026-06-26 continuation: Core collection/archive/user-metadata Debug output now reports collection names/descriptions, tag labels, archive names/schema names/descriptions, user labels/colors, and user config JSON as lengths/classes/presence flags instead of raw hierarchy names, tags, archive schema identifiers, labels, color strings, config keys/values, paths, URLs, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core provenance Debug output now reports source URLs, relations, activity types, model names, metadata JSON, chain edges, and revision-node parents as lengths/classes/counts/presence flags instead of raw source URLs, relation strings, activity/model identifiers, prompts, provider URLs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Core OAuth/API-key readback Debug output now reports token introspection, OAuth errors, authorization-server metadata, API-key records, and API-key create requests as lengths/counts/presence flags instead of raw scopes, client ids, usernames, issuers, endpoint URLs, key prefixes, labels, descriptions, error text, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Core attachment-search request Debug output now reports note/content/event/time/spatial/location/device filters as lengths or presence flags instead of raw MIME filters, event types, coordinates, radius values, location names, emails, phone-like values, paths, or token-shaped values.
  - 2026-06-26 continuation: Core provenance creation request Debug output now reports location, named-location, device, file-provenance, and note-provenance fields as lengths/classes/presence flags instead of raw coordinates, addresses, device names, event descriptions, metadata JSON, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core memory/provenance result Debug output now reports extracted EXIF, spatial/temporal search hits, memory locations, memory devices, provenance records, and provenance chains as lengths/classes/counts/presence flags instead of raw filenames, content types, coordinates, distances, locations, device details, event text, EXIF metadata, URLs, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core specialized media metadata Debug output now reports 3D model and structured media metadata as lengths/counts/presence flags instead of raw formats, bounds, volumes, AI descriptions/models, SVG text, MIDI/tracker names, tracker messages, diagram labels, combined text, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Core embedding record/config Debug output now reports embedding text, vector payloads, and model identifiers as lengths/dimensions instead of raw chunk text, vector values, model names, emails, paths, or token-shaped values.
  - 2026-06-26 continuation: Core job pause-state Debug output now reports global pause state and per-archive pause maps as lengths/counts plus numeric queue stats instead of raw archive names, pause reasons, URLs, emails, or token-shaped values.
  - 2026-06-26 continuation: Search deduplication Debug output now reports chain metadata and enhanced search hits as title lengths/counts plus redacted `SearchHit` metadata instead of raw note titles, snippets, tags, paths, emails, phone-like values, or token-shaped values.
  - 2026-06-26 continuation: Hybrid search request Debug output now reports query text, embedding vectors, filters, config hints, and sort fields as lengths/dimensions/presence flags instead of raw queries, filter strings, vector values, language/script hints, sort values, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: API style and MCP deployment docs no longer instruct clients to use the legacy `{error}` envelope; troubleshooting now points operators to Problem Details `type` and `request_id`.
- [!] **#910** AuditEvent / TracingSink / bounded AuditBuffer — audit baseline. (2026-06-25: first core slice added `AuditEvent`, `AuditSink`, `TracingSink`, bounded `AuditBuffer`, event failure policies, pre-buffer redaction/sanitization, a low-risk process startup producer, and a mapping note for localized audit tables. 2026-06-26: PKE keyset lifecycle slice emits metadata-only `fortemi.audit` events for create/import/activate/delete/export without passphrases, key material, ciphertext, or exported payloads. 2026-06-26: webhook control lifecycle slice emits metadata-only `fortemi.audit` events for subscription create/update/delete/test with principal and webhook refs while excluding destination URLs, HMAC secrets, delivery payloads, and response bodies. Incoming webhook receiver decision slice emits metadata-only audit events for active-secret checks, signature verification, idempotency replay/conflict, payload parse, schema validation, and accepted receipt while excluding HMAC secrets, raw signatures, idempotency keys, slugs, payloads, bodies, and URLs. Inference config mutation slice mirrors set/reset and archive-scoped set/reset changes into metadata-only `fortemi.audit` events with principal, scope, changed-field names/counts, provider presence flags, and no before/after config values. Twilio realtime WebSocket bind decision slice emits metadata-only audit events for unknown call-session denial, stale/ended session denial, and successful bind without raw CallSid values, signatures, media payloads, or WebSocket URLs. Job queue pause/resume control slice emits metadata-only audit events for global and archive-scoped queue control successes with principal, scope, and archive-name presence/length while excluding raw archive names and event-bus scope strings. Attachment upload slice mirrors JSON, multipart, and TUS accepted/denied upload decisions into metadata-only audit events with note/attachment refs, size, content-type class, filename/extension lengths, and stable reason classes while excluding filenames, content bodies, storage paths, archive names, and raw MIME parameters. Hosted mandatory-audit contract slice adds code-level bootstrap-vs-ready audit-unavailable dispositions so fail-closed events degrade during bootstrap but reject hosted-ready operations. SKOS scheme/concept mutation slice mirrors create/update/delete successes into metadata-only taxonomy audit events with resource refs, safe field names/counts, relation counts, status class, archive presence/length, and no labels, definitions, notation values, change JSON, or archive names. SKOS collection slice mirrors collection create/update/delete plus member add/remove/replace successes into metadata-only taxonomy audit events with resource refs, membership counts, position presence, archive presence/length, and no labels, definitions, member lists, or archive names. OAuth lifecycle slice mirrors token issue, introspection, and revocation successes into metadata-only audit events with credential lengths/classes, scope counts, token refs where already persisted, and no bearer tokens, refresh tokens, codes, secrets, redirect URIs, raw scopes, hashes, client IDs, or user IDs. Operator-docs slice documents CE `fortemi.audit` routing through `TracingSink`, external retention/access responsibilities, and non-guarantees for local durability, WORM, tamper evidence, acknowledgements, and hosted fail-closed enforcement. 2026-06-26 blocker: remaining hosted mandatory-audit producer consumption and KMS lifecycle audit wiring require #897 KeyProvider/KMS and hosted audit-health surfaces that do not exist in `crates/` yet.)
- [~] **#968 / #974** hosted secret inventory + telemetry redaction taxonomy. (2026-06-26: first telemetry redaction slice replaced raw AI parse-response, search-query, Twilio recording URL, and DTMF digit logs with bounded lengths/classes and added safe telemetry helper coverage. Inference probe telemetry slice now redacts provider base URLs and raw probe diagnostics across atomic config probes, validate probes, test-connection logs, and startup reachability logs, retaining URL class/length and stable reason/count fields. Inference debug-surface slice replaces derived Debug on BYOK completion, runtime config update, provider partial config, and test-connection requests with manual redacted Debug output that exposes only presence flags, URL classes/lengths, and bounded counts. PKE debug-surface slice replaces derived Debug on secret-bearing PKE HTTP DTOs plus keyset row/create/export structs with manual output that exposes only presence flags, lengths, counts, stable ids/addresses, and timestamps. PKE response/keyset Debug follow-up now reports PKE addresses, recipient addresses, keyset names, labels, and imported exported-keyset metadata as lengths/counts/presence only. OAuth/API-key debug-surface slice replaces derived Debug on client registration, token/code, introspection, revocation, and one-time API-key response DTOs with redacted presence/length/count output. MCP tool-output slice adds a recursive sanitizer boundary for model-visible success/error content and replaces generated curl auth headers with scanner-safe placeholders. Docs-contract slice adds an advisory, redaction-safe #999/#1001 scanner for token/API-key/client-secret placeholders and DB/backup credential examples. Docs-contract baseline slice commits a fingerprint-only baseline for the current 202 known findings and makes blocking mode fail on new findings while allowing known findings during cleanup. Public docs placeholder cleanup replaces scanner-hostile token/client-secret examples in API, auth, realtime, and MCP docs with canonical placeholders and shrinks the known baseline from 202 to 109 findings. Public/operator DB docs cleanup replaces reusable Postgres DSNs and `PGPASSWORD=matric` guidance in docs with placeholders, shrinking the known baseline to 76 findings. Public provider/token docs cleanup replaces remaining provider-key and bearer-token examples in inference, memory-search, operator, release, job, and MCP troubleshooting docs with canonical placeholders, shrinking the known baseline to 36 findings. Docs-contract allowlist slice adds explicit owner/reason/review metadata for the remaining 36 CI, Docker/local-dev, script, and test-fixture findings. Backup subprocess telemetry slice routes pg_dump failures and post-restore psql maintenance warnings through stable command/status/stderr-length fields instead of raw stderr in client details or ordinary logs. Public streaming-health inbound metrics now aggregate source count/events/errors/lag without exposing connector names or per-source activity on the auth-exempt endpoint. Inbound connector ordinary logs now use source-name lengths, destination classes/lengths, and stable reason codes for SSE/Redis/Kafka connect, supervisor fetch/build/commit/DLQ failures, and outbox write failures instead of raw URLs, connector names, stream/topic/group names, or backend errors. The `fortemi::events` `job.failed` tracing mirror now logs error class and length instead of raw job failure text. Job API response Debug output now reports job/note id presence, payload/result classes and lengths, and progress/error lengths instead of raw job IDs, payload/result JSON, progress messages, paths, DSNs, URLs, or generated text. Hosted telemetry classification slice adds a cross-linked policy for `security_audit`, `operational_event`, `metrics_health`, `diagnostic_sensitive`, `content_prohibited`, and retained `event_outbox`/`inbound_dlq` payload sinks. Inbound DLQ retained-payload telemetry now logs payload class, serialized length, and secret-candidate flag for dead-letter events without raw payload snippets. Docs-contract passphrase/webhook-secret slice adds #999 scanner coverage for realistic passphrase and webhook secret examples and replaces public/generated examples with `<PKE_PASSPHRASE>` and `<WEBHOOK_SECRET>` while keeping contract output at `new=0`. Sidecar subprocess telemetry slice routes docker compose start/stop failures through stable status, stderr length, and reason-code fields instead of raw sidecar stderr or IO error text. Media optimizer subprocess telemetry now routes ffprobe/ffmpeg failures through stable status, stderr length, reason-code, and IO-kind fields instead of raw subprocess stderr, paths, or token-shaped diagnostics. Audio utility subprocess telemetry now routes ffmpeg/ffprobe transcode, duration, and chunk-split failures through stable status, stderr length, reason-code, IO-kind, and path-length fields instead of raw stderr, filesystem paths, or OS diagnostics. Video multimodal subprocess telemetry now routes ffprobe duration/media-info/faststart and ffmpeg keyframe/faststart failures through stable status, stderr length, reason-code, IO-kind, and path-length fields instead of raw stderr, filesystem paths, or OS diagnostics. Video thumbnail/waveform logs now report media path lengths for thumbnail and waveform success/failure logs instead of raw filesystem paths. Video keyframe-read warnings now report extracted frame path lengths instead of raw temporary frame filesystem paths. PDF and Office extraction subprocess failures now report command, phase, status, stderr length, reason code, and IO kind instead of raw pdftotext/pdfinfo/pdftoppm/tesseract/pandoc stderr or OS diagnostics. GLB renderer telemetry now reports renderer destination class, URL length, filename length, status/body/error lengths, and stable reason codes instead of raw renderer URLs, filenames, response bodies, or backend errors. Extraction derived-file and completion/failure telemetry now reports filename/path lengths plus stable error reason classes instead of raw derived filenames, source paths, or extraction backend error text. Worker and sidecar lifecycle logs now report error lengths and stable reason classes instead of raw stale-job, model warmup/unload, or sidecar lifecycle backend error text. Keyframe vision handler logs now report error lengths and stable reason classes instead of raw vision backend, transcript-context, fan-in, or queue backend error text. View vision handler logs now report error lengths and stable reason classes instead of raw vision backend, fan-in, or queue backend error text. Keyframe/view assembly logs now report error lengths, stable reason classes, and note presence flags instead of raw note ids or note/update/manifest/downstream queue backend error text. Audio chunk transcription job telemetry and failure/result text now report parent attachment presence, error lengths, merge-error lengths, and stable reason codes instead of raw attachment UUIDs, schema/database diagnostics, Whisper/backend errors, DSNs, paths, hostnames, or token-shaped values. Speaker relabel job failure text now reports stable reason codes instead of raw schema names, note/attachment UUIDs, speaker-map parser diagnostics, database/storage diagnostics, DSNs, paths, hostnames, or token-shaped values. Jobs inbound/worker event Debug output now reports inbound payloads, offsets, connector errors, worker progress, job refs, and failure text as lengths/classes/reason codes/presence flags instead of raw payload JSON, offsets, job UUIDs, progress messages, backend errors, URLs, emails, DSNs, paths, or token-shaped values. Jobs archive/code adapter Debug output now reports archive entry filenames, skip reasons, and code declaration names as lengths/classes with numeric metadata instead of raw paths, symbol names, emails, DSNs, URLs, or token-shaped values. Realtime codec Debug output now reports telephone-event/DTMF payloads as presence flags instead of raw event codes while preserving non-content codec metadata. Spreadsheet extraction now emits stable cell and sheet-read error reason codes/lengths instead of raw calamine debug values or backend error strings in extracted markdown or metadata. JobResult Debug output now reports result presence/class/serialized length and failure/retry error lengths instead of raw job result JSON or handler error text. Inference retry telemetry now emits stable reqwest reason/status/length metadata and safe propagated error text instead of raw backend request errors, URLs, or diagnostics. Inference sidecar HTTP errors now report status, body length, and stable body reason for GLiNER, pyannote, Whisper model download/transcription, and vision backends instead of raw upstream response bodies. OpenAI-compatible provider errors now report request, parse, stream, status, body length, and stable reason metadata instead of raw reqwest diagnostics or upstream response bodies. Ollama provider and summarizer errors now report request, parse, stream, status, body length, and stable reason metadata instead of raw reqwest diagnostics, upstream bodies, provider URLs, or model names. Webhook delivery response diagnostics now persist status, length, reason, and secret-candidate metadata instead of raw downstream response bodies or delivery error strings. Job progress messages for concept tagging, reference extraction, related-concept inference, document-type inference, embedding-set requeue, and media-variant storage now store fixed labels or length metadata instead of raw extracted labels, entity names, related concept labels, document type names, embedding-set slugs, or variant identifiers. Linking and document-type inference job/provenance summaries now store strategy, document-type id, and detection-method metadata as presence/length fields instead of raw labels/ids. AI revision and contextual revision job/provenance summaries now store revision/effective-mode lengths instead of raw mode labels. Graph maintenance completion telemetry and retained job results now store schema length instead of raw archive schema labels. Purge-note retained job results now store deleted-note presence metadata instead of raw deleted note UUIDs. Metadata extraction retained job results now store extracted-key counts/lengths/secret-candidate metadata instead of raw model-derived metadata keys. Graph maintenance snapshot step results now store snapshot-id presence metadata instead of raw diagnostics snapshot UUIDs. Core audit event Debug output now reports identifier presence, text lengths, and attribute counts instead of raw audit event identifiers or attribute content. API archive context Debug output now reports schema/name lengths and default status instead of raw archive schema labels or human-readable memory names. GLB renderer health warnings now report renderer-unavailable reason classes and lengths instead of raw renderer diagnostic strings. Hybrid search tracing now reports query class and character length instead of raw search query text.)
  - 2026-06-27 continuation: Backup script database credentials now require explicit `PGPASSWORD` or `PGPASSFILE` instead of defaulting to the reusable `matric` password.
  - 2026-06-27 continuation: OAuth/API-key Debug metadata now uses character-count lengths for token introspection, OAuth errors, authorization-server metadata, API-key records, and API-key creation requests while preserving raw-value redaction.
  - 2026-06-27 continuation: Realtime call-detail Debug metadata now uses character-count provider and end-reason lengths while preserving redaction of provider-call metadata, recording URLs, remote parties, transcripts, and raw call identifiers.
  - 2026-06-27 continuation: Core note/link/search Debug metadata now uses character-count lengths for note metadata, revisions, concept summaries, note summaries, links, and search hits while preserving raw content, URL, tag, metadata, and identifier redaction.
  - 2026-06-27 continuation: API backup, restore, post-restore maintenance, and psql size-query subprocesses now require explicit `PGPASSWORD` or `PGPASSFILE` instead of injecting the reusable `matric` password.
  - 2026-06-27 continuation: Backup script scratch handling now requires an explicit private `BACKUP_TEMP_DIR`, rejects shared `/tmp`, verifies mode/ownership, and requires tmpfs/ramfs or operator-marked encrypted scratch for real backups.
  - 2026-06-27 continuation: Backup status/list/info path response metadata now uses structured length-only `path_metadata` tokens with path, filename, and extension lengths instead of raw absolute paths, DSNs, hostnames, or token-shaped path fragments.
  - 2026-06-27 continuation: Backup script dry-run, distribution, retention, verification, and subprocess verbose logs now report destination/path/output classes plus lengths instead of raw local paths, scratch paths, rsync/S3 URIs, or command output lines.
  - 2026-06-27 continuation: Backup script verbose config-file and encryption-key diagnostics now report filesystem path class/length metadata instead of raw operator config or key paths.
  - 2026-06-27 continuation: PKE crypto file/header Debug output now reports public-key, recipient, filename, encrypted-DEK, nonce, salt, and KDF string metadata as lengths/counts/presence instead of raw addresses, filenames, encrypted key material, salts, or nonces.
  - 2026-06-27 continuation: PKE CLI keygen/decrypt now support `--passphrase-stdin` and `--passphrase-file`, require exactly one passphrase source, keep legacy `--passphrase` explicit, and public encryption docs no longer show passphrases in argv.
  - 2026-06-27 continuation: PKE CLI legacy `--passphrase/-p` usage now warns that argv and shell history can expose secrets and points operators to stdin/file passphrase sources without echoing the submitted value.
  - 2026-06-27 continuation: Public PKE Rust API docs now use `<PKE_PASSPHRASE>` placeholders for private-key save/load examples, and docs-contract now rejects realistic PKE passphrase literals in helper-call examples.
  - 2026-06-27 continuation: Public `matric-crypto` Rustdoc entry points now use `<PKE_PASSPHRASE>` placeholders in key-save examples, and docs-contract scans those Rustdoc surfaces for realistic helper-call passphrase literals.
  - 2026-06-27 continuation: SSE EventSource API docs now use `<STREAM_TOKEN>` and `<ACCESS_TOKEN>` placeholders instead of `mm_at_xxx`/`mm_key_xxx` token-shaped examples in query/header auth guidance.
  - 2026-06-27 continuation: PKE CLI success JSON now reports path and original-filename metadata lengths/presence instead of raw local paths or embedded filenames, and public examples use the metadata-only shape.
  - 2026-06-27 continuation: PKE CLI invalid address verification now reports submitted-address metadata and stable error codes instead of echoing arbitrary invalid address strings or parser diagnostics.
  - 2026-06-27 continuation: Chat-stream replay/resumption Redis keys now use SHA-256 session fingerprints instead of storing raw session ids in Redis keyspace while preserving public SSE resume cursor format.
  - 2026-06-27 continuation: Ingest stream cursor Redis keys now use SHA-256 stream fingerprints instead of storing raw stream ids in Redis keyspace while preserving public cursor format.
  - 2026-06-27 continuation: Ingest stream token Redis keys and reverse-index values now use SHA-256 token fingerprints instead of storing raw bearer tokens in Redis keyspace or revoke-by-id index values.
  - 2026-06-27 continuation: Incoming webhook idempotency Redis keys now use SHA-256 fingerprints of receiver slug plus idempotency key instead of storing raw slugs or caller-supplied idempotency keys in Redis keyspace.
  - 2026-06-27 continuation: Core authorization policy contract Debug output now reports action/resource/context/decision/obligation metadata instead of raw action names, scopes, resource identifiers, tenant/correlation identifiers, context environment keys/values, custom deny reasons, policy ids, or obligation text.
  - 2026-06-27 continuation: Core file-safety validation Debug output now reports block-reason lengths and detected-type classes/lengths instead of raw user-controlled validation reasons or detected-type strings.
  - 2026-06-27 continuation: API authorization credential/PKE route metadata Debug output now reports id presence and field lengths instead of raw API-key prefixes, API-key names/scopes, PKE keyset names, PKE addresses, or labels.
  - 2026-06-27 continuation: API collection/template route metadata Debug output now reports id presence, name/format lengths, parent/collection presence, and counts instead of raw collection/template UUIDs, names, formats, emails, paths, or token-shaped values.
  - 2026-06-27 continuation: API taxonomy concept/scheme/collection route metadata Debug output now reports id presence, notation/status/version lengths, scheme presence, and count/boolean metadata instead of raw taxonomy UUIDs, notation/version/status strings, emails, paths, or token-shaped values.
  - 2026-06-27 continuation: API document-type route metadata Debug output now reports id presence, name/category lengths, and boolean flags instead of raw document-type UUIDs, names, categories, emails, paths, or token-shaped values.
  - 2026-06-27 continuation: Core TracingSink audit telemetry now reports audit id/idempotency/principal/tenant/resource/correlation presence, category/action/reason lengths, and attribute counts instead of raw audit identifiers, category/action/reason strings, resource ids, or sanitized attribute maps.
  - 2026-06-27 continuation: DB file-storage cleanup telemetry now reports blob/parent-attachment id presence instead of raw blob UUIDs or parent attachment UUIDs in orphaned-blob delete warnings and derived-caption cleanup logs.
  - 2026-06-27 continuation: Core EventBus emit telemetry now reports event-type length, event-id presence, and memory-scope length instead of raw SSE event ids, event type strings, or memory scope names in debug logs.
  - 2026-06-27 continuation: Removed tracked stale `crates/matric-core/src/models.rs.bak`, which preserved pre-redaction core model definitions with derived Debug on note content, OAuth/client-secret/token fields, API-key creation responses, and other secret-bearing surfaces outside the compiled module.
  - 2026-06-27 continuation: Public use-case, configuration, troubleshooting, `.env.example`, and bundle Dockerfile examples now use canonical MCP OAuth placeholders instead of inconsistent `xxx`/`xxxxx`/`xxxxxxxxxxxxx` client-secret examples.
  - 2026-06-27 continuation: Public config, image, and quickstart provider-key examples now use neutral `<OPENAI_API_KEY>`, `<OPENROUTER_API_KEY>`, and `<HF_TOKEN>` placeholders instead of scanner-shaped `sk-*`, `sk-or-*`, or `hf_*` values, shrinking docs-contract known findings to 34.
  - 2026-06-27 continuation: MCP feature-chain secret fixture and extraction-service deployment docs now use neutral API-key/HuggingFace placeholders instead of scanner-shaped `sk_test_*` or `hf_*` values, shrinking docs-contract known findings to 33.
  - 2026-06-27 continuation: MCP integration test runner usage now uses a scanner-safe `<ACCESS_TOKEN>` placeholder instead of an `mm_at`-shaped token example, shrinking docs-contract known findings to 32.
  - 2026-06-27 continuation: Production/test quick-start scripts and MCP API-error fixtures now avoid hardcoded local Postgres passwords and inline credential-bearing DSN literals, shrinking docs-contract known findings to 27.
  - 2026-06-27 continuation: MCP agentic test docs and the CI shard rebuild harness now use placeholder/generated database credentials instead of hardcoded Postgres passwords or inline credential-bearing DSN literals, shrinking docs-contract known findings to 24.
  - 2026-06-27 continuation: The Gitea test workflow now uses generated per-run database passwords and runtime-constructed Postgres DSNs instead of hardcoded local DB credentials, shrinking docs-contract known findings to 13.
  - 2026-06-27 continuation: The Gitea CI builder workflow now uses generated per-run database passwords and runtime-constructed Postgres DSNs instead of hardcoded local DB credentials, shrinking docs-contract known findings to 8.
  - 2026-06-27 continuation: Docker, bundle image, and local compose defaults now avoid hardcoded local DB passwords and inline credential-bearing DSN literals; hosted-strict docs-contract findings are now 0 and blocking mode passes.
  - 2026-06-27 continuation: Core enum parser validation errors for embedding sets/statuses, document categories, chunking/extraction/attachment statuses, embedding providers, and SKOS/tag relation/status types now report invalid value lengths instead of echoing raw user/operator supplied strings.
  - 2026-06-27 continuation: Twilio realtime adapter validation errors now report stable diagnostic classes and lengths instead of raw media payload, timestamp, sequence, or webhook form parser diagnostics.
  - 2026-06-27 continuation: API route-policy Debug output now reports route/action-family lengths and nested authorization metadata instead of raw route templates, action-family labels, action names/scopes, resource ids, tenant ids, or context environment values.
  - 2026-06-27 continuation: Reconstruction chunk-chain not-found errors now report chain-id presence metadata instead of raw chain UUIDs.
  - 2026-06-27 continuation: Tag resolver required tag/scheme not-found errors now report notation length metadata instead of raw tag or scheme notation strings.
  - 2026-06-27 continuation: Core EXIF read errors now report parser diagnostic length metadata instead of raw EXIF parser diagnostics.
  - 2026-06-27 continuation: DB document-type in-use delete errors now report document-type name length and reference-count presence metadata instead of raw document-type names or exact reference counts.
  - 2026-06-27 continuation: API collection delete in-use conflicts now report note-count presence and force-required metadata instead of the exact referenced-note count.
  - 2026-06-27 continuation: DB SKOS scheme/concept delete in-use errors now report count-presence and force-required metadata instead of exact concept or note-tag counts.
  - 2026-06-27 continuation: DB embedding-config delete in-use errors now report embedding-set count presence instead of exact embedding-set usage counts.
  - 2026-06-27 continuation: Deepgram realtime ASR transport, parser, and provider error paths now report stable diagnostic classes and lengths instead of raw request, WebSocket, JSON parser, or provider error text.
  - 2026-06-27 continuation: DB PKE keyset import and duplicate-name validation errors now report stable diagnostic classes/name lengths instead of raw base64 parser diagnostics or keyset names.
  - 2026-06-27 continuation: DB incoming-webhook schema validation errors now report schema/path/diagnostic metadata as lengths/classes instead of raw schema refs, JSON pointer paths, property names, payload values, or parser diagnostics.
  - 2026-06-27 continuation: DB schema-name validation errors now report first-character classes, invalid-character classes, and name lengths instead of raw schema names, SQL-injection fragments, secret-shaped identifiers, or Unicode input.
  - 2026-06-27 continuation: DB note not-found errors now report note-id presence metadata instead of raw note UUIDs in repository fetch/update paths.
  - 2026-06-27 continuation: DB attachment delete not-found errors now report attachment-id presence metadata instead of raw attachment UUIDs.
  - 2026-06-27 continuation: DB document-type not-found errors now report document-type name length metadata instead of raw document-type names or lookup values.
  - 2026-06-27 continuation: DB archive not-found errors now report archive-name length metadata instead of raw archive names or source archive lookup values.
  - 2026-06-27 continuation: DB embedding-set/config not-found errors now report slug length and id-presence metadata instead of raw embedding-set slugs or config/set UUIDs.
  - 2026-06-27 continuation: DB PKE keyset not-found errors now report keyset id presence or name length metadata instead of raw keyset UUIDs or names.
  - 2026-06-27 continuation: DB note-version not-found errors now report version-presence metadata instead of raw version numbers.
  - 2026-06-27 continuation: API template not-found errors now report template-id presence metadata instead of raw template UUIDs.
  - 2026-06-27 continuation: API concept-update not-found errors now report concept-id presence metadata instead of raw concept UUIDs.
  - 2026-06-27 continuation: API graph diagnostics snapshot not-found errors now report snapshot side and id-presence metadata instead of raw snapshot UUIDs.
  - 2026-06-27 continuation: Realtime call-session not-found errors now report call-id presence metadata instead of raw call UUIDs.
  - 2026-06-27 continuation: Core split memory-search model source now mirrors the compiled memory/search Debug redaction contract, keeping memory hits, timelines, cross-archive search requests/results, attachment filters, filenames, snippets, tags, locations, device IDs, and archive names represented as lengths/counts/presence flags instead of raw user/content/topology strings.
  - 2026-06-27 continuation: Hybrid search config Debug output now reports weighting, archive behavior, embedding-set presence, deduplication options, strict/unified filter presence, language/script hint lengths, feature-flag metadata, and diversity metadata instead of raw filter values, language/script hints, paths, emails, or token-shaped values.
  - 2026-06-27 continuation: Media optimization ffprobe skip results now store stable reason codes and error lengths instead of raw subprocess diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: Media optimization static skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: Thumbnail sprite skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: EXIF no-image skip results now store a stable reason code instead of prose diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: Core list response Debug output now reports note and global-attachment response envelopes as counts/totals instead of delegating into nested note summaries or attachment summaries that can carry titles, snippets, filenames, content types, tags, document-type labels, metadata, paths, emails, DSNs, UUIDs, or token-shaped values.
  - 2026-06-27 continuation: DB strict-filter list response Debug output now reports note envelope counts/totals instead of delegating into nested note summaries that can carry titles, snippets, tags, metadata values, document-type labels, emails, DSNs, paths, UUIDs, or token-shaped values.
  - 2026-06-27 continuation: DB strict-filter list request Debug output now reports active filter dimensions and sort-string lengths instead of raw strict-filter values, sort fields, emails, paths, UUIDs, or token-shaped values.
  - 2026-06-27 continuation: DB graph diagnostics Debug output now reports metric payload counts, totals, and presence flags instead of raw diagnostic vectors or nested metric payloads.
  - 2026-06-27 continuation: DB graph maintenance SNN/PFNET result Debug output now reports distribution counts/totals and numeric controls instead of raw SNN score-distribution vectors.
  - 2026-06-27 continuation: DB archive schema-sync missing-column Debug output now reports table/column/type/default lengths instead of raw schema names, column names, or SQL default text.
  - 2026-06-27 continuation: Core audit sink error Display/Debug output now reports sink message class and length instead of raw backend failure text, paths, or token-shaped values.
  - 2026-06-27 continuation: Core MatricError Display/Debug output now reports message classes, lengths, and id-presence flags instead of raw backend errors, resource ids, paths, URLs, or token-shaped values.
  - 2026-06-27 continuation: DB ColBERT stats Debug output now reports total-size string length instead of raw human-size text that can carry backend strings, paths, URLs, or token-shaped values.
  - 2026-06-27 continuation: Core OAuth token Debug output now reports token-type lengths instead of raw token-type strings that can carry token-shaped values.
  - 2026-06-27 continuation: Speaker relabel no-op results now store stable status/count metadata instead of prose diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: AI revision/contextual revision and bulk re-embed skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
  - 2026-06-27 continuation: Archive adapter empty-input metadata now stores a stable error code instead of prose diagnostic text in retained extraction metadata.
  - 2026-06-27 continuation: PKE public-key registry Debug output now reports address/key/label lengths instead of raw addresses, key bytes, or labels.
  - 2026-06-27 continuation: PKE keyset summary/create/export Debug output now reports name/address/key/label lengths instead of raw keyset names, PKE addresses, key blobs, or labels.
  - 2026-06-27 continuation: Event outbox record/create Debug output now reports event/payload/memory lengths and classes instead of raw retained payload JSON, event types, or memory labels.
  - 2026-06-27 continuation: Realtime call metrics Debug output now reports backend-key count/length metadata instead of raw ASR backend labels.
  - 2026-06-27 continuation: DB chunk Debug output now reports text and metadata lengths/counts instead of raw chunk text or metadata key/value strings.
  - 2026-06-27 continuation: DB strict/unified filter Debug output now reports query parameter kinds, counts, and SQL lengths instead of raw UUIDs, filter strings, SQL fragments, or generated CTEs.
  - 2026-06-27 continuation: OAuth introspection/revocation request Debug output now reports token-type-hint classes instead of raw user-supplied hint strings.
  - 2026-06-27 continuation: Event envelope Debug output now reports payload variant/type metadata instead of delegating to nested event payload Debug output.
  - 2026-06-27 continuation: Backup import note Debug output now reports note/collection id presence instead of raw imported note or collection UUIDs.
  - 2026-06-27 continuation: Related/federated search Debug output now reports note-id presence instead of raw related/search result note UUIDs.
  - 2026-06-27 continuation: Reconstruction service Debug output now reports document/chunk id presence instead of raw reconstructed document or chunk UUIDs.
  - 2026-06-27 continuation: Memory info Debug output now reports embedding-set id presence instead of raw embedding-set UUIDs.
  - 2026-06-27 continuation: Core webhook Debug output now reports webhook/delivery/receiver id presence instead of raw webhook UUIDs.
  - 2026-06-27 continuation: Core inbound source Debug output now reports source id presence instead of raw inbound-source UUIDs.
  - 2026-06-27 continuation: Core OAuth client/token Debug output now reports id presence instead of raw OAuth row UUIDs.
  - 2026-06-27 continuation: DB event outbox Debug output now reports outbox record id presence instead of raw outbox UUIDs.
  - 2026-06-27 continuation: DB PKE keyset Debug output now reports keyset id presence instead of raw keyset UUIDs.
  - 2026-06-27 continuation: Core one-time API-key response Debug output now reports id presence instead of raw API-key UUIDs.
  - 2026-06-27 continuation: Inference config audit-row Debug output now reports audit-row id presence instead of raw localized audit row ids.
  - 2026-06-27 continuation: DB graph community Debug output now reports community-id presence instead of raw graph community identifiers.
  - 2026-06-27 continuation: Job pause persisted-state Debug output now reports paused archive counts instead of raw archive names.
  - 2026-06-27 continuation: Core strict-filter Debug output now reports active dimensions, constraint presence, query-planning flags, identifier presence/counts, metadata string lengths, and boolean/numeric controls instead of raw tag strings, collection/security/embedding identifiers, metadata format/source strings, paths, URLs, emails, or token-shaped values.
  - 2026-06-27 continuation: Core collection-filter Debug output now reports collection identifier counts/presence, recursive-query flags, collection path counts, and path lengths instead of raw collection UUIDs, collection path strings, paths, URLs, emails, or token-shaped values.
  - 2026-06-27 continuation: Core embedding-provider request Debug output now reports config/model/content-type lengths, provider-config JSON class/length, MRL/HNSW numeric settings, and document-composition presence instead of raw config names, descriptions, model IDs, provider URLs/API-key fields, content types, paths, emails, or token-shaped values.
  - 2026-06-27 continuation: Core EXIF Debug output now reports capture/device/location presence, orientation, dimensions, GPS coordinate presence, altitude presence, and device string lengths instead of exact GPS coordinates, altitude values, camera make/model/software strings, paths, emails, or token-shaped values.
  - 2026-06-27 continuation: Core FAIR context/score Debug output now reports JSON-LD context namespaces and FAIR assessment issue strings as lengths/counts plus numeric scores instead of raw namespace URLs, assessment text, URLs, emails, paths, or token-shaped values.
  - 2026-06-27 continuation: Core trait request/result Debug output now reports note listing filters, note creation fields, search queries, extraction results, derived files, AI metadata, and template requests as lengths/counts/classes/presence flags instead of raw content, tags, metadata JSON, generated summaries, filenames, filesystem paths, URLs, emails, or token-shaped values.
  - 2026-06-27 continuation: Core event Debug output now reports actors, event context, envelopes, server-event payloads, and variant metadata as lengths/counts/presence flags instead of raw actor ids/names, memory scopes, tenant ids, job errors, progress messages, note titles/tags, filenames, archive names, inference config field names, URLs, emails, paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: API problem-type catalog Debug output now reports problem URIs, titles, and descriptions as lengths plus status codes instead of raw URLs, public titles, or descriptive strings.
  - 2026-06-27 continuation: Core embedding lifecycle and extraction stats Debug output now reports embedding-set ids as presence flags and extraction strategy maps as counts/key lengths instead of raw set UUIDs, strategy names, URLs, emails, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Audio transcription handler job failure/retry text now reports stable phases, error lengths, reason codes, and IO-kind/path-length metadata instead of prose-only backend or OS diagnostics.
  - 2026-06-27 continuation: Audio utility chunked and single-pass transcription backend failures now report phase, backend-name length, audio-size, error-length, and stable reason metadata instead of raw backend names or backend diagnostics.
  - 2026-06-27 continuation: SSE and Redis inbound connector transient errors now store connector, phase, error-length, and reason-code metadata instead of raw config, client, connect, read, or ack diagnostics.
  - 2026-06-27 continuation: Kafka inbound connector transient errors now store connector, phase, error-length, and reason-code metadata instead of raw config, subscribe, producer, commit, receive, or malformed-offset diagnostics.
  - 2026-06-27 continuation: Email adapter parse failures now report parser diagnostic length and stable reason metadata instead of raw mail parser diagnostics that can contain headers, addresses, paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Archive adapter ZIP/TAR parser failures now report phase, diagnostic length, and stable reason metadata instead of raw archive parser diagnostics that can contain paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Media optimizer ffprobe JSON parse failures now report parser diagnostic length and stable reason metadata instead of raw serde parser diagnostics that can contain paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Job pause-state serialization failures now report diagnostic length and stable reason metadata instead of raw serde diagnostics that can contain paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Core tokenizer initialization failures now report tokenizer family, diagnostic length, and stable reason metadata instead of raw third-party tokenizer diagnostics that can contain paths, DSNs, or token-shaped values.
  - 2026-06-27 continuation: DB graph diagnostics Debug output now reports graph nodes, edges, traversal metadata, topology strategy, diagnostics snapshots/deltas, and coarse communities as counts, lengths, classes, and presence flags instead of raw note ids, titles, labels, provenance strings, JSON metrics, delta summaries, URLs, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: API cold-spot row Debug output now reports note-id presence and title length metadata instead of raw cold-spot note UUIDs or titles.
  - 2026-06-27 continuation: Core graph environment warning telemetry now reports invalid config value length/class metadata instead of raw `GRAPH_LINKING_STRATEGY` or `GRAPH_K_NEIGHBORS` values.
  - 2026-06-27 continuation: Embedding-set refresh jobs now queue new work by set id, keep legacy slug fallback for existing queued jobs, and persist/log set slug length metadata instead of raw embedding-set slugs in job payload/result and refresh telemetry.
  - 2026-06-27 continuation: SSE connection telemetry now reports memory filter length, event-type filter count/total length, and entity-filter presence instead of raw memory/archive filter values, event type prefixes, or entity identifiers.
  - 2026-06-27 continuation: Stored AI revision, title-generation, and EXIF job payload/result/provenance fields now use document-type ids/lengths, title lengths, filename lengths, and id-presence flags instead of raw document type names, generated titles, filenames, or provenance/attachment UUIDs.
  - 2026-06-27 continuation: Job pause/resume operational telemetry now logs archive-name lengths and emits archive-scoped job-control server events with safe scope metadata instead of raw archive names or `archive:<name>` event scopes.
  - 2026-06-27 continuation: Twilio recording import now persists recording URL class/length and provider-call-id length metadata in note metadata, transcription job payloads, call-session metadata, and generated filenames instead of raw recording URLs or Twilio provider call identifiers.
  - 2026-06-27 continuation: Twilio recording attachment filenames now use call-id length metadata and audio extension only instead of raw call/provider identifiers that can contain paths, emails, or token-shaped values.
  - 2026-06-27 continuation: OAuth registration and token grant/scope validation errors now report submitted-value length metadata and allowed-count metadata instead of raw `grant_type` or scope strings that can contain paths, emails, URLs, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Jobs extraction registry missing-adapter errors now report extraction-strategy length metadata instead of raw `Debug`/display strategy labels in support-safe internal diagnostics.
  - 2026-06-27 continuation: GLB/3D inline vision prompts now send uploaded filename length metadata to the vision backend instead of raw filenames that can contain paths, emails, or token-shaped values.
  - 2026-06-27 continuation: Retained Twilio `call_event` outbox payloads now store provider-call-id lengths, remote-party presence/length, metadata class/length, and recording URL class/length instead of raw provider call identifiers, phone numbers, metadata JSON, or recording URLs.
  - 2026-06-27 continuation: Twilio voice webhook side-effect responses now return provider-call-id presence/length and recording URL class/length metadata instead of raw Twilio provider call identifiers or recording URLs.
  - 2026-06-27 continuation: Twilio realtime ASR session metadata now sends provider-call-id presence/length and call-id presence metadata to ASR backends instead of raw Twilio provider call identifiers or internal call UUIDs.
  - 2026-06-27 continuation: DB archive clone duplicate-name errors now report archive-name length metadata instead of raw destination archive names that can contain paths, emails, or token-shaped values.
  - 2026-06-27 continuation: Call-detail API responses now summarize provider call identifiers, ASR backend labels, remote parties, and session metadata as presence/length/class fields instead of returning raw provider IDs, phone numbers, backend labels, recording URLs, or metadata values.
  - 2026-06-27 continuation: Twilio media-stream stop custom control payloads now carry provider-call-id presence/length metadata instead of raw Twilio CallSid values.
  - 2026-06-27 continuation: Twilio media-stream mark custom control payloads now carry mark-name presence/length metadata instead of raw provider mark names.
  - 2026-06-27 continuation: Event-bus telemetry mirror logs now emit job/note identifier presence/length metadata and job-type lengths instead of raw job IDs, note IDs, or job-type strings.
  - 2026-06-27 continuation: Worker job-processing, handler-registration, completion/failure, and NLP queue-failure telemetry now reports job-type lengths instead of raw job-type labels.
  - 2026-06-27 continuation: Attachment extraction queue success/failure logs now report note/attachment/job id presence/length metadata plus strategy and filename lengths instead of raw ids, strategy labels, or filenames.
  - 2026-06-27 continuation: Extraction handler downstream requeue, fanout, completion, and failure logs now report job-type and strategy lengths instead of raw job-type or strategy labels.
  - 2026-06-27 continuation: Extraction handler skip-vision, skip-transcription, and diarization-skip logs now report strategy lengths instead of raw extraction strategy labels.
  - 2026-06-27 continuation: Keyframe and view assembly downstream queue failure logs now report job-type lengths instead of raw job-type labels.
  - 2026-06-27 continuation: SSE replay and webhook-list failure telemetry now reports Last-Event-ID cursor and event-type lengths instead of raw cursor or event-type strings.
  - 2026-06-27 continuation: Inference config probe and embedding-routing telemetry now reports provider label lengths instead of raw request, detected, probe, or embedding provider labels.
  - 2026-06-27 continuation: Job model-profile, linking, GLiNER, and document-type completion telemetry now reports model, strategy, document-type, and detection-method lengths instead of raw labels.
  - 2026-06-27 continuation: Inference config audit, atomic-probe, and archive-override telemetry now reports action/schema/provider label lengths and counts instead of raw action, schema, or failed-provider labels.
  - 2026-06-27 continuation: Search cache-hit telemetry now reports query and archive-schema lengths instead of raw query text or archive schema labels.
  - 2026-06-27 continuation: Audio chunked-transcription completion telemetry now reports language length instead of raw language/provider text.
  - 2026-06-27 continuation: AI revision completion telemetry now reports revision-mode and effective-mode lengths instead of raw mode labels.
  - 2026-06-27 continuation: Authorization denial warning telemetry now reports deny-reason length instead of raw policy reason labels.
  - 2026-06-27 continuation: Startup logging and process-startup audit metadata now report log-format class/length instead of raw operator-provided `LOG_FORMAT` values.
  - 2026-06-27 continuation: GLB renderer health telemetry now reports renderer test-status class/length instead of raw renderer-provided status text.
  - 2026-06-27 continuation: Worker tiered-batch and claim-failure telemetry now reports stable tier classes instead of raw `TierGroup` debug labels.
  - 2026-06-27 continuation: Access-frequency health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
  - 2026-06-27 continuation: Orphan-tag health diagnostics now return tag presence/length metadata instead of raw tag names.
  - 2026-06-27 continuation: Stale-note health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
  - 2026-06-27 continuation: Unlinked-note health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
  - 2026-06-27 continuation: Tag co-occurrence health diagnostics now return tag presence and length metadata instead of raw tag pair names.
  - 2026-06-27 continuation: Streaming RTP health metrics now return backend duration count/total metadata instead of raw ASR backend label maps.
  - 2026-06-27 continuation: Retained inbound outbox envelopes now store source, offset, and event-type presence/length metadata instead of raw wrapper values.
  - 2026-06-27 continuation: Retained inbound DLQ rows now store source fingerprints plus offset/error metadata instead of raw source names, offsets, or error text.
  - 2026-06-27 continuation: Note access analytics log rows now store access-source presence/length metadata instead of raw source labels.
  - 2026-06-27 continuation: SKOS taxonomy audit log rows now store/read back actor and change-payload metadata instead of raw actor identifiers, changed field names, labels, nested values, or note/reference identifiers.
  - 2026-06-27 continuation: TUS upload sessions now aggregate arbitrary upload metadata as counts/lengths/secret-candidate totals and only retain finite `vision_mode` values instead of raw client-supplied metadata keys, metadata values, callback URLs, API-key-like values, or custom vision-mode strings.
  - 2026-06-27 continuation: Attachment-backed extraction jobs now retain filename and MIME lengths in queued payloads while resolving raw filename/MIME from file storage at execution time, removing raw client filenames and content-type strings from `job_queue.payload`.
  - 2026-06-27 continuation: Media optimization jobs now retain content-type length metadata in queued payloads while resolving raw content type from file storage at execution time, removing raw audio/video MIME strings from `job_queue.payload`.
  - 2026-06-27 continuation: Twilio recording audio-transcription jobs now retain call-id presence plus provider-call and recording metadata instead of raw call UUIDs, provider call identifiers, recording URLs, or token-bearing URL fragments in `job_queue.payload`.
  - 2026-06-27 continuation: AI contextual revision job results now retain document-type name length metadata instead of raw content-type/document-type names in stored `job_queue.result` payloads.
  - 2026-06-27 continuation: Embedding provenance metadata now retains embedding model length and embedding-set presence metadata instead of raw model names or embedding-set UUIDs in retained provenance activity payloads.
  - 2026-06-27 continuation: Audio transcription job results now retain language presence/length metadata instead of raw provider language labels in stored `job_queue.result` payloads.
  - 2026-06-27 continuation: View assembly jobs now retain filename length metadata in queued payloads while resolving the parent model filename from attachment storage at execution time, removing raw 3D model filenames from the downstream `job_queue.payload`.
  - 2026-06-27 continuation: Note markdown export attachment filenames now use note-title length metadata plus note id instead of raw note titles in `Content-Disposition` headers.
  - 2026-06-27 continuation: Attachment media and subtitle `Content-Disposition` filenames now use filename-length metadata plus attachment ids instead of raw stored attachment filenames.
  - 2026-06-27 continuation: Public attachment and memory-backup docs now show metadata-only `Content-Disposition` filename examples instead of raw uploaded filenames or archive names.
  - 2026-06-27 continuation: Public operations docs now use the canonical `<MCP_CLIENT_SECRET>` placeholder instead of a scanner-hostile realistic OAuth/MCP client-secret example.
  - 2026-06-27 continuation: Media optimization job results now retain variant derivation/content-type lengths and attachment-id presence metadata instead of raw derivation labels, derived attachment UUIDs, or content-type strings in stored `job_queue.result` payloads.
  - 2026-06-27 continuation: Extraction job results now retain strategy length plus adapter metadata class/length/count/secret-candidate metadata instead of raw extraction strategy labels or full adapter metadata in stored `job_queue.result` payloads.
  - 2026-06-27 continuation: Concept tagging and reference extraction job/provenance summaries now retain label counts/lengths/secret-candidate metadata and extraction-method lengths instead of raw SKOS labels, tag paths, or method labels in retained `job_queue.result` and provenance metadata payloads.
  - 2026-06-27 continuation: Related-concept job ConceptInfo Debug output now reports id presence and notation/label/broader-notation lengths instead of raw concept UUIDs, notation strings, labels, broader notation, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: DB ColBERT token embedding Debug output now reports id presence, token-text length, embedding dimensions, and model length instead of raw token UUIDs, note/chunk UUIDs, token text, embedding vector values, model names, emails, DSNs, or token-shaped values.
  - 2026-06-27 continuation: DB note versioning Debug output now reports version id presence, note id presence, content/hash/actor/model lengths, timestamps, version numbers, and child counts instead of raw note/version UUIDs, original note content, version hashes, editor identifiers, model names, emails, DSNs, or token-shaped values.
  - 2026-06-27 continuation: DB syntactic CodeChunk Debug output now reports code unit names, languages, parent scopes, module paths, chunk text, and chunk metadata as lengths/counts instead of raw code symbols, module/path strings, chunk text, metadata key/value strings, emails, DSNs, or token-shaped values.
  - 2026-06-27 continuation: Core shard migration warning Debug output now reports field/default/detail text as lengths instead of raw shard field names, default values, migration detail strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: Core shard downgrade impact Debug output now reports shard/current versions, feature names, introduced versions, descriptions, component names, fields, summaries, and data-loss descriptions as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: Core shard upgrade guidance Debug output now reports from/to versions, step titles/descriptions/commands, feature names, and summaries as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: Core shard compatibility Debug output now reports manifest versions, migration versions, warning text, incompatibility reasons, and minimum-required versions as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: Core shard reserved-field validation error Display/Debug output now reports field names, removed versions, and removal reasons as lengths instead of raw shard/import strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: Core shard version parse and migration error/result Display/Debug output now reports version strings, migration failure text, and migrated JSON payloads as lengths/classes instead of raw import strings, emails, DSNs, paths, or token-shaped values.
  - 2026-06-27 continuation: PDF/OCR/spreadsheet adapter validation errors now report filename/error length metadata and stable reason codes instead of raw filenames or parser diagnostics.
  - 2026-06-27 continuation: Audio ffprobe duration parse failures now report stdout/error lengths and stable reason codes instead of raw ffprobe stdout or parser diagnostics.
  - 2026-06-27 continuation: Audio transcription adapter setup/read/backend errors now report path/model/MIME/error length metadata plus stable IO/reason codes instead of raw paths, labels, or backend diagnostics.
  - 2026-06-27 continuation: PDF text, PDF OCR, and Office conversion temp IO failures now report stable phase and IO-kind metadata instead of raw OS diagnostics, filesystem paths, or token-shaped temp details.
  - 2026-06-27 continuation: Video multimodal temp setup, duration parse, and local audio/frame read errors now report stable phase/path-length/error-length metadata and reason codes instead of raw OS diagnostics, paths, or parser text.
  - 2026-06-26 continuation: extraction setup job telemetry and failure text now report filename lengths, available-strategy counts, and stable reason codes instead of raw schema names, attachment UUIDs, filenames, storage paths, parser diagnostics, database/storage diagnostics, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: extraction adapter failure job/status text now stores stable reason codes instead of raw adapter/backend errors, DSNs, paths, hostnames, filenames, or token-shaped values.
  - 2026-06-26 continuation: media optimize setup job failure text now stores stable reason codes for schema, temp-dir, transaction, download, commit, and temp-file write failures instead of raw schema names, database/storage diagnostics, filesystem paths, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: backup status directory-creation failure responses now return stable reason-code status values instead of raw OS diagnostics, filesystem paths, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: backup trigger dry-run responses now report the generated backup filename only instead of echoing full backup destination paths that can contain filesystem paths, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: memory backup download headers now use memory-name length metadata in the generated filename instead of raw memory/archive names, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: knowledge-archive download headers now use backup-filename length metadata in the generated `.archive` filename instead of raw backup filenames, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: backup metadata get/update responses now return backup-filename length metadata and fixed fallback metadata titles instead of raw backup filenames, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: database and memory restore success messages now use fixed text instead of echoing memory names, backup filenames, filesystem paths, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: database and memory restore responses now return pre-restore and restored-from filename length metadata instead of raw backup filenames, filesystem paths, DSNs, hostnames, customer path segments, or token-shaped values.
  - 2026-06-26 continuation: generated backup upload and pre-restore metadata sidecars now store fixed generated text and source filename lengths instead of raw uploaded/restored-from filenames, filesystem paths, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: database snapshot/upload responses now return backup path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
  - 2026-06-26 continuation: backup status, latest-backup, list, and detail responses now return backup directory/path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
  - 2026-06-26 continuation: knowledge-archive upload responses now return extracted backup path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
  - 2026-06-26 continuation: knowledge-archive upload responses now return extracted backup filename length metadata instead of raw customer-controlled archive entry filenames that can contain DSNs, hostnames, customer path segments, or token-shaped values.
  - 2026-06-26 continuation: video multimodal adapter audio/keyframe/description broad logs now report filename lengths, error lengths, and stable reason classes instead of raw filenames or backend error text.
  - 2026-06-26 continuation: PDF text/OCR and Office conversion broad logs now report filename lengths, error lengths, and stable reason classes instead of raw filenames or extraction backend error text.
  - 2026-06-26 continuation: Spreadsheet adapter broad logs now report filename/sheet-name lengths, error lengths, and stable reason classes instead of raw filenames, sheet names, or backend error text.
  - 2026-06-26 continuation: Worker and sidecar lifecycle broad logs now report error lengths and stable reason classes instead of raw stale-job, model warmup/unload, or sidecar lifecycle backend error text.
  - 2026-06-26 continuation: Keyframe vision broad logs now report error lengths and stable reason classes instead of raw vision backend, transcript-context, fan-in, or queue backend error text.
  - 2026-06-26 continuation: View vision broad logs now report error lengths and stable reason classes instead of raw vision backend, fan-in, or queue backend error text.
  - 2026-06-26 continuation: Keyframe scene/view vision job telemetry and job failure text now report attachment/view presence, model/error/description lengths, job-id presence, and stable reason codes instead of raw schema names, attachment UUIDs, model names, database diagnostics, backend errors, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Keyframe/view assembly broad logs now report error lengths, stable reason classes, and note presence flags instead of raw note ids or note/update/manifest/downstream queue backend error text.
  - 2026-06-26 continuation: Keyframe/view assembly job telemetry and job failure text now report attachment/note presence, filename/base-name lengths, error lengths, and stable reason codes instead of raw schema names, attachment UUIDs, filenames, database diagnostics, backend errors, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Speaker diarization broad logs now report error lengths, stable reason classes, and attachment presence flags instead of raw transcode, metadata, caption-storage, or speaker-config backend error text and raw attachment IDs.
  - 2026-06-26 continuation: Speaker diarization job telemetry and job failure text now report attachment/note presence, model/error lengths, counts, and stable reason codes instead of raw schema names, attachment UUIDs, storage paths, database diagnostics, diarization backend errors, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Speaker relabel broad logs now report error lengths, stable reason classes, and attachment presence flags instead of raw caption-storage backend error text and raw attachment IDs.
  - 2026-06-26 continuation: Audio transcription broad logs now report error lengths, stable reason classes, filename lengths, and note presence flags instead of raw caption filenames, queue/transcode/chunk backend errors, or raw note IDs.
  - 2026-06-26 continuation: Audio transcription job telemetry and job failure text now report parent attachment presence, job-id presence, filename/error lengths, and stable reason codes instead of raw schema names, attachment UUIDs, temp paths, Whisper/backend errors, queue diagnostics, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Audio chunk fan-in broad logs now report error lengths and stable reason classes instead of raw fan-in count or merge backend error text.
  - 2026-06-26 continuation: Extraction result, faststart, and thumbnail broad logs now report attachment/note presence flags, error lengths, and stable reason classes instead of raw attachment IDs, thumbnail IDs, or backend error text.
  - 2026-06-26 continuation: Shard manifest Debug output now reports version/component/checksum/migration metadata lengths and counts instead of raw shard versions, component names, checksums, migration authors, or migration change text.
  - 2026-06-26 continuation: Extraction transcript attachment broad logs now report attachment/note presence flags, error lengths, and stable reason classes instead of raw parent attachment IDs or caption-storage backend error text.
  - 2026-06-26 continuation: Extraction derived-file metadata, AI-description, and commit broad logs now report child/parent attachment presence flags, stored counts, error lengths, and stable reason classes instead of raw child/parent attachment IDs or backend error text.
  - 2026-06-26 continuation: Extraction audio-transcription and diarization queue broad logs now report note/attachment presence flags, error lengths, and stable reason classes instead of raw note IDs, attachment IDs, or queue backend error text.
  - 2026-06-26 continuation: Extraction file-access, thumbnail sprite, and keyframe vision queue broad logs now report path lengths, note/attachment presence flags, error lengths, and stable reason classes instead of raw filesystem paths, note IDs, attachment IDs, or queue backend error text.
  - 2026-06-26 continuation: Extraction view vision queue broad logs now report note/attachment presence flags, view counts, error lengths, and stable reason classes instead of raw note IDs, attachment IDs, or queue backend error text.
  - 2026-06-26 continuation: Extraction content propagation and downstream requeue broad logs now report note presence, content lengths, job types, error lengths, and stable reason classes instead of raw note IDs or note/job backend error text.
  - 2026-06-26 continuation: Extraction checkpoint and failure-status broad logs now report attachment presence, completed counts, error lengths, and stable reason classes instead of raw attachment IDs or attachment-status backend error text; the targeted extraction-handler raw ID/error scan is now empty.
  - 2026-06-26 continuation: Audio utility duration-probe fallback broad logs now report error lengths and stable reason classes instead of raw probe backend error text; the `matric-jobs` raw telemetry scan is now down to the persisted pause-state parse warning.
  - 2026-06-26 continuation: Pause-state parse fallback broad logs now report error lengths and stable JSON reason classes instead of raw persisted-state parser text; the targeted `matric-jobs` raw telemetry scan is now empty.
  - 2026-06-26 continuation: Generic job failure ordinary logs now report retry state, duration, error length, and stable reason class instead of raw handler failure text, while persisted job failure state remains unchanged.
  - 2026-06-26 continuation: Inbound connector telemetry length fields now cap user/backend-originated diagnostic text at 512 characters and retain metadata-only output for CR/LF log-injection text, credential-bearing DSNs, hostnames, and token-shaped values.
  - 2026-06-26 continuation: Backup archive-list Debug output now reports directory, filename, path, metadata, checksum, and manifest presence/length metadata instead of raw backup directories, filenames, paths, sidecar text, hashes, or embedded manifest details.
  - 2026-06-26 continuation: Related-notes Debug output now reports snippet, title, tag, source, and LLM context-summary lengths/counts instead of raw related-note content or generated summaries.
  - 2026-06-26 continuation: Call-detail Debug output now reports provider/call-id/remote-party/metadata/transcript segment lengths, classes, counts, and presence instead of raw provider identifiers, phone-like values, metadata JSON, or transcript text.
  - 2026-06-26 continuation: Backup JSON export Debug output now reports manifest string lengths and exported notes/collections/tags/templates counts instead of raw exported records, tag names, collection names, template bodies, or manifest strings.
  - 2026-06-26 continuation: Backup JSON import Debug output now reports manifest presence, imported record counts, note field lengths, and error counts instead of raw imported note content, tags, collection/template JSON, manifest JSON, status text, or import diagnostics.
  - 2026-06-26 continuation: Backup trigger/status Debug output now reports destination counts, status/output/path/filename/size-label lengths, and latest-backup presence instead of raw backup destinations, command output strings, backup directories, filenames, disk labels, or status strings.
  - 2026-06-26 continuation: Shard import Debug output now reports shard payload length, include length, manifest presence, import counts, error counts, and warning counts instead of raw base64 shard payloads, component filters, manifest details, status text, import errors, or warnings.
  - 2026-06-26 continuation: Backup swap Debug output now reports filename, strategy, status, and message lengths plus import stats instead of raw backup filenames, restore strategies, status text, or restore messages.
  - 2026-06-26 continuation: Database restore Debug output now reports filename, memory, message, prerestore-backup, and restored-from lengths instead of raw backup filenames, memory names, restore messages, prerestore filenames, restored-from paths, DSNs, or hostnames.
  - 2026-06-26 continuation: Database backup upload Debug output now reports backup payload, original filename, title, and description lengths instead of raw uploaded backup data, filenames, titles, descriptions, DSNs, paths, or token-shaped values.
  - 2026-06-26 continuation: Database snapshot request Debug output now reports snapshot name, title, and description lengths instead of raw operator-supplied backup names, titles, descriptions, DSNs, paths, or token-shaped values.
  - 2026-06-26 continuation: Backup metadata update request Debug output now reports title and description lengths instead of raw operator-supplied backup metadata, DSNs, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Memory info Debug output now reports embedding-set counts, field lengths, storage sizes, and recommendation note lengths instead of raw embedding names, slugs, descriptions, model identifiers, index labels, size labels, recommendation notes, paths, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Memories overview Debug output now reports memory counts, storage byte totals, and size-label/name lengths instead of raw memory/archive names, human storage labels, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Attachment upload Debug output now reports document-type presence, option values, and filename/content-type/payload/vision-mode lengths instead of raw filenames, content types, base64 payloads, media/vision option strings, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Create-note request Debug output now reports content/title/source/format/document-type/model/pipeline/tag lengths, counts, chunking values, and metadata presence instead of raw note content, titles, tags, metadata JSON, model identifiers, source strings, document-type slugs, pipeline names, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Bulk create-note Debug output now reports per-item content/title/source/format/document-type/tag/revision metadata lengths, counts, chunking values, and metadata presence instead of raw note content, titles, tags, metadata JSON, source strings, document-type slugs, revision modes, formats, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Update-note request Debug output now reports content/revision-mode/model/tag lengths, counts, chunking values, booleans, and metadata presence instead of raw note content, tags, metadata JSON, model identifiers, revision modes, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Restore-note query Debug output now reports revision-mode length instead of raw restore revision-mode strings, DSNs, hostnames, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: OAuth authorization request/form Debug output now reports response-type, client-id, redirect-uri, scope, state, PKCE, method, and action lengths instead of raw client IDs, redirect URLs, scopes, state values, PKCE challenges, emails, DSNs, paths, or token-shaped values.
  - 2026-06-26 continuation: job create/list Debug output now reports note-id presence, job/status/filter lengths, payload class/length, and secret-candidate flags instead of raw job payload JSON, note IDs, filters, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Job API response Debug output now reports job/note id presence, payload/result classes and lengths, and progress/error lengths instead of raw job IDs, payload/result JSON, progress messages, paths, DSNs, URLs, or generated text.
  - 2026-06-26 continuation: ProblemDetails Debug output now reports type/title/detail/instance/request-id lengths and attachment-id presence instead of raw problem details, instance paths, request ids, DSNs, paths, URLs, or token-shaped values.
  - 2026-06-26 continuation: ApiError Debug output now reports variant metadata and detail/path lengths instead of raw error details, operation diagnostics, provider diagnostics, blob paths, attachment ids, DSNs, filesystem paths, URLs, or token-shaped values.
  - 2026-06-26 continuation: Startup storage path telemetry now reports file-storage and TUS staging path lengths plus error lengths instead of raw filesystem paths or OS diagnostics in startup logs.
  - 2026-06-26 continuation: Startup issuer/auth telemetry now reports issuer URL class/length and fallback host length instead of raw issuer URLs or bind-host values in auth-required startup logs.
  - 2026-06-26 continuation: Startup model/bind telemetry now reports model identifier lengths and bind-host length/port instead of raw backend model names or server bind addresses in startup logs.
  - 2026-06-26 continuation: Memory search query Debug output now reports coordinate/radius/time-bound presence instead of raw latitude, longitude, radius, or temporal bounds in spatial/temporal search diagnostics.
  - 2026-06-26 continuation: Call detail Debug output now reports call/archive ID presence plus metadata lengths/classes instead of raw call IDs, archive IDs, provider call identifiers, remote parties, transcript text, or recording metadata.
  - 2026-06-26 continuation: export, version, shard, attachment, and TUS query Debug output now reports selector/filter/include/filename/content-type/variant/vision-mode lengths and UUID presence instead of raw operator strings, filenames, MIME filters, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: related-concept LLM pair Debug output now reports concept string lengths and confidence instead of raw model-generated concept labels, emails, DSNs, paths, or token-shaped values.
  - 2026-06-26 continuation: model discovery response Debug output now reports model/provider/default counts and field lengths instead of raw model slugs, provider identifiers, deployment health strings, model families, or default model names.
  - 2026-06-26 continuation: Reprocess request Debug output now reports revision-mode/model/step lengths, step counts, note-id counts, limits, and chunking values instead of raw pipeline step names, model identifiers, revision modes, note IDs, DSNs, hostnames, or token-shaped values.
  - 2026-06-26 continuation: Tag mutation Debug output now reports tag counts/lengths, concept-id presence, and primary flags instead of raw tag paths, email-like tag values, token-shaped tag values, or concept IDs.
  - 2026-06-26 continuation: SKOS relation mutation Debug output now reports target concept-id presence instead of raw relation target UUIDs.
  - 2026-06-26 continuation: Collection mutation Debug output now reports collection name/description lengths and parent/collection-id presence instead of raw collection names, descriptions, parent IDs, collection IDs, paths, emails, DSNs, or token-shaped values.
  - 2026-06-26 continuation: Collection and SKOS collection list-query Debug output now reports parent/scheme id presence instead of raw parent collection or taxonomy scheme UUIDs.
  - 2026-06-26 continuation: SKOS query Debug output now reports search/autocomplete/status/facet lengths and scheme-id presence instead of raw search strings, taxonomy filter strings, scheme IDs, DSNs, hostnames, emails, or token-shaped values.
  - 2026-06-26 continuation: Graph diagnostics/maintenance Debug output now reports edge-filter, snapshot-label, and maintenance-step lengths/counts plus numeric controls instead of raw filter strings, labels, step names, DSNs, hostnames, emails, paths, or token-shaped values.
  - 2026-06-26 continuation: Template request Debug output now reports template name/description/content/format/tag/variable lengths, counts, collection-id presence, and revision-mode length instead of raw template content, variables, tags, names, descriptions, collection IDs, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Note query Debug output now reports timeline/activity/health/access/list filter lengths, counts, numeric controls, date-filter presence, and collection-id presence instead of raw query filters, tag filters, sort strings, relative-time strings, collection IDs, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Note timeline/activity response Debug output now reports note-id counts, title lengths, timestamps, and recency flags instead of raw note IDs, note titles, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Note links response Debug output now reports link counts, URL/snippet/metadata counts, and metadata secret-candidate counts instead of raw link IDs, note IDs, URLs, relation kinds, snippets, metadata JSON, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Archive management request Debug output now reports archive name/new-name and description lengths instead of raw memory names, clone names, descriptions, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Archive stats response Debug output now reports archive/schema-name lengths and numeric stats instead of raw memory names, schema names, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Document-type request Debug output now reports category, filename, content-sample, and MIME-type lengths instead of raw category filters, filenames, content samples, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Twilio realtime wire Debug output now reports media-stream sequence, call SID, payload, timestamp, chunk, mark, DTMF, webhook phone/recording/consent/disclosure field lengths instead of raw provider IDs, phone numbers, recording URLs, media payloads, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Deepgram realtime wire Debug output now reports event type, transcript, provider description/message lengths, channel presence, alternative/word counts, and word timing/speaker presence instead of raw transcripts, provider messages, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Deepgram provider config Debug output now reports API-key presence/length plus listen URL, model, language, and encoding lengths instead of raw API keys, provider URLs, model/profile identifiers, emails, or token-shaped values.
  - 2026-06-26 continuation: SSE EventSource query Debug output now reports token presence/length plus memory, event-type filter, and entity-id lengths instead of raw bearer/API-key tokens, memory names, event filters, entity IDs, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Hosted telemetry classification now inventories localized audit/telemetry stores (`inference_config_audit`, `file_upload_audit`, `skos_audit_log`, `note_access_log`, non-inbound `event_outbox`, and `inbound_dlq`) with owner, access boundary, retention/DSAR handling, safe broad telemetry shape, and `fortemi.audit` mapping.
  - 2026-06-26 continuation: Hosted secret inventory contract now defines secret classes, storage rules, response metadata, lifecycle requirements, surface inventory, and cross-issue ownership, with links from telemetry, API error, operator, and DSAR docs.
  - 2026-06-26 continuation: Webhook Debug redaction slice replaces derived Debug on outbound webhook configs/deliveries plus webhook create/receiver requests with metadata-only output covering retrievable signing secrets, HMAC secrets, credential-bearing URLs, delivery payloads, response bodies, and schema docs.
  - 2026-06-26 continuation: Incoming webhook receiver/validation Debug slice replaces raw stored receiver, update, validate-request, and validation-response output with metadata-only lengths, JSON classes, counts, and presence flags.
  - 2026-06-26 continuation: Webhook delivery diagnostic-read slice changes the delivery-history API from raw retained payload/response records to metadata-only output with JSON class, lengths, status, and secret-candidate flags plus route-level regression coverage.
  - 2026-06-26 continuation: Webhook management response slice replaces list/get/update raw webhook records with metadata-only output carrying URL class/length, URL secret-candidate flag, `secret_set`, event count, status, timestamps, and retry state.
  - 2026-06-26 continuation: Incoming webhook receiver management response slice replaces list/get/update raw receiver records with metadata-only output carrying slug/provider/schema/header lengths/classes, `secret_set`, schema-doc class/length, and secret-candidate flags.
  - 2026-06-26 continuation: Incoming webhook accepted-response slice changes signed receiver acknowledgements and idempotency replay bodies from raw slug/provider/schema/payload/side-effect echoes to metadata-only output with lengths/classes, secret-candidate flags, and idempotency flags.
  - 2026-06-26 continuation: Inference completion/provider DTO Debug slice replaces raw chat message, completion response, provider, and providers-list output with metadata-only lengths, classes, counts, and booleans.
  - 2026-06-26 continuation: Audio/vision ad-hoc response Debug output now reports transcript/description/model lengths and segment counts instead of raw generated text, transcript segments, speaker labels, word text, or model identifiers.
  - 2026-06-26 continuation: Effective inference config response Debug slice replaces raw sourced base URLs, model names, referers, app names, API-key sentinels, embedding backends, and provider ids with metadata-only lengths/classes/counts.
  - 2026-06-26 continuation: Inference config audit/readback slice replaces API-key prefix masks with metadata-only length sentinels and defensively sanitizes audit JSON on diagnostic reads.
  - 2026-06-26 continuation: Inference config audit Debug slice replaces raw localized audit rows and audit-query filters with metadata-only actor/action lengths, JSON classes/lengths, source-IP classes, and limits.
  - 2026-06-26 continuation: Inference test-connection response Debug slice replaces raw detected providers, Ollama versions, model names, provider error text, and recovery suggestions with metadata-only lengths/counts/booleans.
  - 2026-06-26 continuation: Inference config update/reset response Debug slice replaces raw previous/current/effective config JSON, status strings, and warning text with metadata-only JSON classes, lengths, and warning counts.
  - 2026-06-26 continuation: API-key management list slice replaces raw key prefixes, labels, descriptions, and scopes with metadata-only lengths while preserving one-time create display.
  - 2026-06-26 continuation: OAuth client readback Debug slice replaces derived output on post-creation client records with metadata-only lengths/counts for ids, labels, URLs, contacts, software fields, and scopes.
  - 2026-06-26 continuation: Inbound connector config Debug slice replaces raw SSE/Redis/Kafka config output with metadata-only destination classes, lengths, counts, and presence flags for credential-bearing URLs, headers, SASL credentials, topics, and extra config.
  - 2026-06-26 continuation: Inbound source management list slice replaces raw connector names, kinds, and config JSON with metadata-only lengths/classes, secret-candidate flags, and key counts.
  - 2026-06-26 continuation: Inbound source model Debug slice replaces raw stored source and create-request output with metadata-only name/kind lengths, config classes, config lengths, and config key counts.
  - 2026-06-26 continuation: Chat DTO and stream replay Debug slice replaces raw prompts, context, history, generated messages, model metadata, action payloads, SSE frame data, replay frame data, and session ids with metadata-only lengths/classes/counts.
  - 2026-06-26 continuation: Chat resume cursor and model-list Debug slice replaces raw Last-Event-ID session ids, default model ids, and installed model/profile identifiers with metadata-only lengths/counts.
  - 2026-06-26 continuation: Chat stream-store warning slice replaces raw Redis/serialization diagnostics with stable operation names, reason codes, and error lengths for connect/open/serialize/RPUSH/LRANGE failures.
  - 2026-06-26 continuation: Idempotency, ingest cursor, and ingest token Redis-store warning slice replaces raw Redis/serde diagnostics, replay keys, stream ids, token ids, and bearer-token-shaped values with stable operation names, reason codes, key lengths, and error lengths.
  - 2026-06-26 continuation: Whisper preload warning slice replaces raw startup model-preload diagnostics with stable reason codes and error lengths for timeout/connect/status/invalid-response/model-unavailable failures.
  - 2026-06-26 continuation: Backup metadata and memory-restore warning slice replaces raw metadata, schema-sync, and restore-recovery diagnostics with stable operation/phase/reason metadata, error lengths, and memory-name lengths.
  - 2026-06-26 continuation: CORS and archive-routing warning slice replaces raw invalid-origin, archive-name, and archive schema-sync diagnostics with URL classes, bounded lengths, stable reason codes, and error lengths.
  - 2026-06-26 continuation: Search-cache Redis telemetry slice replaces raw Redis URLs, backend errors, and query-derived cache keys with URL classes, bounded lengths, stable reason codes, and key lengths.
  - 2026-06-26 continuation: Search query Debug output now reports query/filter/mode/embedding-set/since/tag/strict-filter lengths and date-filter presence instead of raw query text, filter JSON, embedding-set names, tags, DSNs, paths, emails, or token-shaped values.
  - 2026-06-26 continuation: Ingest-token data Debug slice replaces raw token revocation ids and archive schema names with metadata-only lengths while preserving JSON persistence.
  - 2026-06-26 continuation: Idempotency replay-record Debug slice replaces raw body hashes and cached webhook response bodies with body-hash length, response status, JSON class, and response length while preserving replay persistence.
  - 2026-06-26 continuation: Reconstruction response Debug slice replaces raw reconstructed note content, document titles, tags, and chunk titles with metadata-only lengths/counts while preserving API serialization.
  - 2026-06-26 continuation: Speaker relabel config Debug slice replaces raw speaker ids, user-assigned names, roles, and transcript-derived labels with metadata-only lengths/counts while preserving config serialization/rendering.
  - 2026-06-26 continuation: Search response Debug slice replaces raw user queries and search-hit content with result counts, query length, and total while preserving API/cache serialization.
  - 2026-06-26 continuation: Generic list response Debug output now reports item counts and pagination only instead of recursively formatting raw list items.
  - 2026-06-26 continuation: Federated search Debug slice replaces raw query text, memory names, snippets, titles, and tags with metadata-only lengths/counts while preserving request/response serialization.
  - 2026-06-26 continuation: Backup metadata Debug slice replaces raw sidecar titles, descriptions, source strings, extra metadata, version strings, PostgreSQL version text, and migration names with metadata-only lengths/counts while preserving sidecar/API serialization.
  - 2026-06-26 continuation: Database backup response Debug slice replaces raw filenames, local paths, metadata filenames, titles, and descriptions with metadata-only lengths/presence while preserving API serialization.
- [x] **#926 / #928 / #933** fail-closed startup: require issuer in multi-tenant; reject invalid security booleans; validate rate-limit env before constructing limiter. (2026-06-26: startup-config slice added strict parsing for auth/tenant/local-issuer/rate-limit/realtime-consent booleans, hosted-safe issuer validation reused by AsyncAPI metadata, deterministic process-local rate-limit numeric validation before limiter construction, and operator/env docs for accepted forms, local issuer override, and hosted issuer requirements. Issues closed after focused regression tests and docs audit.)

## Phase 1 — Open BSL desktop GA  `tier/open-build`

**Gating:** Phase 0 contracts (partial — error shape, config validation). **Parallel to Phase 2.**
Single-user desktop product; no multi-tenancy or advanced OAuth.

- [ ] **#884** finish ADR-094 sidecar AuthContext + middleware coverage; basic MCP any-client on the user's own data via local/API-key auth.
- [ ] **#950** webhook receive/validate hardening (per-route body cap, validate-public decision, 401/404 uniformity).
- [ ] **#994 / #970 / #922** attachment & upload hardening (pre-validation buffering bounds; malware/scan gate + quarantine; TUS finalization).
- [ ] **#995 / #976 / #979 / #929 / #975 / #885** embeddings & search correctness (effective-embedding fingerprint; all-or-nothing `embed_texts()`; transaction rollback; query-provider resolver; cache lineage; semantic filter parity).
- [ ] **#971 / #909 / #931** jobs: delayed-retry/backoff + poison quarantine; readiness probe + graceful SIGTERM; fail-closed on unknown job type/status.
- [ ] **#927 / #980** backup/restore: operator-local restore (break-glass), strict `backup.conf` parser, restore drills, verified-artifact-only.
- [ ] **#989 / #937 / #992 / #990 / #973 / #982** Docker bundle local-first (127.0.0.1 default, generated DB secret, rendered-config preflight, image pinning, autoheal behind `ops-autoheal` profile, no default socket mount).
- [ ] **#997 / #1002** observability: hosted-safe logging defaults + LOG_FORMAT contract.
- [~] **#1004 → #999 / #1001 / #998 / #1000** docs-contract runner (advisory+baseline → blocking; redacted output; portable, CI-owned) + leakage guard. (2026-06-26: portable advisory runner and committed fingerprint-only baseline landed for the 202 current #999/#1001 findings. Public docs placeholder cleanup removed all docs-contract findings from `docs/content/api.md`, `docs/content/authentication.md`, `docs/content/real-time-events.md`, and `docs/content/mcp-deployment.md`, shrinking the baseline to 109 known findings. Public/operator DB docs cleanup replaced reusable Postgres DSNs and `PGPASSWORD=matric` guidance in docs with placeholders, shrinking the baseline to 76 known findings. Public provider/token docs cleanup replaced remaining provider-key and bearer-token examples in inference, memory-search, operator, release, job, and MCP troubleshooting docs with canonical placeholders, shrinking the baseline to 36 known findings. Remaining CI/test/local-dev findings were removed in follow-up #968/#974 cleanup slices; the hosted-strict baseline is now empty and blocking mode passes.)

## Phase 2 — Licensed-server / hosted multi-tenant GA  `tier/licensed-server`  (milestone #62)

**Gating:** Phase 0. **Parallel to Phase 1.** This is the hosted/enterprise product.

- [ ] **Tenant isolation / RLS** — ADR-090 shared-schema RLS: #726 / #728 / #729 / #733. *(The isolation floor for everything below.)*
- [ ] **Object-level authorization** #956–#963 — note CRUD/status/versions/sharing, attachments, collections/templates/export, provenance, SKOS governance, memory admin/federated search, document-type use, ad-hoc AI gating.
- [ ] **Control-plane admin-gating** #945–#964 (incl. #946 inference config, #954 queue control, #955 graph/embedding control, #949 webhook mgmt, #978 backup inventory).
- [ ] **Advanced OAuth** (decided 2026-06-23): #1003 capabilities/profile source-of-truth → #943 (P0) consent/redirect → #972/#944 CIMD-first + gated DCR → #941 PKCE/S256 + client-auth → #917 resource/audience (API+MCP) → #924 scope cleanup → #1005 same-client introspection/revocation.
- [ ] **KMS** #897 (P0) AWS launch contract + #910; #911/#912 (Vault/GCP) deferred. **PKE** #947/#948.
- [ ] **Privacy/DSAR** #900 / #969 / #892 / #961 (retention); **inbound connectors** #988 / #920 / #968; **realtime/Twilio** #952 / #981 / #986 / #951 / #953.

## Phase 3 — Universal Model Gateway / Bridge  (milestones #60 foundation, #61 expansion)

**Gating:** Phase 0; benefits from Phase 2 auth/metering.

- [ ] **#873** canonical protocol-adapter framework → **#864/#865** chat (non-stream/stream) → **#866** `/v1/models` (strict OpenAI shape) → **#867** typed `RoutePlan`/`BridgeResolvedModel`.
- [ ] Metering/cost #877/#878/#879/#880; per-consumer policy #870/#871; session logging #868.
- [ ] Provider/cache/privacy #985 (strip external router cache; retention=eligibility; coarse classes) / #969.
- [ ] Provider expansion (#61, blocked on foundation): #874 Anthropic, #875 Gemini, #876 vLLM/LiteLLM/Azure/Bedrock, #869 embeddings.

## Phase 4 — Referenced (BYO) storage  (milestones #58 follow-up, #59 v2 deferred)

**Gating:** design P0s first. Large workstream (~70 issues).

- [ ] **Design P0s** #890 (root-handle vs canonical-path) / #902 (error contract) / #903 (scanner ignore/secret registry) / #904 (derived layout/symlink-safe cleanup) / #905 (clone/export/import/backup semantics).
- [ ] **Core impl** backend/source (#748/#749/#751), registry/migration (#752/#753/#754/#755), scan walker + hash + ingest (#757/#758/#760/#761/#762/#763), quarantine (#759/#765/#776), API (#771/#773/#774/#775), MCP (#777/#778/#779).
- [ ] **Security regression suite** — epic #746, #780/#781/#782/#783, CI gate #797.
- [ ] **Docs/ops** — epic #747, #784–#789, #798–#802.

## Phase 5 — Streaming realtime (#63) + Native distribution (#64)

**Gating:** can proceed alongside; distribution gates final GA packaging.

- [ ] Streaming: #906 (cost/default mode) / #915 / #939 (outbox backpressure + payload minimization) / #896 (ADR refresh).
- [ ] Native distribution: #64 packaging/service lifecycle/CI publish; supply-chain #916/#888/#887/#886; licensing notices #901/#894.

---

## Immediate next actions (decisions + implementation patterns ready)

1. **#1005** — same-client introspection/revocation guard (P1; lands before #917). Plan in comment 71419.
2. **#988** — legacy enabled-row quarantine (first inbound guard PR). Plan in comment 71424.
3. **#967** — RFC 9457 migration (centralized `ProblemType` registry). Pattern in comment 71652.
4. **#710** — stand up the route/action inventory (unblocks Phases 0/1/2).

## Critical-path P0 blockers to schedule

#943 (OAuth consent/redirect) · #897 (AWS KMS contract) · #926 (issuer required) · Referenced-storage design set #890/#902/#903/#904/#905 · #797 (Referenced storage CI gate).

---

## Product decisions captured 2026-06-23 (index → owning issue)

All recorded as "Operator product decision" comments on-issue. Keystones: **#853** (BSL-desktop vs licensed-server boundary), **#967** (RFC 9457).

| Area | Decision | Issue |
|---|---|---|
| **Architecture** | Advanced auth + multi-tenant hosting = licensed-server only, separable from BSL desktop build | #853 |
| Public docs | Minimal curated consumer docs; no public generated schema; OAuth excluded; operator-only schema path | #965 |
| Docs leakage guard | Re-scoped from "public projection" to "no-leakage + operator-gating"; Swagger operator-only, try-it-out off | #1000 |
| Error contract | Adopt RFC 9457 (clean pre-GA break) + centralized registry pattern | #967 |
| Rate limit | No legacy `X-RateLimit-*`; standard `RateLimit`/`Retry-After` only | #898 |
| OAuth tiering | Any client basic MCP; advanced auth licensed-server-only; PKCE/S256, no `plain` | #941 |
| OAuth resource | RFC 8707 resource/audience for API+MCP together | #917 |
| OAuth registration | CIMD-first + gated/deprecated DCR fallback | #972 |
| OAuth scope | Remove `delete` until #710 defines it; one canonical scope source; subset enforcement | #924 |
| OAuth introspect/revoke | Same-client only + separate privileged operator path | #1005 |
| OAuth profiles | Single `OAuthCapabilities` source of truth drives discovery/metadata/fixtures | #1003 |
| Backup restore | Hosted = operator-local break-glass only; verified Fortemi-produced artifacts only | #927 / #978 |
| Backup config | Strict key/value parser (no shell `source`); periodic restore drills as gate | #980 |
| Docker bundle | Local-first default (127.0.0.1, generated secret); rendered-`compose config` preflight | #989 |
| Docker autoheal | Behind explicit `ops-autoheal` profile; no default socket mount | #937 |
| Bridge models | Strict OpenAI `/v1/models`; omit unverifiable models for strict tenants; strict slug resolver | #866 |
| Bridge routing | Typed `RoutePlan`/`BridgeResolvedModel` before any bridge route | #867 |
| Provider cache | Strip external router cache controls; retention = model eligibility; coarse classes to clients | #985 |
| Embeddings | One persisted effective-embedding fingerprint across jobs/query/cache/restore | #995 |
| Embedding validation | Cardinality/order/dimension in `EmbeddingBackend` trait + defensive job-level | #976 |
| Search degrade | FTS fallback with explicit degraded flag + audit (never silent) | #929 |
| MCP legacy SSE | Disabled by default; pin to 2025-11-25 Streamable HTTP | #940 |
| MCP test harness | In-process app/session-manager factory in main `npm test` | #899 |
| MCP tool output | Structured metadata in hosted (no live-token curl); scanner-safe placeholders local | #987 |
| Streaming auth | Distinct short-lived `<STREAM_TOKEN>` class; no ordinary tokens in query | #953 |
| DSAR receipts | Metadata + TTL only; never recoverable cached payload | #900 |
| Tollbooth | Generic OpenAI-compatible proxy docs; Tollbooth as one example only | #983 |
| Docs placeholders | Scanner-safe placeholders enforced everywhere incl MCP docs + generated examples | #999 |
| Default creds | None as guidance; only local-dev/test fixtures allowlisted | #1001 |
| Docs-contract | Advisory+baseline → blocking; redacted output; portable; CI-owned | #1004 |

## References
- Memory: `bsl-desktop-vs-licensed-server-boundary`, `fortemi-rfc9457-error-contract`.
- Milestones: #60 Bridge foundation · #61 Bridge expansion (blocked) · #62 Hosted auth & multi-tenancy launch gate · #63 Streaming realtime phase 1 · #64 Native server distribution · #58 Referenced storage follow-up · #59 Referenced storage v2 (deferred).
- Audit history: `.aiwg/working/new-agent-handoff-prompt-2026-06-23.md`, `.aiwg/working/issue-audit-agent-handoff-2026-06-23.md`.

## Progress log

- 2026-06-25 — Roadmap handoff clarified: `.aiwg/planning/roadmap.md` is the main operating project plan for future agents; `fortemi-roadmap-skill` is the resume/advance procedure.
- 2026-06-25 — #710 route/action inventory slice landed: `matric-api` now has an executable route policy inventory, coverage tests against registered Axum routes, and auth helper delegation for public routes plus credential-management admin gating.
- 2026-06-25 — #710 core policy contract slice landed: `matric-core` now exports `AuthorizationPolicy`, action/resource/decision/obligation types, `AllowAllPolicy`, and a starter `RoleBasedPolicy` proving MCP transport scope does not imply REST write.
- 2026-06-25 — #710 policy-input handoff slice landed: route policy rows now build `Action`/`Resource`/`AuthzContext` inputs and auth middleware attaches them to protected request extensions without enforcing hosted policy yet.
- 2026-06-25 — #710 authorize middleware slice landed: API stack now invokes `AuthorizationPolicy` from protected request extensions with `AllowAllPolicy` preserving current behavior and deny-path coverage proving fail-closed 403 handling.
- 2026-06-25 — #710 hosted policy selection slice landed: `FORTEMI_MULTI_TENANT=true` now selects `RoleBasedPolicy`, personal mode keeps `AllowAllPolicy`, and representative REST mutation allow/deny tests exercise the real policy bridge.
- 2026-06-25 — #710 admin/high-risk policy coverage slice landed: credential-management and backup-restore routes now have real `RoleBasedPolicy` allow/deny tests, and the remaining handler-local admin helper is documented as transitional defense in depth delegated to route inventory.
- 2026-06-25 — #710 realtime transport scope slice landed: realtime transport policy inputs now require MCP transport scope, with `RoleBasedPolicy` tests proving read scope is denied and `mcp` scope is allowed.
- 2026-06-25 — #710 policy performance target slice landed: documented the in-process `RoleBasedPolicy` average evaluation budget and added a regression test to catch high-latency policy work.
- 2026-06-25 — #967 RFC 9457 boundary slice landed: `ApiError` now emits `application/problem+json` with a Fortemi problem-type registry and redacts raw internal/database/blob storage diagnostics from client bodies.
- 2026-06-25 — #710 auth-exemption consistency slice landed: route-policy public checks now keep system-health and realtime provider callbacks public while preserving method-aware inline-proof webhook exemption.
- 2026-06-25 — #967 request-id slice landed: problem responses now include the propagated `x-request-id` as the RFC 9457 `request_id` extension member, with CORS exposure for browser clients.
- 2026-06-25 — #967 OAuth server-error slice landed: `OAuthApiError` database/internal `server_error` responses now use generic RFC 9457 problem details instead of exposing raw OAuth/database diagnostics.
- 2026-06-25 — #967 auth/rate-limit middleware slice landed: central auth, authorization-policy, admin-scope, and rate-limit middleware failures now return RFC 9457 problem details without serializing scope strings or policy reasons.
- 2026-06-25 — #710 resource-normalization metadata slice landed: route-policy inputs now mark route-param IDs as unnormalized candidates and flag object/admin/high-risk resources that still require backing-store normalization before final object-policy enforcement.
- 2026-06-25 — #910 core audit baseline slice landed: `matric-core` now exposes `AuditEvent`, `AuditSink`, `TracingSink`, bounded `AuditBuffer`, failure policy and redaction tests, plus `matric-api` emits a low-risk process startup audit event.
- 2026-06-25 — #710 authorization-audit slice landed: authorization allow/deny/error decisions now emit sanitized `fortemi.audit` events carrying policy id/version, action/resource metadata, and deny reason codes without scope strings or raw policy errors.
- 2026-06-25 — #710 hosted resource-normalization guard slice landed: hosted `RoleBasedPolicy` now denies unnormalized route-param resources before policy evaluation and audits `InvalidResource`, while personal `AllowAllPolicy` remains compatible.
- 2026-06-25 — #710 note resource-normalization slice landed: note route UUIDs now run a backing-store existence lookup before hosted policy evaluation and clear `resource_id_normalized` only for existing notes; missing, invalid, or lookup-error paths keep the hosted fail-closed guard.
- 2026-06-25 — #710 attachment resource-normalization slice landed: attachment route UUIDs now resolve through file storage, attach parent note metadata, and clear `resource_id_normalized` only for existing attachments; missing, invalid, lookup-error, and note-scoped attachment routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 archive resource-normalization slice landed: archive and memory route names now resolve through the archive registry, attach archive id/schema/default metadata, and clear `resource_id_normalized` only for existing archives; missing, lookup-error, and collection routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 document-type resource-normalization slice landed: document-type route names now resolve through the catalog, attach id/category/system/active/attachment metadata, and clear `resource_id_normalized` only for existing document types; missing, lookup-error, and collection routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 job resource-normalization slice landed: job route UUIDs now resolve through the job queue, attach job type/status/parent-note/cost-tier metadata, and clear `resource_id_normalized` only for existing jobs; missing, invalid, lookup-error, and collection routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 webhook-control resource-normalization slice landed: outbound webhook route UUIDs now resolve through the webhook repository, attach safe state/retry metadata, and clear `resource_id_normalized` only for existing webhooks; missing, invalid, lookup-error, collection, and incoming receiver routes keep the hosted fail-closed/control-boundary behavior.
- 2026-06-25 — #710 inbound-source resource-normalization slice landed: inbound connector route names now resolve through the inbound source registry, attach safe source id/name/kind/enabled metadata, and clear `resource_id_normalized` only for existing sources; missing, lookup-error, collection, and unrelated webhook-control routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 PKE keyset resource-normalization slice landed: PKE keyset route `name_or_id` values now resolve through the keyset repository, attach safe id/name/address/label metadata, and clear `resource_id_normalized` only for existing keysets; missing, lookup-error, collection, and unrelated inbound routes keep the hosted fail-closed guard.
- 2026-06-25 — #710 collection resource-normalization slice landed: collection route UUIDs now resolve through the collection repository, attach safe id/name/parent/note-count metadata, and clear `resource_id_normalized` only for existing collections; missing, invalid, lookup-error, root, and note-move routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 template resource-normalization slice landed: template route UUIDs now resolve through the template repository, attach safe id/name/format/default-tag-count/collection metadata, and clear `resource_id_normalized` only for existing templates; missing, invalid, lookup-error, collection, and unrelated document-type routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 credential-management resource-normalization slice landed: API-key route UUIDs now resolve through the OAuth repository, attach safe id/prefix/name/scope/active/expiration/rate-limit metadata, and clear `resource_id_normalized` only for existing API keys; missing, invalid, lookup-error, collection, and unrelated PKE routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 taxonomy concept resource-normalization slice landed: SKOS concept route UUIDs now resolve through the SKOS repository, attach safe id/primary-scheme/status/count metadata, and clear `resource_id_normalized` only for existing concepts; missing, invalid, lookup-error, collection, scheme, and secondary-target routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 taxonomy scheme resource-normalization slice landed: SKOS scheme route UUIDs now resolve through the SKOS repository, attach safe id/notation/version/activity/system/date-presence metadata, and clear `resource_id_normalized` only for existing schemes; missing, invalid, lookup-error, collection, and concept routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 taxonomy collection resource-normalization slice landed: SKOS collection route UUIDs now resolve through the SKOS repository, attach safe id/scheme/ordered/date-presence metadata, and clear `resource_id_normalized` only for existing collections; missing, invalid, lookup-error, root, concept, and scheme routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 taxonomy relation resource-normalization slice landed: SKOS relation delete routes now verify both source and target concepts, attach safe endpoint id/scheme/relation-type metadata, and clear `resource_id_normalized` only when both endpoints exist; missing, invalid, lookup-error, and relation collection routes keep the hosted fail-closed or lookup-free boundary.
- 2026-06-25 — #710 incoming webhook receiver method-boundary slice landed: route policy inputs now keep signed receiver `POST` calls public inline-proof while classifying GET/PATCH/DELETE management calls on the same slug route as admin `webhook_control` actions, matching the auth-exemption boundary.
- 2026-06-25 — #710 credential-management admin-helper retirement slice landed: auth middleware no longer runs the transitional credential-management scope helper; API-key admin allow/deny behavior now relies on `AuthorizationPolicy`, with sanitized problem-response coverage and no stale route-policy helper warnings.
- 2026-06-25 — #710 archive-context authorization slice landed: archive routing now precedes auth/authorization, policy inputs carry selected archive name/schema/default metadata, and collection/template backing lookups use the selected archive schema.
- 2026-06-25 — #967 response-class security-header slice landed: central middleware now applies hosted-safe nosniff/referrer/permissions/frame/CSP/COOP/CORP headers by response class and forces no-store on client/server error responses.
- 2026-06-25 — #967 backup error redaction slice landed: backup `pg_dump` spawn/stderr failures now map to a stable `operation-failed` problem type, with raw stderr, DSNs, secrets, and host paths retained only in server logs.
- 2026-06-25 — #967 PKE error-contract slice landed: PKE crypto/keyset handlers now return RFC 9457 problem responses through `ApiError`, preserving safe validation/not-found/conflict details while redacting raw crypto/database diagnostics and removing legacy `error_description`.
- 2026-06-25 — #967 audio/vision provider-error slice landed: ad-hoc transcription and vision handlers now use a stable `provider-failure` problem type for backend failures and safe validation details for multipart/read errors, without exposing provider URLs or parser diagnostics.
- 2026-06-25 — #967 upload multipart error slice landed: attachment multipart upload now returns safe validation problem details for parser/read failures instead of raw multipart/read diagnostics.
- 2026-06-26 — #967 inference-config error-contract slice landed: inference config helper/probe failures now return RFC 9457 problem details without legacy `error`/`failures` payloads, submitted provider URL echoes, or raw probe diagnostics.
- 2026-06-26 — #967 chat/stream error-contract slice landed: chat validation/capacity/provider HTTP errors now return RFC 9457 problem details, `Retry-After` is preserved as a header, model-discovery failures use redacted provider-failure responses, and SSE generation failures no longer expose raw backend text.
- 2026-06-26 — #967 job API redaction slice landed: job list/detail responses now use a hosted-safe `JobResponse` projection that redacts sensitive payload/result fields and replaces stored failure diagnostics with a generic client-facing message.
- 2026-06-26 — #967 stored job generation-error slice landed: common AI revision/context/related-concept/metadata job failures now store a generic diagnostic message while logging raw backend detail server-side.
- 2026-06-26 — #967 inference-complete error-contract slice landed: `/api/v1/inference/complete` HTTP failures now use central problem responses and `/api/v1/inference/stream` SSE provider failures no longer expose raw backend text.
- 2026-06-26 — #967 ingest-stream pre-stream error slice landed: ingest cursor expiry and missing/invalid stream-token HTTP failures now return RFC 9457 problem responses, preserving 410/401 without legacy `{error, code}` bodies.
- 2026-06-26 — #967 stored job status-error slice landed: model-resolution failures and graph-maintenance step failures now store generic diagnostics instead of raw backend/database/provider strings.
- 2026-06-26 — #967 backup/restore diagnostic-redaction slice landed: backup filesystem/compression and restore psql/partial-failure paths now use generic operation-failed or issue messages without raw local paths, command stderr, or secrets.
- 2026-06-26 — #967 API error contract docs slice landed: developer docs now describe RFC 9457 Problem Details, the public problem-type catalog, request-id correlation, and the redaction boundary instead of promising legacy `{error}` bodies.
- 2026-06-26 — #967 OpenAPI problem-contract slice landed: generated OpenAPI now includes ProblemDetails/catalog schemas and a Fortemi error-contract extension with the stable problem-type URIs.
- 2026-06-26 — #967 MCP upstream-error parsing slice landed: MCP tool errors now summarize RFC 9457 problem details and suppress raw legacy JSON/text upstream response bodies, including 401 reauthorization failures.
- 2026-06-26 — #967 TUS upload error-contract slice landed: resumable-upload staging, record, offset, delete, and finalization failures now return generic operation-failed problems without raw staging paths, database diagnostics, or secrets.
- 2026-06-26 — #967 attachment media/download error-contract slice landed: variant, thumbnail, sprite, streaming open/seek, and non-missing file-read failures now return generic operation-failed problems without raw blob paths, backend diagnostics, or secrets.
- 2026-06-26 — #967 Twilio recording error-contract slice landed: recording HTTP client, download, non-success status, and body-read failures now return generic provider-failure problems without raw recording URLs, provider hosts, tokens, paths, or network diagnostics.
- 2026-06-26 — #967 archive-routing error-contract slice landed: invalid `X-Fortemi-Memory`, missing memory, and memory lookup failures now return `application/problem+json` without legacy `{error}` bodies or requested-memory-name echoes.
- 2026-06-26 — #967 health diagnostics redaction slice landed: public health output now reports provider URL presence instead of base URLs and live dependency failures expose stable reason codes instead of raw backend/database/provider exception text.
- 2026-06-26 — #967 live route-boundary coverage landed: an Axum router regression now verifies RFC 9457 problem responses carry propagated `request_id`, no legacy error envelope or raw internals, no-store cache policy, and API security headers at the middleware boundary.
- 2026-06-26 — #967 representative status-matrix coverage landed: route-boundary regressions now cover 400/401/403/404/409/429/500/503 RFC 9457 problem responses with stable type URIs, propagated `request_id`, no legacy error fields, no-store cache, hardening headers, and internal-detail redaction.
- 2026-06-26 — #967 ingest-stream protocol-frame redaction landed: per-line ack errors and fatal stream initialization frames now sanitize arbitrary parser/store/schema detail, keeping safe validation classes but excluding DSNs, secrets, paths, and raw backend text from SSE payloads.
- 2026-06-26 — #967 Twilio realtime protocol-frame redaction landed: invalid media-envelope WebSocket replies now come from a fixed helper payload with a stable code and coverage proving malformed provider/frame details, tokens, URLs, and paths are not echoed.
- 2026-06-26 — #910 PKE keyset audit-producer slice landed: keyset create/import/activate/delete/export successes now emit metadata-only `fortemi.audit` events with principal and keyset refs while excluding passphrases and key material.
- 2026-06-26 — #910 webhook control audit-producer slice landed: subscription create/update/delete/test successes now emit metadata-only `fortemi.audit` events with principal and webhook refs while excluding destination URLs, HMAC secrets, payloads, and response bodies.
- 2026-06-26 — #910 incoming webhook receiver audit-producer slice landed: receiver active-secret, signature, idempotency, payload-parse, schema-validation, and accepted-receipt decisions now emit metadata-only `fortemi.audit` events without HMAC secrets, raw signatures, idempotency keys, slugs, payloads, bodies, or URLs.
- 2026-06-26 — #910 inference config audit-producer slice landed: global and archive-scoped inference config set/reset operations now emit normalized metadata-only `fortemi.audit` events with principal, scope, changed-field names/counts, and provider presence flags while excluding before/after config JSON, API keys, URLs, models, referers, app names, and archive schema values.
- 2026-06-26 — #910 Twilio realtime audit-producer slice landed: WebSocket bind decisions now emit metadata-only `fortemi.audit` events for missing/stale session denial and successful bind without raw CallSid values, signatures, media payloads, or WebSocket URLs.
- 2026-06-26 — #910 job queue control audit-producer slice landed: global and archive-scoped pause/resume successes now emit metadata-only `fortemi.audit` events with principal, queue scope, and archive-name presence/length while excluding raw archive names and event-bus scope strings.
- 2026-06-26 — #926/#928/#933 fail-closed startup-config slice landed: hosted issuer, security boolean, realtime consent, and process-local rate-limit env values now fail fast on invalid startup configuration, with AsyncAPI metadata using the validated issuer source.
- 2026-06-26 — #710 MCP scope-family cleanup landed: bare `mcp` no longer implies REST read/write in the legacy principal helper, while MCP transport and tool policy tests continue to require explicit resource scopes.
- 2026-06-26 — #968/#974 telemetry redaction slice landed: raw model parse responses, search queries, Twilio recording URLs, and DTMF digits are no longer emitted to ordinary logs; logs retain bounded lengths/classes instead.
- 2026-06-26 — #968/#974 inference probe telemetry slice landed: inference config/test-connection/startup probe logs now use URL classes, lengths, counts, and stable reason codes instead of raw provider URLs or probe diagnostics.
- 2026-06-26 — #968/#974 inference Debug redaction slice landed: BYOK completion, runtime config update, provider partial config, and test-connection request Debug output now omits API keys, credential-bearing URLs, model/prompt text, referers, app names, and embedding backend values while retaining safe presence/count metadata.
- 2026-06-26 — #968/#974 PKE Debug redaction slice landed: PKE keygen/encrypt/decrypt/recipient/keyset import/export request and response Debug output now omits passphrases, plaintext, ciphertext, public-key payloads, encrypted private-key blobs, exported keyset names, labels, and filenames while retaining safe lengths/counts and stable non-secret ids/addresses.
- 2026-06-26 — #968/#974 PKE response/keyset Debug follow-up now reports PKE addresses, recipient addresses, keyset names, labels, and imported exported-keyset metadata as lengths/counts/presence only instead of raw `mm:` addresses or keyset identifiers.
- 2026-06-26 — #968/#974/#993 OAuth/API-key Debug redaction slice landed: OAuth client registration, authorization code/token, token request/response, introspection/revocation form, and one-time API-key creation response Debug output now omits client secrets, registration access tokens, authorization codes, bearer/refresh tokens, token hashes, PKCE values, credential-bearing URLs, raw API keys, and key prefixes while retaining safe presence/length/count metadata.
- 2026-06-26 — #968/#974/#987 MCP tool-output redaction slice landed: MCP success/error tool results now pass through a recursive sanitizer, generated curl auth headers use `<ACCESS_TOKEN>` placeholders instead of session/API-key interpolation, and returned streaming docs use scanner-safe `<ACCESS_TOKEN>` / `<STREAM_TOKEN>` examples.
- 2026-06-26 — #968/#974/#999/#1001/#1004 docs-contract advisory runner slice landed: `scripts/ci/docs-contract.cjs` now scans docs/config/example surfaces for token/API-key/client-secret placeholders and database/backup credential examples with redacted findings, self-test fixtures, advisory CI wiring, and local npm scripts.
- 2026-06-26 — #968/#974/#999/#1001/#1004 docs-contract baseline slice landed: `scripts/ci/docs-contract.baseline.json` records 202 known findings by redacted fingerprint only, and blocking mode now fails only for new unbaselined findings.
- 2026-06-26 — #968/#974/#999/#1004 public docs placeholder cleanup landed: API, authentication, realtime-event, and MCP deployment docs now use canonical scanner-safe placeholders, removing 93 docs-contract findings and shrinking the redacted baseline to 109 known findings.
- 2026-06-26 — #968/#974/#1001/#1004 public/operator DB docs cleanup landed: backup, configuration, use-case, integration, release, test-guide, operations, troubleshooting, and research docs now use `<DATABASE_URL>` / `<POSTGRES_PASSWORD>` placeholders, removing 33 docs-contract findings and shrinking the redacted baseline to 76 known findings.
- 2026-06-26 — #968/#974/#999/#1004 public provider/token docs cleanup landed: inference, memory-search, operator, release, job-monitoring, and MCP troubleshooting docs now use scanner-safe provider-key and bearer-token placeholders, removing 40 docs-contract findings and shrinking the redacted baseline to 36 known findings.
- 2026-06-26 — #968/#974/#999/#1001/#1004 docs-contract allowlist slice landed: remaining 36 findings are now fingerprint-allowlisted with owner issue, reason, review date, and local-dev/test-fixture classifications rather than generic baseline debt.
- 2026-06-26 — #968/#974 backup subprocess telemetry slice landed: pg_dump failure paths and post-restore psql maintenance warnings now expose stable command/status/stderr-length telemetry and generic operation-failed problems instead of raw subprocess stderr, DSNs, paths, SQLSTATE text, or secrets.
- 2026-06-26 — #968/#974 inbound streaming-health redaction landed: auth-exempt `/api/v1/health/streaming` now exposes aggregate inbound source count/events/errors/lag only, keeping connector names and per-source activity out of public health output.
- 2026-06-26 — #968/#974 inbound connector log-redaction landed: SSE/Redis/Kafka connector and supervisor ordinary logs now use destination classes, bounded lengths, and stable reason codes instead of raw endpoints, connector names, stream/topic/group names, or backend error text.
- 2026-06-26 — #974 event-bus job-failure mirror redaction landed: `fortemi::events` `job.failed` tracing now keeps job id/type plus error class/length, without raw stored job failure text.
- 2026-06-26 — #926/#928/#933 startup fail-closed gate completed: issuer, security boolean, realtime consent, and process-local rate-limit startup validation passed focused tests, and local/hosted issuer guidance was reconciled across README and operator/MCP docs.
- 2026-06-26 — #967 health live/readiness diagnostics now expose stable service `reason_code` fields instead of legacy `error` fields, with tests covering DSN/credential/hostname redaction.
- 2026-06-26 — #967 knowledge-shard export/import diagnostics now use generic operation-failed or fixed validation problem text instead of raw tar/multipart IO details.
- 2026-06-26 — #967 backup knowledge-archive diagnostics now use generic operation-failed or fixed validation problem text instead of raw filesystem, tar, multipart, metadata, or path diagnostics.
- 2026-06-26 — #967 knowledge-shard import/swap diagnostics now avoid raw file, tar, entry, and path errors by using generic operation-failed and fixed validation problem messages.
- 2026-06-26 — #967 backup JSON export and memory-scoped backup/restore lookup diagnostics now use generic operation-failed responses instead of raw serialization or archive lookup errors.
- 2026-06-26 — #967 incoming webhook parser diagnostics now use fixed validation problem text instead of raw JSON/form parser messages, with regression coverage for malformed JSON redaction.
- 2026-06-26 — #967 base64 upload/import decode diagnostics now use fixed validation problem text across shard import, attachment upload, and database backup upload paths.
- 2026-06-26 — #967 search strict-filter parser diagnostics now use fixed validation problem text instead of raw JSON parser line/column detail.
- 2026-06-26 — #967 backup/shard import result diagnostics now use fixed client-safe `errors[]` messages instead of raw database or JSON parser detail.
- 2026-06-26 — #967 EXIF attachment job failures now store generic diagnostics in job and attachment status surfaces instead of raw parser, transaction, storage, path, or attachment-id details.
- 2026-06-26 — #967 shared job schema-context failures now store a generic diagnostic instead of raw schema names or backend/schema resolution details.
- 2026-06-26 — #967 related-concept AI parse, metadata extraction, and document-type inference job failures now store generic diagnostics instead of raw parser, provider, transaction, or database details.
- 2026-06-26 — #967 embedding job generation and storage failures now store a generic diagnostic instead of raw provider, transaction, or database details.
- 2026-06-26 — #967 title-generation provider and title-save failures now store a generic diagnostic instead of raw provider, transaction, or database details.
- 2026-06-26 — #967 context-update link-fetch, note-fetch, and save failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 AI revision note-fetch, media-check, and save failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 AI contextual revision phase-1 fetch and save failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 embedding note-fetch failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 title-generation note-fetch failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 linking note-fetch and embedding-lookup setup failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 purge-note transaction, delete, and commit failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 embedding note-fetch commit failures now store a generic diagnostic instead of raw transaction commit details.
- 2026-06-26 — #967 concept-tagging and related-concept setup failures now store generic diagnostics instead of raw transaction or database details.
- 2026-06-26 — #967 reference-extraction setup failures now store a generic diagnostic instead of raw transaction or database details.
- 2026-06-26 — #967 re-embedding and embedding-set refresh failures now store generic diagnostics instead of raw database, set, or missing-embedding details.
- 2026-06-26 — #967 final job-handler raw-format cleanup removed stored AI revision pipeline detail and purge note-id echoing; `JobResult::Failed(format!(...))` no longer appears in `jobs.rs`.
- 2026-06-26 — #967 search and federated-search internal failures now use generic operation-failed problems instead of raw schema, memory, query, or database diagnostics.
- 2026-06-26 — #967 attachment upload/delete commit failures now use generic operation-failed problems instead of raw transaction diagnostics.
- 2026-06-26 — #967 note/tag validation errors now avoid echoing submitted tag content in RFC 9457 validation responses.
- 2026-06-26 — #967 attachment upload MIME validation now avoids echoing submitted content-type strings in RFC 9457 validation responses.
- 2026-06-26 — #967 note revision-mode and document-type validation now avoid echoing submitted mode/slug strings in RFC 9457 validation responses.
- 2026-06-26 — #967 incoming webhook schema validation now avoids echoing raw validator paths or payload values in RFC 9457 validation responses.
- 2026-06-26 — #967 chat model validation now avoids echoing requested model slugs in RFC 9457 validation responses.
- 2026-06-26 — #967 archive duplicate-name validation now avoids echoing submitted archive names in RFC 9457 validation responses.
- 2026-06-26 — #967 job type validation now avoids echoing submitted job type strings in RFC 9457 validation responses.
- 2026-06-26 — #967 embedding-config lookup validation now avoids echoing submitted config IDs in RFC 9457 validation responses.
- 2026-06-26 — #967 attachment document-type validation now avoids echoing submitted document type IDs in RFC 9457 validation responses.
- 2026-06-26 — #967 archive and backup-archive not-found responses now avoid echoing submitted archive names or filenames in RFC 9457 responses.
- 2026-06-26 — #967 memory not-found responses now avoid echoing submitted memory names in RFC 9457 responses.
- 2026-06-26 — #967 webhook not-found responses now avoid echoing webhook IDs or incoming receiver slugs in RFC 9457 responses.
- 2026-06-26 — #967 call-session not-found responses now avoid echoing submitted call UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 collection not-found responses now avoid echoing submitted collection UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 note not-found responses now avoid echoing submitted note UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 embedding set/config not-found responses now avoid echoing submitted slugs or config UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 inbound-source not-found responses now avoid echoing submitted source names in RFC 9457 responses.
- 2026-06-26 — #967 TUS upload not-found responses now avoid echoing submitted upload UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 note version/revision not-found responses now avoid echoing submitted version numbers in RFC 9457 responses.
- 2026-06-26 — #967 backup restore/archive/metadata not-found responses now avoid echoing submitted filenames in RFC 9457 responses.
- 2026-06-26 — #967 attachment media variant/sprite not-found responses now avoid echoing submitted variant names, sprite indexes, or attachment UUIDs in RFC 9457 responses.
- 2026-06-26 — #967 document-type not-found responses now avoid echoing submitted type names, and `ApiError::NotFound(format!(...))` no longer appears in `matric-api/src`.
- 2026-06-26 — #967 attachment sprite/TUS upload validation now avoids echoing submitted sprite indexes, upload lengths, offsets, or chunk sizes in RFC 9457 responses.
- 2026-06-26 — #967 incoming webhook idempotency conflicts now avoid echoing submitted idempotency keys in RFC 9457 responses.
- 2026-06-26 — #967 memory-search coordinate validation now avoids echoing submitted latitude, longitude, or radius values in RFC 9457 responses.
- 2026-06-26 — #967 PKE public-key length validation now avoids echoing decoded byte lengths in RFC 9457 responses.
- 2026-06-26 — #967 archive live-memory-limit validation now avoids echoing live archive counts or configured limits in RFC 9457 responses.
- 2026-06-26 — #967 note chunking validation now avoids echoing submitted chunk sizes or bulk note indexes in RFC 9457 responses.
- 2026-06-26 — #967 inference-config database failures now avoid constructing raw database error response strings in RFC 9457 responses.
- 2026-06-26 — #967 linking job schema/search/commit step failures now store fixed diagnostics instead of raw transaction or database details.
- 2026-06-26 — #967 ingest-stream NDJSON parser failures now avoid raw serde line/column diagnostics in stream validation errors.
- 2026-06-26 — #967 query datetime validation now avoids echoing submitted date strings or relative time units in client-visible validation errors.
- 2026-06-26 — #967 ingest-stream tag validation now avoids exposing submitted tag text or configured length/depth limits in stream validation errors.
- 2026-06-26 — #967 inference provider probes now use fixed diagnostic classes instead of constructing raw provider, HTTP status, or JSON parser strings.
- 2026-06-26 — #967 contextual revision empty-output jobs now store the generic contextual-revision failure message instead of pipeline-control detail.
- 2026-06-26 — #967 ad-hoc audio, vision, and inference completion provider failures now use fixed provider details instead of carrying raw backend text through the error boundary.
- 2026-06-26 — #967 PKE key generation, encryption, and keyset creation failures now use fixed operation details instead of carrying raw crypto/storage text through the error boundary.
- 2026-06-26 — #967 model and chat model discovery failures now log fixed diagnostic classes plus bounded lengths instead of raw provider discovery errors.
- 2026-06-26 — #967 chat generation and streaming startup failures now log fixed diagnostic classes plus bounded lengths instead of raw provider errors or model slugs.
- 2026-06-26 — #967 inference config override and audit failures now log fixed diagnostic classes plus bounded lengths instead of raw database, validation, or schema details.
- 2026-06-26 — #967 PKE keyset audit emission failures now log a fixed diagnostic class plus bounded length instead of raw audit sink errors.
- 2026-06-26 — #967 job ancillary chunk-merge, follow-up queue, contextual-discovery, revision-note, and provenance logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors.
- 2026-06-26 — #967 embedding/title/linking provenance, title escalation, link creation, and graph-maintenance queue logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors.
- 2026-06-26 — #967 linking tag-boost, threshold/wiki link creation, purge cleanup, and context-update link-fetch logs now use fixed diagnostic classes plus bounded lengths instead of raw errors or IDs/names.
- 2026-06-26 — #967 concept-tagging and reference-extraction GLiNER/LLM/tag/provenance logs now use fixed diagnostic classes plus bounded lengths instead of raw provider/parser errors or concept IDs.
- 2026-06-26 — #967 related-concept, metadata, document-type provenance, and re-embed queue logs now use fixed diagnostic classes plus bounded lengths instead of raw provider/parser errors, concept notations, or note IDs.
- 2026-06-26 — #967 EXIF attachment status/provenance/metadata update logs now use a fixed diagnostic class plus bounded lengths instead of raw storage/provenance errors or attachment IDs.
- 2026-06-26 — #967 remaining job-handler escalation, concept-resolution, AI revision, and parser fallback logs now use fixed diagnostic classes plus bounded lengths; `jobs.rs` no longer has raw `error = %e` structured log fields.
- 2026-06-26 — #967 top-level attachment/NLP pipeline job enqueue failures now log fixed diagnostic classes plus bounded lengths instead of raw queue errors, note IDs, or attachment IDs.
- 2026-06-26 — #967 operational audit, pause-state, job-progress, and webhook dispatch logs now use fixed diagnostic classes plus bounded lengths instead of raw sink/database/outbox errors or webhook slugs.
- 2026-06-26 — #967 authorization normalization, policy, and decision-audit logs now use fixed diagnostic classes plus bounded lengths instead of raw lookup/policy/audit sink errors.
- 2026-06-26 — #967 remaining `main.rs` related-notes, OAuth, attachment missing-blob, and central database logs now use fixed diagnostic classes plus bounded lengths instead of raw backend errors, paths, or OAuth descriptions.
- 2026-06-26 — #967 ingest fatal and chat generation terminal SSE error frames now use RFC 9457-style problem payloads instead of legacy `{error, code}` envelopes.
- 2026-06-26 — #967 OAuth token/introspection/revocation handler errors now use RFC 9457 problem responses instead of legacy `{error, error_description}` JSON, and public auth docs now show problem details.
- 2026-06-26 — #967 central ApiError internal, operation, provider, and blob-missing logs now use fixed diagnostic classes plus bounded lengths instead of raw details, paths, storage backends, or attachment IDs.
- 2026-06-26 — #967 attachment streaming missing-blob and EXIF job outcome telemetry now uses fixed classes, presence flags, and bounded lengths instead of raw attachment IDs, filenames, content types, or storage paths.
- 2026-06-26 — #967 AI revision deferral, chunking, follow-up queue, budget, and completion logs now use fixed diagnostic classes and presence flags instead of raw note/job IDs.
- 2026-06-26 — #967 contextual revision fallback, related-note discovery, phase-2 chunking, budget, and completion logs now use fixed diagnostic classes and presence flags instead of raw note IDs.
- 2026-06-26 — #967 embedding, title-generation, linking, and purge completion logs now use fixed diagnostic classes, presence flags, and bounded title length instead of raw note IDs or generated titles.
- 2026-06-26 — #967 remaining job-handler tier-flow and completion telemetry now uses presence flags and fixed diagnostic metadata instead of raw note IDs; the targeted `jobs.rs` raw-ID grep is empty.
- 2026-06-26 — #967 Twilio realtime WebSocket and ASR telemetry now uses fixed diagnostic classes, bounded lengths, and presence flags instead of raw CallSid/call UUID/error/backend fields.
- 2026-06-26 — #967 API style and MCP deployment docs now describe RFC 9457 Problem Details instead of the legacy `{error}` envelope, with docs-contract verification showing no new findings.
- 2026-06-26 — #967 completed: proxy/HSTS/header responsibilities are documented in the API error contract, focused header/problem tests pass, docs-contract has no new findings, and raw internal/error-format greps show no remaining API error-contract gaps.
- 2026-06-26 — #910 attachment upload audit slice emits metadata-only `fortemi.audit` events for JSON, multipart, and TUS accepted/denied decisions, and TUS finalization now runs the same safety validation before storing.
- 2026-06-26 — #910 hosted mandatory-audit contract slice adds `AuditAvailabilityPhase` and audit-unavailable dispositions for bootstrap degrade versus hosted-ready fail-closed rejection, with docs and focused core tests.
- 2026-06-26 — #910 SKOS taxonomy audit slice emits metadata-only `fortemi.audit` events for scheme/concept create/update/delete successes while excluding labels, definitions, notation values, change JSON, and archive names.
- 2026-06-26 — #910 SKOS collection audit slice emits metadata-only `fortemi.audit` events for collection create/update/delete and member add/remove/replace successes while excluding labels, definitions, member lists, and archive names.
- 2026-06-26 — #910 OAuth lifecycle audit slice emits metadata-only `fortemi.audit` events for token issue, introspection, and revocation successes while excluding bearer tokens, refresh tokens, codes, secrets, redirect URIs, raw scopes, hashes, client IDs, and user IDs.
- 2026-06-26 — #910 operator audit-routing docs explain how CE routes `fortemi.audit` through `TracingSink`, what operators must enforce externally, and which hosted/EE guarantees CE does not provide.
- 2026-06-26 — #910 remaining hosted/KMS audit wiring is blocked on #897 KeyProvider/KMS and hosted audit-health surfaces; roadmap proceeds with #968/#974 meanwhile.
- 2026-06-26 — #968/#974 hosted telemetry classification doc defines support-safe log/metric/diagnostic classes plus retained `event_outbox`/`inbound_dlq` payload sink policy, cross-linked from audit, errors, DSAR, stream-ingest, and operator docs.
- 2026-06-26 — #968/#974 inbound DLQ retained-payload telemetry now emits payload class, serialized length, and a secret-candidate flag for dead-letter events without raw payload snippets, with focused inbound tests.
- 2026-06-26 — #968/#974/#999 docs-contract passphrase/webhook-secret slice adds scanner coverage and cleans public/generated examples to canonical placeholders with no new findings.
- 2026-06-26 — #968/#974 sidecar subprocess telemetry now reports docker compose status, stderr length, and stable reason codes instead of raw sidecar stderr or IO diagnostics.
- 2026-06-26 — #968/#974 media optimizer subprocess telemetry now reports ffprobe/ffmpeg status, stderr length, reason codes, and IO-kind fields instead of raw stderr, paths, or token-shaped diagnostics.
- 2026-06-26 — #968/#974 audio utility subprocess telemetry now reports ffmpeg/ffprobe status, stderr length, reason codes, IO-kind, and path lengths instead of raw stderr, filesystem paths, or OS diagnostics.
- 2026-06-26 — #968/#974 video multimodal subprocess telemetry now reports ffprobe/ffmpeg status, stderr length, reason codes, IO-kind, and path lengths instead of raw stderr, filesystem paths, or OS diagnostics.
- 2026-06-26 — #968/#974 video thumbnail/waveform logs now report media path lengths instead of raw video/audio filesystem paths in broad telemetry.
- 2026-06-26 — #968/#974 video keyframe-read warnings now report extracted frame path lengths instead of raw temporary frame filesystem paths in broad telemetry.
- 2026-06-26 — #968/#974 PDF and Office extraction subprocess failures now report stable command/status/stderr metadata instead of raw tool stderr or OS diagnostics.
- 2026-06-26 — #968/#974 GLB renderer telemetry now reports destination classes, lengths, status, and reason codes instead of raw renderer URLs, filenames, response bodies, or backend errors.
- 2026-06-26 — #968/#974 extraction handler derived-file and terminal extraction logs now report filename/path lengths and stable reason classes instead of raw derived filenames, source paths, or backend error text.
- 2026-06-26 — #968/#974 video multimodal adapter audio/keyframe/description logs now report filename lengths, error lengths, and stable reason classes instead of raw filenames or backend error text.
- 2026-06-26 — #968/#974 PDF text/OCR and Office conversion logs now report filename lengths, error lengths, and stable reason classes instead of raw filenames or extraction backend error text.
- 2026-06-26 — #968/#974 spreadsheet adapter logs now report filename/sheet-name lengths, error lengths, and stable reason classes instead of raw filenames, sheet names, or backend error text.
- 2026-06-26 — #968/#974 worker and sidecar lifecycle logs now report error lengths and stable reason classes instead of raw stale-job, model warmup/unload, or sidecar lifecycle backend error text.
- 2026-06-26 — #968/#974 keyframe vision handler logs now report error lengths and stable reason classes instead of raw vision backend, transcript-context, fan-in, or queue backend error text.
- 2026-06-26 — #968/#974 view vision handler logs now report error lengths and stable reason classes instead of raw vision backend, fan-in, or queue backend error text.
- 2026-06-26 — #968/#974 keyframe/view assembly logs now report error lengths, stable reason classes, and note presence flags instead of raw note ids or note/update/manifest/downstream queue backend error text.
- 2026-06-26 — #968/#974 speaker diarization logs now report error lengths, stable reason classes, and attachment presence flags instead of raw transcode, metadata, caption-storage, or speaker-config backend error text and raw attachment IDs.
- 2026-06-26 — #968/#974 speaker relabel logs now report error lengths, stable reason classes, and attachment presence flags instead of raw caption-storage backend error text and raw attachment IDs.
- 2026-06-26 — #968/#974 audio transcription logs now report error lengths, stable reason classes, filename lengths, and note presence flags instead of raw caption filenames, queue/transcode/chunk backend errors, or raw note IDs.
- 2026-06-26 — #968/#974 audio chunk fan-in logs now report error lengths and stable reason classes instead of raw fan-in count or merge backend error text.
- 2026-06-26 — #968/#974 extraction result, faststart, and thumbnail logs now report attachment/note presence flags, error lengths, and stable reason classes instead of raw attachment IDs, thumbnail IDs, or backend error text.
- 2026-06-26 — #968/#974 extraction transcript attachment logs now report attachment/note presence flags, error lengths, and stable reason classes instead of raw parent attachment IDs or caption-storage backend error text.
- 2026-06-26 — #968/#974 extraction derived-file metadata, AI-description, and commit logs now report child/parent attachment presence flags, stored counts, error lengths, and stable reason classes instead of raw child/parent attachment IDs or backend error text.
- 2026-06-26 — #968/#974 extraction audio-transcription and diarization queue logs now report note/attachment presence flags, error lengths, and stable reason classes instead of raw note IDs, attachment IDs, or queue backend error text.
- 2026-06-26 — #968/#974 extraction file-access, thumbnail sprite, and keyframe vision queue logs now report path lengths, note/attachment presence flags, error lengths, and stable reason classes instead of raw filesystem paths, note IDs, attachment IDs, or queue backend error text.
- 2026-06-26 — #968/#974 extraction view vision queue logs now report note/attachment presence flags, view counts, error lengths, and stable reason classes instead of raw note IDs, attachment IDs, or queue backend error text.
- 2026-06-26 — #968/#974 extraction content propagation and downstream requeue logs now report note presence, content lengths, job types, error lengths, and stable reason classes instead of raw note IDs or note/job backend error text.
- 2026-06-26 — #968/#974 extraction checkpoint and failure-status logs now report attachment presence, completed counts, error lengths, and stable reason classes instead of raw attachment IDs or attachment-status backend error text; the targeted extraction-handler raw ID/error scan is now empty.
- 2026-06-26 — #968/#974 audio utility duration-probe fallback logs now report error lengths and stable reason classes instead of raw probe backend error text; the `matric-jobs` raw telemetry scan is now down to the persisted pause-state parse warning.
- 2026-06-26 — #968/#974 pause-state parse fallback logs now report error lengths and stable JSON reason classes instead of raw persisted-state parser text; the targeted `matric-jobs` raw telemetry scan is now empty.
- 2026-06-26 — #968/#974 generic job failure ordinary logs now report retry state, duration, error length, and stable reason class instead of raw handler failure text, while persisted job failure state remains unchanged.
- 2026-06-26 — #968/#974 inbound connector telemetry length fields now cap user/backend-originated diagnostic text at 512 characters and retain metadata-only output for CR/LF log-injection text, credential-bearing DSNs, hostnames, and token-shaped values.
- 2026-06-26 — #968/#974 hosted telemetry classification now inventories localized audit/telemetry stores with owner, access, retention/DSAR, safe broad telemetry, and `fortemi.audit` mapping details.
- 2026-06-26 — #968 hosted secret inventory contract now defines secret classes, storage rules, response metadata, lifecycle requirements, surface inventory, and cross-issue ownership, with docs-contract verification at `new=0`.
- 2026-06-26 — #968/#974 webhook Debug redaction now uses metadata-only output for webhook configs, delivery records, create requests, and incoming receiver requests, with focused model coverage.
- 2026-06-26 — #968/#974 incoming webhook receiver and validation Debug output now redacts raw slugs, providers, schema refs/docs, signature headers, payloads, and validation errors.
- 2026-06-26 — #968/#974 webhook delivery history now exposes metadata-only diagnostic records instead of raw retained payloads or response bodies, with route-level redaction coverage.
- 2026-06-26 — #968/#974 webhook management list/get/update responses now expose metadata-only subscription records instead of raw destination URLs, event names, or signing secrets.
- 2026-06-26 — #968/#974 incoming webhook receiver management list/get/update responses now expose metadata-only receiver records instead of raw slugs, provider/schema names, signature headers, schema docs, or HMAC material.
- 2026-06-26 — #968/#974 incoming webhook accepted responses now return metadata-only acknowledgements instead of raw receiver identifiers, payloads, side effects, or idempotency keys.
- 2026-06-26 — #968/#974 inference completion/provider DTO Debug output now redacts raw prompts, generated content, model/provider identifiers, provider URLs, and capability strings.
- 2026-06-26 — #968/#974 audio/vision ad-hoc response Debug output now reports transcript/description/model lengths and segment counts instead of raw generated text, transcript segments, speaker labels, word text, or model identifiers.
- 2026-06-26 — #968/#974 effective inference config Debug output now redacts sourced URLs, model names, API-key sentinels, referers, app names, embedding backend ids, and provider ids.
- 2026-06-26 — #968/#974 inference config API-key readback and audit JSON now use metadata-only secret sentinels instead of raw or prefix-masked key values.
- 2026-06-26 — #968/#974 inference config audit row/query Debug output now redacts raw audit JSON snapshots, actor filters, action filters, source IPs, URLs, model names, and API keys.
- 2026-06-26 — #968/#974 inference test-connection response Debug output now redacts raw provider detection details, model lists, backend errors, and recovery suggestions.
- 2026-06-26 — #968/#974 inference config update/reset response Debug output now redacts raw config snapshots, status strings, warnings, URLs, model names, API keys, referers, and app names.
- 2026-06-26 — #968/#974 API-key management list responses now return metadata-only key records after one-time create display, without key prefixes, labels, descriptions, scopes, hashes, or raw keys.
- 2026-06-26 — #968/#974 OAuth client post-creation Debug output now reports metadata-only lengths/counts instead of raw client ids, labels, URLs, contacts, software fields, or scopes.
- 2026-06-26 — #968/#974 inbound SSE, Redis Stream, and Kafka connector config Debug output now redacts credential-bearing URLs, headers, SASL credentials, topics, event fields, and extra config values.
- 2026-06-26 — #968/#974 inbound source management list responses now return metadata-only connector records instead of raw names, kinds, or config JSON.
- 2026-06-26 — #968/#974 inbound source model Debug output now reports metadata-only source/config shape instead of raw connector names, kinds, URLs, headers, stream metadata, or config values.
- 2026-06-26 — #968/#974 chat request/response and stream replay Debug output now redacts raw prompts, context, history, generated content, action payloads, model metadata, SSE frame data, replay frame data, and session ids.
- 2026-06-26 — #968/#974 chat resume cursor and model-list Debug output now redacts raw Last-Event-ID session ids, default model ids, installed model ids, and model profile identifiers.
- 2026-06-26 — #968/#974 chat stream-store warnings now report stable Redis/serialization operation and reason metadata instead of raw Redis URLs, hostnames, auth text, session keys, or backend diagnostics.
- 2026-06-26 — #968/#974 idempotency, ingest cursor, and ingest token store warnings now report stable operation/reason metadata instead of raw Redis URLs, hostnames, auth text, replay keys, stream ids, token ids, bearer-token-shaped values, or backend diagnostics.
- 2026-06-26 — #968/#974 Whisper preload warnings now report stable reason metadata instead of raw provider URLs, status bodies, parser details, model identifiers, tokens, or backend diagnostics.
- 2026-06-26 — #968/#974 backup metadata and memory-restore warnings now report stable operation/phase/reason metadata, error lengths, and memory-name lengths instead of raw DB, filesystem, schema, path, or secret-shaped diagnostics.
- 2026-06-26 — #968/#974 CORS and archive-routing warnings now report URL classes, bounded lengths, stable reason codes, and error lengths instead of raw invalid origins, archive names, schema names, topology URLs, or backend diagnostics.
- 2026-06-26 — #968/#974 search-cache Redis telemetry now reports URL classes, bounded lengths, stable reason codes, and key lengths instead of raw Redis URLs, backend errors, or query-derived cache keys.
- 2026-06-26 — #968/#974 search query Debug output now reports query/filter/mode/embedding-set/since/tag/strict-filter lengths and date-filter presence instead of raw query text, filter JSON, embedding-set names, tags, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 ingest-token data Debug output now reports token-id and schema lengths instead of raw revocation ids or archive schema names while preserving Redis JSON persistence.
- 2026-06-26 — #968/#974 idempotency replay-record Debug output now reports body-hash length, response status, JSON class, and response length instead of raw request hashes, cached webhook response bodies, URLs, tokens, or payload fields.
- 2026-06-26 — #968/#974 reconstruction response Debug output now reports content/title/tag/chunk metadata lengths and counts instead of raw reconstructed note content, titles, tags, or chunk titles.
- 2026-06-26 — #968/#974 speaker relabel config Debug output now reports speaker counts and field lengths instead of raw speaker ids, names, roles, transcript-derived labels, or secret-shaped fragments.
- 2026-06-26 — #968/#974 search response Debug output now reports result count, query length, and total instead of raw user query text, snippets, titles, tags, emails, or token-shaped hit content.
- 2026-06-26 — #968/#974 generic list response Debug output now reports item counts and pagination only instead of recursively formatting raw list items, content, DSNs, emails, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 federated search Debug output now reports query/memory/result metadata lengths and counts instead of raw cross-memory queries, memory names, snippets, titles, tags, or token-shaped hit content.
- 2026-06-26 — #968/#974 backup metadata Debug output now reports sidecar metadata lengths/counts and version-field presence instead of raw titles, descriptions, source strings, filenames, paths, DSNs, PostgreSQL version text, or migration names.
- 2026-06-26 — #968/#974 database backup response Debug output now reports filename/path/metadata lengths and presence instead of raw backup filenames, local paths, echoed titles/descriptions, metadata filenames, DSNs, or token-shaped values.
- 2026-06-26 — #968/#974 shard manifest Debug output now reports version/component/checksum/migration metadata lengths and counts instead of raw shard versions, component names, checksums, migration authors, or migration change text.
- 2026-06-26 — #968/#974 backup archive-list Debug output now reports directory, filename, path, metadata, checksum, and manifest presence/length metadata instead of raw backup directories, filenames, paths, sidecar text, hashes, or embedded manifest details.
- 2026-06-26 — #968/#974 related-notes Debug output now reports snippet, title, tag, source, and LLM context-summary lengths/counts instead of raw related-note content or generated summaries.
- 2026-06-26 — #968/#974 call-detail Debug output now reports provider/call-id/remote-party/metadata/transcript segment lengths, classes, counts, and presence instead of raw provider identifiers, phone-like values, metadata JSON, or transcript text.
- 2026-06-26 — #968/#974 backup JSON export Debug output now reports manifest string lengths and exported notes/collections/tags/templates counts instead of raw exported records, tag names, collection names, template bodies, or manifest strings.
- 2026-06-26 — #968/#974 backup JSON import Debug output now reports manifest presence, imported record counts, note field lengths, and error counts instead of raw imported note content, tags, collection/template JSON, manifest JSON, status text, or import diagnostics.
- 2026-06-26 — #968/#974 backup trigger/status Debug output now reports destination counts, status/output/path/filename/size-label lengths, and latest-backup presence instead of raw backup destinations, command output strings, backup directories, filenames, disk labels, or status strings.
- 2026-06-26 — #968/#974 shard import Debug output now reports shard payload length, include length, manifest presence, import counts, error counts, and warning counts instead of raw base64 shard payloads, component filters, manifest details, status text, import errors, or warnings.
- 2026-06-26 — #968/#974 backup swap Debug output now reports filename, strategy, status, and message lengths plus import stats instead of raw backup filenames, restore strategies, status text, or restore messages.
- 2026-06-26 — #968/#974 database restore Debug output now reports filename, memory, message, prerestore-backup, and restored-from lengths instead of raw backup filenames, memory names, restore messages, prerestore filenames, restored-from paths, DSNs, or hostnames.
- 2026-06-26 — #968/#974 database backup upload Debug output now reports backup payload, original filename, title, and description lengths instead of raw uploaded backup data, filenames, titles, descriptions, DSNs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 database snapshot request Debug output now reports snapshot name, title, and description lengths instead of raw operator-supplied backup names, titles, descriptions, DSNs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 backup metadata update request Debug output now reports title and description lengths instead of raw operator-supplied backup metadata, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 memory info Debug output now reports embedding-set counts, field lengths, storage sizes, and recommendation note lengths instead of raw embedding names, slugs, descriptions, model identifiers, index labels, size labels, recommendation notes, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 memories overview Debug output now reports memory counts, storage byte totals, and size-label/name lengths instead of raw memory/archive names, human storage labels, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 attachment upload Debug output now reports document-type presence, option values, and filename/content-type/payload/vision-mode lengths instead of raw filenames, content types, base64 payloads, media/vision option strings, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 create-note request Debug output now reports content/title/source/format/document-type/model/pipeline/tag lengths, counts, chunking values, and metadata presence instead of raw note content, titles, tags, metadata JSON, model identifiers, source strings, document-type slugs, pipeline names, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 bulk create-note Debug output now reports per-item content/title/source/format/document-type/tag/revision metadata lengths, counts, chunking values, and metadata presence instead of raw note content, titles, tags, metadata JSON, source strings, document-type slugs, revision modes, formats, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 update-note request Debug output now reports content/revision-mode/model/tag lengths, counts, chunking values, booleans, and metadata presence instead of raw note content, tags, metadata JSON, model identifiers, revision modes, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 restore-note query Debug output now reports revision-mode length instead of raw restore revision-mode strings, DSNs, hostnames, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 reprocess request Debug output now reports revision-mode/model/step lengths, step counts, note-id counts, limits, and chunking values instead of raw pipeline step names, model identifiers, revision modes, note IDs, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 tag mutation Debug output now reports tag counts/lengths, concept-id presence, and primary flags instead of raw tag paths, email-like tag values, token-shaped tag values, or concept IDs.
- 2026-06-26 — #968/#974 SKOS relation mutation Debug output now reports target concept-id presence instead of raw relation target UUIDs.
- 2026-06-26 — #968/#974 collection mutation Debug output now reports collection name/description lengths and parent/collection-id presence instead of raw collection names, descriptions, parent IDs, collection IDs, paths, emails, DSNs, or token-shaped values.
- 2026-06-26 — #968/#974 collection and SKOS collection list-query Debug output now reports parent/scheme id presence instead of raw parent collection or taxonomy scheme UUIDs.
- 2026-06-26 — #968/#974 SKOS query Debug output now reports search/autocomplete/status/facet lengths and scheme-id presence instead of raw search strings, taxonomy filter strings, scheme IDs, DSNs, hostnames, emails, or token-shaped values.
- 2026-06-26 — #968/#974 graph diagnostics/maintenance Debug output now reports edge-filter, snapshot-label, and maintenance-step lengths/counts plus numeric controls instead of raw filter strings, labels, step names, DSNs, hostnames, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 template request Debug output now reports template name/description/content/format/tag/variable lengths, counts, collection-id presence, and revision-mode length instead of raw template content, variables, tags, names, descriptions, collection IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 note query Debug output now reports timeline/activity/health/access/list filter lengths, counts, numeric controls, date-filter presence, and collection-id presence instead of raw query filters, tag filters, sort strings, relative-time strings, collection IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 note timeline/activity response Debug output now reports note-id counts, title lengths, timestamps, and recency flags instead of raw note IDs, note titles, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 note links response Debug output now reports link counts, URL/snippet/metadata counts, and metadata secret-candidate counts instead of raw link IDs, note IDs, URLs, relation kinds, snippets, metadata JSON, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 archive management request Debug output now reports archive name/new-name and description lengths instead of raw memory names, clone names, descriptions, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 archive stats response Debug output now reports archive/schema-name lengths and numeric stats instead of raw memory names, schema names, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 document-type request Debug output now reports category, filename, content-sample, and MIME-type lengths instead of raw category filters, filenames, content samples, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Twilio realtime wire Debug output now reports media-stream sequence, call SID, payload, timestamp, chunk, mark, DTMF, webhook phone/recording/consent/disclosure field lengths instead of raw provider IDs, phone numbers, recording URLs, media payloads, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Deepgram realtime wire Debug output now reports event type, transcript, provider description/message lengths, channel presence, alternative/word counts, and word timing/speaker presence instead of raw transcripts, provider messages, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Deepgram provider config Debug output now reports API-key presence/length plus listen URL, model, language, and encoding lengths instead of raw API keys, provider URLs, model/profile identifiers, emails, or token-shaped values.
- 2026-06-26 — #968/#974 inference provider config Debug and registration telemetry now report provider/base URL/API-key/referer/title/model values as lengths, booleans, capabilities, or health metadata instead of raw provider IDs, credential-bearing URLs, API keys, referers, model slugs, emails, or token-shaped values.
- 2026-06-26 — #968/#974 compiled inference config module now redacts Ollama/OpenAI URL, API-key, and model Debug output plus invalid base-URL validation diagnostics, and restores the documented `matric_inference::config` API path with declared `dirs`/`toml` dependencies.
- 2026-06-26 — #968/#974 inference selector Debug output now reports selected/recommended model names, alternative lists, rationale text, and capability maps as lengths/counts plus operation/tier metadata instead of raw private model identifiers, generated rationale, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SSE EventSource query Debug output now reports token presence/length plus memory, event-type filter, and entity-id lengths instead of raw bearer/API-key tokens, memory names, event filters, entity IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 OAuthApiError Debug output now reports OAuth code/description/URI lengths and database diagnostic length instead of raw OAuth descriptions, internal URLs, client secrets, DSNs, hosts, or backend diagnostics.
- 2026-06-26 — #968/#974 AuthPrincipal/Auth extractor Debug output now reports principal class plus identity/scope lengths and counts instead of raw OAuth client IDs, user IDs, API-key IDs, scopes, tenant strings, or token-shaped values.
- 2026-06-26 — #968/#974 NoteTemplate Debug output now reports template field lengths/counts and identifier presence instead of raw template names, descriptions, body content, formats, tags, collection IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 memory and attachment search Debug output now reports hit/response counts, field lengths, and presence flags instead of raw filenames, content types, event titles, location names, timeline periods, UUIDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 cross-archive search Debug output now reports query/archive/result/tag lengths and counts instead of raw queries, archive names, snippets, titles, tags, note IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 FAIR export Debug output now reports Dublin Core and JSON-LD metadata lengths/counts instead of raw note titles, descriptions, tags, creator/rights/source fields, linked note URNs, SKOS concepts, provenance activity IDs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 strict tag filter Debug output now reports concept/scheme/tag counts and notation lengths instead of raw concept UUIDs, user-entered tag/scheme filters, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS label/note Debug output now reports label/note/language/author/source lengths and identifier presence instead of raw concept IDs, labels, documentation notes, authors, source URLs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS concept-scheme Debug output now reports scheme metadata lengths, counts, booleans, and date presence instead of raw scheme IDs, URIs, notation, titles, descriptions, creators, publishers, rights, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS concept Debug output now reports concept metadata lengths, counts, booleans, and identifier presence instead of raw concept IDs, scheme IDs, URIs, notation, labels, facet strings, deprecation reasons, embedding model names, hierarchy paths, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS relation Debug output now reports relation IDs, concept IDs, external target URLs, labels, actors, and validators as presence flags or lengths instead of raw UUIDs, credential-bearing URLs, labels, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS tagging/audit/merge Debug output now reports note/concept IDs, source IDs, target IDs, source strings, actor fields, change JSON, and merge reasons as counts, lengths, classes, or presence flags instead of raw UUIDs, provenance strings, audit change payloads, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS governance/search Debug output now reports scheme metadata, search query filters, and result lists as lengths, counts, numeric controls, or presence flags instead of raw scheme IDs, scheme names/titles, search strings, nested concept results, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 SKOS collection Debug output now reports collection/member metadata as lengths, counts, booleans, timestamps, or identifier presence flags instead of raw collection IDs, scheme IDs, concept IDs, labels, definitions, URLs, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 tag input/spec/resolution Debug output now reports tag paths, SKOS labels, relation paths, scheme names, facet domains, and resolved IDs as lengths, counts, or presence flags instead of raw user tag values, DSNs, paths, emails, UUIDs, or token-shaped values.
- 2026-06-26 — #968/#974 OAuth authorization request/form Debug output now reports response-type, client-id, redirect-uri, scope, state, PKCE, method, and action lengths instead of raw client IDs, redirect URLs, scopes, state values, PKCE challenges, emails, DSNs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 job create/list Debug output now reports note-id presence, job/status/filter lengths, payload class/length, and secret-candidate flags instead of raw job payload JSON, note IDs, filters, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 job API response Debug output now reports job/note id presence, payload/result classes and lengths, and progress/error lengths instead of raw job IDs, payload/result JSON, progress messages, paths, DSNs, URLs, or generated text.
- 2026-06-26 — #968/#974 ProblemDetails Debug output now reports problem metadata lengths and attachment-id presence instead of raw problem detail text, instance paths, request ids, DSNs, paths, URLs, or token-shaped values.
- 2026-06-26 — #968/#974 ApiError Debug output now reports variant metadata and detail/path lengths instead of raw error details, operation/provider diagnostics, blob paths, attachment ids, DSNs, URLs, or token-shaped values.
- 2026-06-26 — #968/#974 Startup storage path telemetry now reports file-storage and TUS staging path/error lengths instead of raw filesystem paths or OS diagnostics in startup logs.
- 2026-06-26 — #968/#974 Startup issuer/auth telemetry now reports issuer URL class/length and fallback host length instead of raw issuer URLs, bind-host values, credentials, hostnames, or email-like path segments.
- 2026-06-26 — #968/#974 Startup model/bind telemetry now reports model identifier lengths and bind-host length/port instead of raw backend model names or server bind addresses in startup logs.
- 2026-06-26 — #968/#974 Memory search query Debug output now reports coordinate/radius/time-bound presence instead of raw latitude, longitude, radius, or temporal bounds in spatial/temporal search diagnostics.
- 2026-06-26 — #968/#974 Call detail Debug output now reports call/archive ID presence plus metadata lengths/classes instead of raw call IDs, archive IDs, provider call identifiers, remote parties, transcript text, or recording metadata.
- 2026-06-26 — #968/#974 export, version, shard, attachment, and TUS query Debug output now reports selector/filter/include/filename/content-type/variant/vision-mode lengths and UUID presence instead of raw operator strings, filenames, MIME filters, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 related-concept LLM pair Debug output now reports concept string lengths and confidence instead of raw model-generated concept labels, emails, DSNs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 model discovery response Debug output now reports model/provider/default counts and field lengths instead of raw model slugs, provider identifiers, deployment health strings, model families, or default model names.
- 2026-06-26 — #968/#974 GLiNER NER and semantic-link classification Debug output now reports entity text/label/model/reasoning metadata lengths and counts instead of raw extracted entities, private model identifiers, generated reasoning, emails, or token-shaped values.
- 2026-06-26 — #968/#974 transcription and diarization Debug output now reports transcript, word, language, speaker, and segment metadata lengths/counts instead of raw transcript text, word text, speaker identifiers, phone-like values, emails, or token-shaped values.
- 2026-06-26 — #968/#974 OpenAI wire DTO Debug output now reports prompt, embedding, chat message, stream delta, response id, model, and provider-error metadata lengths/counts instead of raw prompts, embeddings, model identifiers, response IDs, generated content, provider URLs, emails, or token-shaped values.
- 2026-06-26 — #968/#974 few-shot prompt example Debug output now reports input/output/tag/task-description lengths and counts instead of raw example text, generated outputs, tags, task descriptions, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 refinement and reflexion Debug output now reports refined content, critique, ReAct reasoning, final answer, episode, lesson, and memory metadata lengths/counts instead of raw generated content, critiques, reasoning traces, lessons, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 model discovery Debug output now reports discovered model, recommendation, alternative, cloud-comparison, discovery-error, and Ollama response metadata lengths/counts instead of raw model names, families, provider details, recommendation rationale, URLs, emails, or token-shaped values.
- 2026-06-26 — #968/#974 evaluation DTO Debug output now reports result, summary, test-case, judge prompt/result, and report metadata lengths/counts instead of raw eval outputs, expected answers, prompts, cases, judge reasoning, model identifiers, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 hardware recommendation Debug output now reports GPU-name, model, role, rationale, quality-note, Ollama setting, provider, cost, and latency metadata lengths instead of raw hardware topology, model identifiers, provider labels, recommendation text, URLs, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 model capability Debug output now reports capability, tier, score, latency, note length, model-name length, rating count, rating capabilities, embedding flag, and evaluation timestamp length instead of raw model identifiers, evaluator notes, timestamps, URLs, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 latency diagnostics Debug output now reports operation, latency targets, suggestion length, context config count, configured operations, and scale factor instead of raw optimization suggestions, private model identifiers, URLs, emails, token-shaped values, or recursive context configuration details.
- 2026-06-26 — #968/#974 provider profile Debug output now reports provider ids, display names, base URLs, env-var conventions, default models, headers, and endpoints as lengths/counts plus capability metadata instead of raw provider identifiers, URLs, env names, model ids, header values, endpoints, emails, or token-shaped values.
- 2026-06-26 — #968/#974 embedding model profile Debug output now reports model name, query/passage prefixes, family, and description as lengths while retaining numeric dimension/token/symmetry metadata instead of raw embedding model identifiers, prefixes, descriptions, URLs, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 performance profile Debug output now reports model names, families, sizes, and preferred-family requirements as lengths while retaining numeric context/output/speed and thinking metadata instead of raw private model identifiers, family labels, size labels, emails, or token-shaped values.
- 2026-06-26 — #968/#974 model restriction Debug output now reports restriction type plus model, reason, and alternative lengths instead of raw model identifiers, private policy reasons, alternatives, emails, paths, URLs, or token-shaped values.
- 2026-06-26 — #968/#974 circuit-breaker Debug output now reports service-name length, state, failure counters, trip counts, and last-failure presence instead of raw sidecar service names, URLs, hosts, token query strings, paths, emails, or credential-shaped values.
- 2026-06-26 — #968/#974 mock inference Debug output now reports response-map counts, prompt/response lengths, default-response length, operation/input lengths, and timing metadata instead of raw prompts, generated responses, emails, URLs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 OpenAI backend Debug and telemetry now report base URL, API key, model, referer, title, prompt, and system-message values as lengths or presence flags instead of raw provider URLs, API keys, model identifiers, referers, workspace titles, prompts, emails, or token-shaped values.
- 2026-06-26 — #968/#974 inference config error Debug output now reports file/TOML diagnostic classes and invalid backend, validation, or missing-backend payload lengths instead of raw backend strings, credential-bearing URLs, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 thinking response Debug output now reports extracted reasoning and final-answer lengths plus thinking type instead of raw chain-of-thought, generated answers, emails, URLs, filesystem paths, or token-shaped values.
- 2026-06-26 — #968/#974 eval runner CLI diagnostics now report model, data-dir, output-dir, invalid-tier, test-id, and report-path lengths instead of raw model identifiers, filesystem paths, emails, URLs, or token-shaped values in runner logs and Args Debug output.
- 2026-06-26 — #968/#974 provider routing telemetry now reports provider slug, model, default provider, embedding provider, and registry provider IDs as lengths/counts instead of raw provider identifiers, model slugs, emails, URLs, or token-shaped values.
- 2026-06-26 — #968/#974 Ollama and vision model lifecycle telemetry now reports backend URLs, embedding/generation/vision model names, fast-model names, warmup/unload timeout diagnostics, and VRAM lifecycle events as lengths instead of raw model identifiers, URLs, emails, or token-shaped values.
- 2026-06-26 — #968/#974 inference config and health telemetry now reports config-file paths, OpenAI/Ollama health-check errors, and API fast-generation model startup configuration as lengths instead of raw filesystem paths, endpoint URLs, model identifiers, emails, or token-shaped values.
- 2026-06-26 — #968/#974 inference request telemetry now reports Ollama request span model names plus API completion/chat provider and model identifiers as lengths instead of raw model slugs, provider IDs, emails, URLs, or token-shaped values.
- 2026-06-26 — #968/#974 file-storage operational telemetry now reports filesystem paths, storage paths, temp paths, and storage deletion errors as lengths or stable classes instead of raw host paths, blob paths, OS diagnostics, emails, or token-shaped values.
- 2026-06-26 — #968/#974 job-handler operational telemetry now reports wiki-link targets, related-concept labels/notations, EXIF camera strings, GPS coordinate presence, and provenance IDs as lengths or presence flags instead of raw note-derived titles, concept labels, camera identifiers, coordinates, UUIDs, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 job-worker operational telemetry now reports task panic, tier claim, job start/timeout/completion/failure, and persistence-error diagnostics with job-id presence, bounded job type/tier metadata, error lengths, and stable reason codes instead of raw job UUIDs, panic payloads, database diagnostics, DSNs, paths, or token-shaped values.
- 2026-06-26 — #968/#974 media-optimization operational telemetry now reports ffprobe/ffmpeg fallback failures, probe metadata, variant storage errors, attachment IDs, derivation types, codecs, formats, and derived IDs as lengths, presence flags, sizes, or stable reason codes instead of raw attachment UUIDs, generated filenames, backend diagnostics, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 thumbnail-sprite job telemetry and failure text now report attachment presence, sheet indexes, error lengths, and stable reason codes instead of raw attachment UUIDs, schema names, backend diagnostics, DSNs, hostnames, paths, or token-shaped values.
- 2026-06-26 — #968/#974 sidecar lifecycle telemetry now reports stable sidecar kinds, service-name lengths, status/stderr lengths, elapsed time, and reason codes instead of raw compose service strings, sidecar Debug values, stderr text, DSNs, hostnames, paths, or token-shaped values.
- 2026-06-26 — #968/#974 keyframe character/setting vision job telemetry and job failure text now report attachment presence, model/error/analysis lengths, job-id presence, and stable reason codes instead of raw schema names, attachment UUIDs, model names, database diagnostics, backend errors, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 keyframe scene/view vision job telemetry and job failure text now report attachment/view presence, model/error/description lengths, job-id presence, and stable reason codes instead of raw schema names, attachment UUIDs, model names, database diagnostics, backend errors, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 keyframe/view assembly job telemetry and job failure text now report attachment/note presence, filename/base-name lengths, error lengths, and stable reason codes instead of raw schema names, attachment UUIDs, filenames, database diagnostics, backend errors, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 speaker diarization job telemetry and job failure text now report attachment/note presence, model/error lengths, counts, and stable reason codes instead of raw schema names, attachment UUIDs, storage paths, database diagnostics, diarization backend errors, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 audio transcription job telemetry and job failure text now report parent attachment presence, job-id presence, filename/error lengths, and stable reason codes instead of raw schema names, attachment UUIDs, temp paths, Whisper/backend errors, queue diagnostics, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 audio chunk transcription job telemetry and failure/result text now report parent attachment presence, error lengths, merge-error lengths, and stable reason codes instead of raw attachment UUIDs, schema/database diagnostics, Whisper/backend errors, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 speaker relabel job failure text now reports stable reason codes instead of raw schema names, note/attachment UUIDs, speaker-map parser diagnostics, database/storage diagnostics, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 extraction setup job telemetry and failure text now report filename lengths, available-strategy counts, and stable reason codes instead of raw schema names, attachment UUIDs, filenames, storage paths, parser diagnostics, database/storage diagnostics, DSNs, paths, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 extraction adapter failure job/status text now stores stable reason codes instead of raw adapter/backend errors, DSNs, paths, hostnames, filenames, or token-shaped values.
- 2026-06-26 — #968/#974 media optimize setup job failure text now stores stable reason codes instead of raw schema names, database/storage diagnostics, filesystem paths, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 backup status directory-creation failure responses now return stable reason-code status values instead of raw OS diagnostics, filesystem paths, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 backup trigger dry-run responses now report generated backup filenames only instead of full backup destination paths, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 memory backup download headers now use memory-name length metadata in the generated filename instead of raw memory/archive names, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 knowledge-archive download headers now use backup-filename length metadata in the generated `.archive` filename instead of raw backup filenames, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 backup metadata get/update responses now return backup-filename length metadata and fixed fallback metadata titles instead of raw backup filenames, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 database and memory restore success messages now use fixed text instead of echoing memory names, backup filenames, filesystem paths, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 generated backup upload and pre-restore metadata sidecars now store fixed generated text and source filename lengths instead of raw uploaded/restored-from filenames, filesystem paths, DSNs, hostnames, or token-shaped values.
- 2026-06-26 — #968/#974 database snapshot/upload responses now return backup path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
- 2026-06-26 — #968/#974 backup status, latest-backup, list, and detail responses now return backup directory/path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
- 2026-06-26 — #968/#974 knowledge-archive upload responses now return extracted backup path length metadata instead of full backup filesystem paths that can contain DSNs, hostnames, customer path segments, or token-shaped values.
- 2026-06-26 — #968/#974 knowledge-archive upload responses now return extracted backup filename length metadata instead of raw customer-controlled archive entry filenames that can contain DSNs, hostnames, customer path segments, or token-shaped values.
- 2026-06-26 — #968/#974 database and memory restore responses now return pre-restore and restored-from filename length metadata instead of raw backup filenames, filesystem paths, DSNs, hostnames, customer path segments, or token-shaped values.
- 2026-06-26 — #968/#974 Twilio translated event, media-stream adapter, webhook event, and call-event outbox Debug output now reports payload lengths, event classes, provider-call-id lengths, and JSON classes instead of raw CallSid values, media payloads, phone-like remote parties, recording URLs, metadata JSON, socket-close reasons, or token-shaped values.
- 2026-06-26 — #968/#974 Core realtime MediaFrame, CallControlEvent, ActiveCallSession, and CallSessionManager Debug output now reports payload lengths, event metadata classes, provider-call-id lengths, and redacted session-map presence instead of raw media bytes, DTMF digits, recording URLs, phone-like remote parties, provider call IDs, metadata JSON, or socket-close reasons.
- 2026-06-26 — #968/#974 Core realtime ASR Debug output now reports transcript/metadata/language/speaker/reason lengths, JSON classes, timestamps, confidence, and event counts instead of raw transcripts, metadata JSON, speaker labels, provider URLs, DSNs, phone-like values, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Realtime mock adapter Debug output now reports provider-call-id length, frame counts, aggregate payload bytes, DTMF counts, drop settings, codec metadata, and seed presence instead of raw provider IDs, media fixture payloads, DTMF digits, DSNs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Realtime ASR error outbox payloads now persist stable reason classes and reason lengths instead of raw provider/backend reason text that can contain URLs, tokens, host paths, transcripts, or diagnostics.
- 2026-06-26 — #968/#974 Caption segment Debug output now reports timing, duration, transcript length, and speaker-label length instead of raw caption/transcript text, speaker labels, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Core Job Debug output now reports job IDs as presence flags plus payload/result JSON classes and lengths, error/progress message lengths, status, scheduling, retry, timestamp, and tier metadata instead of raw job payloads, results, backend errors, progress text, DSNs, URLs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Core note aggregate Debug output now reports note ids as presence flags plus title/source/format/content/hash/model lengths, metadata JSON classes/lengths, and tag/concept/link counts instead of raw note content, revised content, titles, tags, metadata JSON, AI metadata, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core note revision and summary Debug output now reports concept/revision/summary fields as lengths, counts, classes, timestamps, booleans, or presence flags instead of raw concept labels/notations/sources, revision content, summaries, rationales, note titles/snippets/tags, metadata JSON, model names, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core link and search-result Debug output now reports URLs, kinds, snippets, titles, tags, metadata, warnings, and result collections as lengths/counts/classes instead of raw external URLs, note snippets, titles, tags, warning text, metadata JSON, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core transcript segment Debug output now reports call ids as presence flags plus transcript and speaker-label lengths, timing presence, confidence, sequence, and timestamps instead of raw transcripts, speaker labels, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core attachment Debug output now reports blob storage, filenames, extracted text, extracted metadata, AI descriptions/models, note titles, document type names, and detection methods as lengths/classes/presence flags instead of raw storage paths, hashes, filenames, extracted content, metadata JSON, model names, titles, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core TUS upload Debug output now reports upload ids, filenames, content types, sizes, offsets, staging paths, metadata, and timestamps as lengths/classes/presence flags instead of raw upload filenames, MIME strings, staging filesystem paths, metadata JSON, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core call-session Debug output now reports provider, provider-call-id, ASR backend, remote party, end reason, archive, metadata, and timing fields as lengths/classes/presence flags instead of raw provider identifiers, phone-like values, URLs, paths, emails, backend names, metadata JSON, or token-shaped values.
- 2026-06-26 — #968/#974 Core entity and fine-tuning Debug output now reports extracted entities, normalized entities, graph vectors, entity types, model names, dataset metadata, source identifiers, generated queries, and error text as lengths/counts/presence flags instead of raw content-derived strings, embeddings, model identifiers, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core embedding-set and coarse-search Debug output now reports membership filters, agent metadata, tag strategies, document composition, provider config JSON, set profiles, summaries, member changes, and coarse vectors as lengths/counts/classes/presence flags instead of raw tags, queries, provider URLs, API-key fields, model names, dataset names, usage hints, vector values, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core document-type Debug output now reports agent prompts, revision chunking, document type metadata, detection rules, chunking/extraction JSON, creation/update requests, summaries, and detection results as lengths/counts/classes/presence flags instead of raw prompts, section names, validation JSON, MIME strings, filename patterns, model/config values, detection methods, paths, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core collection/archive/user-metadata Debug output now reports collection names/descriptions, tag labels, archive names/schema names/descriptions, user labels/colors, and user config JSON as lengths/classes/presence flags instead of raw hierarchy names, tags, archive schema identifiers, labels, color strings, config keys/values, paths, URLs, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core provenance Debug output now reports source URLs, relations, activity types, model names, metadata JSON, chain edges, and revision-node parents as lengths/classes/counts/presence flags instead of raw source URLs, relation strings, activity/model identifiers, prompts, provider URLs, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Core OAuth/API-key readback Debug output now reports token introspection, OAuth errors, authorization-server metadata, API-key records, and API-key create requests as lengths/counts/presence flags instead of raw scopes, client ids, usernames, issuers, endpoint URLs, key prefixes, labels, descriptions, error text, paths, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Core attachment-search request Debug output now reports note/content/event/time/spatial/location/device filters as lengths or presence flags instead of raw MIME filters, event types, coordinates, radius values, location names, emails, phone-like values, paths, or token-shaped values.
- 2026-06-26 — #968/#974 Core provenance creation request Debug output now reports location, named-location, device, file-provenance, and note-provenance fields as lengths/classes/presence flags instead of raw coordinates, addresses, device names, event descriptions, metadata JSON, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core memory/provenance result Debug output now reports extracted EXIF, spatial/temporal search hits, memory locations, memory devices, provenance records, and provenance chains as lengths/classes/counts/presence flags instead of raw filenames, content types, coordinates, distances, locations, device details, event text, EXIF metadata, URLs, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core specialized media metadata Debug output now reports 3D model and structured media metadata as lengths/counts/presence flags instead of raw formats, bounds, volumes, AI descriptions/models, SVG text, MIDI/tracker names, tracker messages, diagram labels, combined text, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Core embedding record/config Debug output now reports embedding text, vector payloads, and model identifiers as lengths/dimensions instead of raw chunk text, vector values, model names, emails, paths, or token-shaped values.
- 2026-06-26 — #968/#974 Core job pause-state Debug output now reports global pause state and per-archive pause maps as lengths/counts plus numeric queue stats instead of raw archive names, pause reasons, URLs, emails, or token-shaped values.
- 2026-06-26 — #968/#974 Search deduplication Debug output now reports chain metadata and enhanced search hits as title lengths/counts plus redacted `SearchHit` metadata instead of raw note titles, snippets, tags, paths, emails, phone-like values, or token-shaped values.
- 2026-06-26 — #968/#974 Hybrid search request Debug output now reports query text, embedding vectors, filters, config hints, and sort fields as lengths/dimensions/presence flags instead of raw queries, filter strings, vector values, language/script hints, sort values, paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Hybrid search config Debug output now reports weighting, archive behavior, embedding-set presence, deduplication options, strict/unified filter presence, language/script hint lengths, feature-flag metadata, and diversity metadata instead of raw filter values, language/script hints, paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core strict-filter Debug output now reports active dimensions, constraint presence, query-planning flags, identifier presence/counts, metadata string lengths, and boolean/numeric controls instead of raw tag strings, collection/security/embedding identifiers, metadata format/source strings, paths, URLs, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core collection-filter Debug output now reports collection identifier counts/presence, recursive-query flags, collection path counts, and path lengths instead of raw collection UUIDs, collection path strings, paths, URLs, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core embedding-provider request Debug output now reports config/model/content-type lengths, provider-config JSON class/length, MRL/HNSW numeric settings, and document-composition presence instead of raw config names, descriptions, model IDs, provider URLs/API-key fields, content types, paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core EXIF Debug output now reports capture/device/location presence, orientation, dimensions, GPS coordinate presence, altitude presence, and device string lengths instead of exact GPS coordinates, altitude values, camera make/model/software strings, paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core FAIR context/score Debug output now reports JSON-LD context namespaces and FAIR assessment issue strings as lengths/counts plus numeric scores instead of raw namespace URLs, assessment text, URLs, emails, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core trait request/result Debug output now reports note listing filters, note creation fields, search queries, extraction results, derived files, AI metadata, and template requests as lengths/counts/classes/presence flags instead of raw content, tags, metadata JSON, generated summaries, filenames, filesystem paths, URLs, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Core event Debug output now reports actors, event context, envelopes, server-event payloads, and variant metadata as lengths/counts/presence flags instead of raw actor ids/names, memory scopes, tenant ids, job errors, progress messages, note titles/tags, filenames, archive names, inference config field names, URLs, emails, paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 API problem-type catalog Debug output now reports problem URIs, titles, and descriptions as lengths plus status codes instead of raw URLs, public titles, or descriptive strings.
- 2026-06-27 — #968/#974 Core embedding lifecycle and extraction stats Debug output now reports embedding-set ids as presence flags and extraction strategy maps as counts/key lengths instead of raw set UUIDs, strategy names, URLs, emails, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 DB graph diagnostics Debug output now reports graph nodes, edges, traversal metadata, topology strategy, diagnostics snapshots/deltas, and coarse communities as counts, lengths, classes, and presence flags instead of raw note ids, titles, labels, provenance strings, JSON metrics, delta summaries, URLs, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Jobs inbound/worker event Debug output now reports inbound payloads, offsets, connector errors, worker progress, job refs, and failure text as lengths/classes/reason codes/presence flags instead of raw payload JSON, offsets, job UUIDs, progress messages, backend errors, URLs, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Jobs archive/code adapter Debug output now reports archive entry filenames, skip reasons, and code declaration names as lengths/classes with numeric metadata instead of raw paths, symbol names, emails, DSNs, URLs, or token-shaped values.
- 2026-06-27 — #968/#974 Realtime codec Debug output now reports telephone-event/DTMF payloads as presence flags instead of raw event codes while preserving non-content codec metadata.
- 2026-06-27 — #968/#974 Spreadsheet extraction now emits stable cell and sheet-read error reason codes/lengths instead of raw calamine debug values or backend error strings in extracted markdown or metadata.
- 2026-06-27 — #968/#974 JobResult Debug output now reports result presence/class/serialized length and failure/retry error lengths instead of raw job result JSON or handler error text.
- 2026-06-27 — #968/#974 Inference retry telemetry now emits stable reqwest reason/status/length metadata and safe propagated error text instead of raw backend request errors, URLs, or diagnostics.
- 2026-06-27 — #968/#974 Inference sidecar HTTP errors now report status, body length, and stable body reason for GLiNER, pyannote, Whisper model download/transcription, and vision backends instead of raw upstream response bodies.
- 2026-06-27 — #968/#974 OpenAI-compatible provider errors now report request, parse, stream, status, body length, and stable reason metadata instead of raw reqwest diagnostics or upstream response bodies.
- 2026-06-27 — #968/#974 Ollama provider and summarizer errors now report request, parse, stream, status, body length, and stable reason metadata instead of raw reqwest diagnostics, upstream bodies, provider URLs, or model names.
- 2026-06-27 — #968/#974 Webhook delivery response diagnostics now persist status, length, reason, and secret-candidate metadata instead of raw downstream response bodies or delivery error strings.
- 2026-06-27 — #968/#974 Job progress messages for concept tagging, reference extraction, related-concept inference, document-type inference, embedding-set requeue, and media-variant storage now store fixed labels or length metadata instead of raw extracted labels, entity names, related concept labels, document type names, embedding-set slugs, or variant identifiers.
- 2026-06-27 — #968/#974 Embedding-set refresh jobs now queue new work by set id, keep legacy slug fallback for existing queued jobs, and persist/log set slug length metadata instead of raw embedding-set slugs in job payload/result and refresh telemetry.
- 2026-06-27 — #968/#974 SSE connection telemetry now reports memory filter length, event-type filter count/total length, and entity-filter presence instead of raw memory/archive filter values, event type prefixes, or entity identifiers.
- 2026-06-27 — #968/#974 Stored AI revision, title-generation, and EXIF job payload/result/provenance fields now use document-type ids/lengths, title lengths, filename lengths, and id-presence flags instead of raw document type names, generated titles, filenames, or provenance/attachment UUIDs.
- 2026-06-27 — #968/#974 Job pause/resume operational telemetry now logs archive-name lengths and emits archive-scoped job-control server events with safe scope metadata instead of raw archive names or `archive:<name>` event scopes.
- 2026-06-27 — #968/#974 Twilio recording import now persists recording URL class/length and provider-call-id length metadata in note metadata, transcription job payloads, call-session metadata, and generated filenames instead of raw recording URLs or Twilio provider call identifiers.
- 2026-06-27 — #968/#974 Retained Twilio `call_event` outbox payloads now store provider-call-id lengths, remote-party presence/length, metadata class/length, and recording URL class/length instead of raw provider call identifiers, phone numbers, metadata JSON, or recording URLs.
- 2026-06-27 — #968/#974 Twilio voice webhook side-effect responses now return provider-call-id presence/length and recording URL class/length metadata instead of raw Twilio provider call identifiers or recording URLs.
- 2026-06-27 — #968/#974 Twilio realtime ASR session metadata now sends provider-call-id presence/length and call-id presence metadata to ASR backends instead of raw Twilio provider call identifiers or internal call UUIDs.
- 2026-06-27 — #968/#974 Call-detail API responses now summarize provider call identifiers, ASR backend labels, remote parties, and session metadata as presence/length/class fields instead of returning raw provider IDs, phone numbers, backend labels, recording URLs, or metadata values.
- 2026-06-27 — #968/#974 Twilio media-stream stop custom control payloads now carry provider-call-id presence/length metadata instead of raw Twilio CallSid values.
- 2026-06-27 — #968/#974 Twilio media-stream mark custom control payloads now carry mark-name presence/length metadata instead of raw provider mark names.
- 2026-06-27 — #968/#974 Event-bus telemetry mirror logs now emit job/note identifier presence/length metadata and job-type lengths instead of raw job IDs, note IDs, or job-type strings.
- 2026-06-27 — #968/#974 Worker job-processing, handler-registration, completion/failure, and NLP queue-failure telemetry now reports job-type lengths instead of raw job-type labels.
- 2026-06-27 — #968/#974 Attachment extraction queue success/failure logs now report note/attachment/job id presence/length metadata plus strategy and filename lengths instead of raw ids, strategy labels, or filenames.
- 2026-06-27 — #968/#974 Extraction handler downstream requeue, fanout, completion, and failure logs now report job-type and strategy lengths instead of raw job-type or strategy labels.
- 2026-06-27 — #968/#974 Extraction handler skip-vision, skip-transcription, and diarization-skip logs now report strategy lengths instead of raw extraction strategy labels.
- 2026-06-27 — #968/#974 Keyframe and view assembly downstream queue failure logs now report job-type lengths instead of raw job-type labels.
- 2026-06-27 — #968/#974 SSE replay and webhook-list failure telemetry now reports Last-Event-ID cursor and event-type lengths instead of raw cursor or event-type strings.
- 2026-06-27 — #968/#974 Inference config probe and embedding-routing telemetry now reports provider label lengths instead of raw request, detected, probe, or embedding provider labels.
- 2026-06-27 — #968/#974 Job model-profile, linking, GLiNER, and document-type completion telemetry now reports model, strategy, document-type, and detection-method lengths instead of raw labels.
- 2026-06-27 — #968/#974 Inference config audit, atomic-probe, and archive-override telemetry now reports action/schema/provider label lengths and counts instead of raw action, schema, or failed-provider labels.
- 2026-06-27 — #968/#974 Search cache-hit telemetry now reports query and archive-schema lengths instead of raw query text or archive schema labels.
- 2026-06-27 — #968/#974 Audio chunked-transcription completion telemetry now reports language length instead of raw language/provider text.
- 2026-06-27 — #968/#974 AI revision completion telemetry now reports revision-mode and effective-mode lengths instead of raw mode labels.
- 2026-06-27 — #968/#974 Authorization denial warning telemetry now reports deny-reason length instead of raw policy reason labels.
- 2026-06-27 — #968/#974 Startup logging and process-startup audit metadata now report log-format class/length instead of raw operator-provided `LOG_FORMAT` values.
- 2026-06-27 — #968/#974 GLB renderer health telemetry now reports renderer test-status class/length instead of raw renderer-provided status text.
- 2026-06-27 — #968/#974 Worker tiered-batch and claim-failure telemetry now reports stable tier classes instead of raw `TierGroup` debug labels.
- 2026-06-27 — #968/#974 Access-frequency health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
- 2026-06-27 — #968/#974 Orphan-tag health diagnostics now return tag presence/length metadata instead of raw tag names.
- 2026-06-27 — #968/#974 Stale-note health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
- 2026-06-27 — #968/#974 Unlinked-note health diagnostics now return note-id presence and title length metadata instead of raw note UUIDs or titles.
- 2026-06-27 — #968/#974 Tag co-occurrence health diagnostics now return tag presence and length metadata instead of raw tag pair names.
- 2026-06-27 — #968/#974 Streaming RTP health metrics now return backend duration count/total metadata instead of raw ASR backend label maps.
- 2026-06-27 — #968/#974 Retained inbound outbox envelopes now store source, offset, and event-type presence/length metadata instead of raw wrapper values.
- 2026-06-27 — #968/#974 Retained inbound DLQ rows now store source fingerprints plus offset/error metadata instead of raw source names, offsets, or error text.
- 2026-06-27 — #968/#974 Note access analytics log rows now store access-source presence/length metadata instead of raw source labels.
- 2026-06-27 — #968/#974 SKOS taxonomy audit log rows now store/read back actor and change-payload metadata instead of raw actor identifiers, changed field names, labels, nested values, or note/reference identifiers.
- 2026-06-27 — #968/#974 TUS upload sessions now aggregate arbitrary upload metadata as counts/lengths/secret-candidate totals and only retain finite `vision_mode` values instead of raw client-supplied metadata keys, metadata values, callback URLs, API-key-like values, or custom vision-mode strings.
- 2026-06-27 — #968/#974 Attachment-backed extraction jobs now retain filename and MIME lengths in queued payloads while resolving raw filename/MIME from file storage at execution time, removing raw client filenames and content-type strings from `job_queue.payload`.
- 2026-06-27 — #968/#974 Media optimization jobs now retain content-type length metadata in queued payloads while resolving raw content type from file storage at execution time, removing raw audio/video MIME strings from `job_queue.payload`.
- 2026-06-27 — #968/#974 Twilio recording audio-transcription jobs now retain call-id presence plus provider-call and recording metadata instead of raw call UUIDs, provider call identifiers, recording URLs, or token-bearing URL fragments in `job_queue.payload`.
- 2026-06-27 — #968/#974 AI contextual revision job results now retain document-type name length metadata instead of raw content-type/document-type names in stored `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Embedding provenance metadata now retains embedding model length and embedding-set presence metadata instead of raw model names or embedding-set UUIDs in retained provenance activity payloads.
- 2026-06-27 — #968/#974 Audio transcription job results now retain language presence/length metadata instead of raw provider language labels in stored `job_queue.result` payloads.
- 2026-06-27 — #968/#974 View assembly jobs now retain filename length metadata in queued payloads while resolving the parent model filename from attachment storage at execution time, removing raw 3D model filenames from the downstream `job_queue.payload`.
- 2026-06-27 — #968/#974 Media optimization job results now retain variant derivation/content-type lengths and attachment-id presence metadata instead of raw derivation labels, derived attachment UUIDs, or content-type strings in stored `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Extraction job results now retain strategy length plus adapter metadata class/length/count/secret-candidate metadata instead of raw extraction strategy labels or full adapter metadata in stored `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Concept tagging and reference extraction job/provenance summaries now retain label counts/lengths/secret-candidate metadata and extraction-method lengths instead of raw SKOS labels, tag paths, or method labels in retained `job_queue.result` and provenance metadata payloads.
- 2026-06-27 — #968/#974 Graph linking and document-type inference job/provenance summaries now retain strategy, document-type id, and detection-method presence/length metadata instead of raw labels or identifiers.
- 2026-06-27 — #968/#974 AI revision and contextual revision retained job/provenance summaries now store revision/effective-mode lengths instead of raw revision-mode labels.
- 2026-06-27 — #968/#974 Graph maintenance completion telemetry and retained job results now store schema length instead of raw archive schema labels.
- 2026-06-27 — #968/#974 Purge-note retained job results now store deleted-note presence metadata instead of raw deleted note UUIDs.
- 2026-06-27 — #968/#974 Metadata extraction retained job results now store extracted-key counts/lengths/secret-candidate metadata instead of raw model-derived metadata keys.
- 2026-06-27 — #968/#974 Graph maintenance snapshot step results now store snapshot-id presence metadata instead of raw diagnostics snapshot UUIDs.
- 2026-06-27 — #968/#974 Media optimization ffprobe skip results now store stable reason codes and error lengths instead of raw subprocess diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Media optimization static skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Thumbnail sprite skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 EXIF no-image skip results now store a stable reason code instead of prose diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Speaker relabel no-op results now store stable status/count metadata instead of prose diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 AI revision/contextual revision and bulk re-embed skip results now store stable reason codes instead of prose diagnostic text in retained `job_queue.result` payloads.
- 2026-06-27 — #968/#974 Archive adapter empty-input metadata now stores a stable error code instead of prose diagnostic text in retained extraction metadata.
- 2026-06-27 — #968/#974 PKE public-key registry Debug output now reports address/key/label lengths instead of raw addresses, key bytes, or labels.
- 2026-06-27 — #968/#974 PKE keyset summary/create/export Debug output now reports name/address/key/label lengths instead of raw keyset names, PKE addresses, key blobs, or labels.
- 2026-06-27 — #968/#974 Event outbox record/create Debug output now reports event/payload/memory lengths and classes instead of raw retained payload JSON, event types, or memory labels.
- 2026-06-27 — #968/#974 Realtime call metrics Debug output now reports backend-key count/length metadata instead of raw ASR backend labels.
- 2026-06-27 — #968/#974 DB chunk Debug output now reports text and metadata lengths/counts instead of raw chunk text or metadata key/value strings.
- 2026-06-27 — #968/#974 DB strict/unified filter Debug output now reports query parameter kinds, counts, and SQL lengths instead of raw UUIDs, filter strings, SQL fragments, or generated CTEs.
- 2026-06-27 — #968/#974 OAuth introspection/revocation request Debug output now reports token-type-hint classes instead of raw user-supplied hint strings.
- 2026-06-27 — #968/#974 Event envelope Debug output now reports payload variant/type metadata instead of delegating to nested event payload Debug output.
- 2026-06-27 — #968/#974 Backup import note Debug output now reports note/collection id presence instead of raw imported note or collection UUIDs.
- 2026-06-27 — #968/#974 Related/federated search Debug output now reports note-id presence instead of raw related/search result note UUIDs.
- 2026-06-27 — #968/#974 Reconstruction service Debug output now reports document/chunk id presence instead of raw reconstructed document or chunk UUIDs.
- 2026-06-27 — #968/#974 Memory info Debug output now reports embedding-set id presence instead of raw embedding-set UUIDs.
- 2026-06-27 — #968/#974 Core webhook Debug output now reports webhook/delivery/receiver id presence instead of raw webhook UUIDs.
- 2026-06-27 — #968/#974 Core inbound source Debug output now reports source id presence instead of raw inbound-source UUIDs.
- 2026-06-27 — #968/#974 Core OAuth client/token Debug output now reports id presence instead of raw OAuth row UUIDs.
- 2026-06-27 — #968/#974 DB event outbox Debug output now reports outbox record id presence instead of raw outbox UUIDs.
- 2026-06-27 — #968/#974 DB PKE keyset Debug output now reports keyset id presence instead of raw keyset UUIDs.
- 2026-06-27 — #968/#974 Core one-time API-key response Debug output now reports id presence instead of raw API-key UUIDs.
- 2026-06-27 — #968/#974 Inference config audit-row Debug output now reports audit-row id presence instead of raw localized audit row ids.
- 2026-06-27 — #968/#974 DB graph community Debug output now reports community-id presence instead of raw graph community identifiers.
- 2026-06-27 — #968/#974 Job pause persisted-state Debug output now reports paused archive counts instead of raw archive names.
- 2026-06-27 — #968/#974 Core audit event Debug output now reports identifier presence, text lengths, and attribute counts instead of raw audit IDs, principal/tenant/resource/correlation IDs, action labels, attribute keys, or attribute values.
- 2026-06-27 — #968/#974 Core authorization policy contract Debug output now reports metadata instead of raw action names, scopes, resource identifiers, tenant/correlation identifiers, context environment keys/values, custom deny reasons, policy ids, or obligation text.
- 2026-06-27 — #968/#974 Core file-safety validation Debug output now reports block-reason lengths and detected-type classes/lengths instead of raw user-controlled validation reasons or detected-type strings.
- 2026-06-27 — #968/#974 API cold-spot row Debug output now reports note-id presence and title length metadata instead of raw cold-spot note UUIDs or titles.
- 2026-06-27 — #968/#974 Core graph environment warning telemetry now reports invalid config value length/class metadata instead of raw `GRAPH_LINKING_STRATEGY` or `GRAPH_K_NEIGHBORS` values.
- 2026-06-27 — #968/#974 API archive context Debug output now reports schema/name lengths and default status instead of raw archive schema labels or human-readable memory names.
- 2026-06-27 — #968/#974 API authorization credential/PKE route metadata Debug output now reports id presence and field lengths instead of raw API-key prefixes, API-key names/scopes, PKE keyset names, PKE addresses, or labels.
- 2026-06-27 — #968/#974 API collection/template route metadata Debug output now reports id presence, name/format lengths, parent/collection presence, and counts instead of raw collection/template UUIDs, names, formats, emails, paths, or token-shaped values.
- 2026-06-27 — #968/#974 API taxonomy concept/scheme/collection route metadata Debug output now reports id presence, notation/status/version lengths, scheme presence, and count/boolean metadata instead of raw taxonomy UUIDs, notation/version/status strings, emails, paths, or token-shaped values.
- 2026-06-27 — #968/#974 API document-type route metadata Debug output now reports id presence, name/category lengths, and boolean flags instead of raw document-type UUIDs, names, categories, emails, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Related-concept job ConceptInfo Debug output now reports id presence and notation/label/broader-notation lengths instead of raw concept UUIDs, notation strings, labels, broader notation, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 DB ColBERT token embedding Debug output now reports id presence, token-text length, embedding dimensions, and model length instead of raw token UUIDs, note/chunk UUIDs, token text, embedding vector values, model names, emails, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 DB note versioning Debug output now reports version id presence, note id presence, content/hash/actor/model lengths, timestamps, version numbers, and child counts instead of raw note/version UUIDs, original note content, version hashes, editor identifiers, model names, emails, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 DB syntactic CodeChunk Debug output now reports code unit names, languages, parent scopes, module paths, chunk text, and chunk metadata as lengths/counts instead of raw code symbols, module/path strings, chunk text, metadata key/value strings, emails, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard migration warning Debug output now reports field/default/detail text as lengths instead of raw shard field names, default values, migration detail strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard downgrade impact Debug output now reports shard/current versions, feature names, introduced versions, descriptions, component names, fields, summaries, and data-loss descriptions as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard upgrade guidance Debug output now reports from/to versions, step titles/descriptions/commands, feature names, and summaries as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard compatibility Debug output now reports manifest versions, migration versions, warning text, incompatibility reasons, and minimum-required versions as lengths/counts instead of raw shard/operator strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard reserved-field validation error Display/Debug output now reports field names, removed versions, and removal reasons as lengths instead of raw shard/import strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core shard version parse and migration error/result Display/Debug output now reports version strings, migration failure text, and migrated JSON payloads as lengths/classes instead of raw import strings, emails, DSNs, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core split memory-search model source now mirrors the compiled memory/search Debug redaction contract for memory hits, timelines, cross-archive search, and attachment filters.
- 2026-06-27 — #968/#974 Core list response Debug output now reports note and global-attachment response envelopes as counts/totals instead of nested note or attachment summary content.
- 2026-06-27 — #968/#974 DB strict-filter list response Debug output now reports note envelope counts/totals instead of nested note summary content.
- 2026-06-27 — #968/#974 DB strict-filter list request Debug output now reports active filter dimensions and sort-string lengths instead of raw filter/sort values.
- 2026-06-27 — #968/#974 DB graph diagnostics Debug output now reports metric payload counts, totals, and presence flags instead of raw diagnostic vectors or nested metric payloads.
- 2026-06-27 — #968/#974 DB graph maintenance SNN/PFNET result Debug output now reports distribution counts/totals and numeric controls instead of raw SNN score-distribution vectors.
- 2026-06-27 — #968/#974 DB archive schema-sync missing-column Debug output now reports table/column/type/default lengths instead of raw schema names, column names, or SQL default text.
- 2026-06-27 — #968/#974 Core audit sink error Display/Debug output now reports sink message class and length instead of raw backend failure text, paths, or token-shaped values.
- 2026-06-27 — #968/#974 Core MatricError Display/Debug output now reports message classes, lengths, and id-presence flags instead of raw backend errors, resource ids, paths, URLs, or token-shaped values.
- 2026-06-27 — #968/#974 DB ColBERT stats Debug output now reports total-size string length instead of raw human-size text that can carry backend strings, paths, URLs, or token-shaped values.
- 2026-06-27 — #968/#974 Core OAuth token Debug output now reports token-type lengths instead of raw token-type strings that can carry token-shaped values.
- 2026-06-27 — #968/#974 Core TracingSink audit telemetry now reports audit id/idempotency/principal/tenant/resource/correlation presence, category/action/reason lengths, and attribute counts instead of raw audit identifiers, category/action/reason strings, resource ids, or sanitized attribute maps.
- 2026-06-27 — #968/#974 DB file-storage cleanup telemetry now reports blob/parent-attachment id presence instead of raw blob UUIDs or parent attachment UUIDs in orphaned-blob delete warnings and derived-caption cleanup logs.
- 2026-06-27 — #968/#974 Core EventBus emit telemetry now reports event-type length, event-id presence, and memory-scope length instead of raw SSE event ids, event type strings, or memory scope names in debug logs.
- 2026-06-27 — #968/#974 API route-policy Debug output now reports route/action-family lengths and nested authorization metadata instead of raw route templates, action-family labels, action names/scopes, resource ids, tenant ids, or context environment values.
- 2026-06-27 — #968/#974 Removed tracked stale `models.rs.bak` source copy that preserved pre-redaction core model Debug/secret-bearing definitions outside the compiled module.
- 2026-06-27 — #968/#974 Core enum parser validation errors now report invalid value lengths instead of echoing raw embedding, document, extraction, attachment, provider, SKOS, or tag parser inputs.
- 2026-06-27 — #968/#974 Twilio realtime adapter validation errors now report stable diagnostic classes and lengths instead of raw media payload, timestamp, sequence, or webhook form parser diagnostics.
- 2026-06-27 — #968/#974 Deepgram realtime ASR transport, parser, and provider error paths now report stable diagnostic classes and lengths instead of raw request, WebSocket, JSON parser, or provider error text.
- 2026-06-27 — #968/#974 DB PKE keyset import and duplicate-name validation errors now report stable diagnostic classes/name lengths instead of raw base64 parser diagnostics or keyset names.
- 2026-06-27 — #968/#974 DB incoming-webhook schema validation errors now report schema/path/diagnostic metadata as lengths/classes instead of raw schema refs, JSON pointer paths, property names, payload values, or parser diagnostics.
- 2026-06-27 — #968/#974 DB schema-name validation errors now report first-character classes, invalid-character classes, and name lengths instead of raw schema names, SQL-injection fragments, secret-shaped identifiers, or Unicode input.
- 2026-06-27 — #968/#974 DB note not-found errors now report note-id presence metadata instead of raw note UUIDs in repository fetch/update paths.
- 2026-06-27 — #968/#974 DB attachment delete not-found errors now report attachment-id presence metadata instead of raw attachment UUIDs.
- 2026-06-27 — #968/#974 DB document-type not-found errors now report document-type name length metadata instead of raw document-type names or lookup values.
- 2026-06-27 — #968/#974 DB archive not-found errors now report archive-name length metadata instead of raw archive names or source archive lookup values.
- 2026-06-27 — #968/#974 DB embedding-set/config not-found errors now report slug length and id-presence metadata instead of raw embedding-set slugs or config/set UUIDs.
- 2026-06-27 — #968/#974 DB PKE keyset not-found errors now report keyset id presence or name length metadata instead of raw keyset UUIDs or names.
- 2026-06-27 — #968/#974 DB note-version not-found errors now report version-presence metadata instead of raw version numbers.
- 2026-06-27 — #968/#974 API template not-found errors now report template-id presence metadata instead of raw template UUIDs.
- 2026-06-27 — #968/#974 API concept-update not-found errors now report concept-id presence metadata instead of raw concept UUIDs.
- 2026-06-27 — #968/#974 API graph diagnostics snapshot not-found errors now report snapshot side and id-presence metadata instead of raw snapshot UUIDs.
- 2026-06-27 — #968/#974 Realtime call-session not-found errors now report call-id presence metadata instead of raw call UUIDs.
- 2026-06-27 — #968/#974 Reconstruction chunk-chain not-found errors now report chain-id presence metadata instead of raw chain UUIDs.
- 2026-06-27 — #968/#974 Tag resolver required tag/scheme not-found errors now report notation length metadata instead of raw tag or scheme notation strings.
- 2026-06-27 — #968/#974 Core EXIF read errors now report parser diagnostic length metadata instead of raw EXIF parser diagnostics.
- 2026-06-27 — #968/#974 DB document-type in-use delete errors now report document-type name length and reference-count presence metadata instead of raw document-type names or exact reference counts.
- 2026-06-27 — #968/#974 API collection delete in-use conflicts now report note-count presence and force-required metadata instead of exact referenced-note counts.
- 2026-06-27 — #968/#974 DB SKOS scheme/concept delete in-use errors now report count-presence metadata instead of exact concept or note-tag counts.
- 2026-06-27 — #968/#974 DB embedding-config delete in-use errors now report embedding-set count presence instead of exact usage counts.
- 2026-06-27 — #968/#974 PDF/OCR/spreadsheet adapter validation errors now report filename/error length metadata and stable reason codes instead of raw filenames or parser diagnostics.
- 2026-06-27 — #968/#974 DB document-type in-use delete errors now report reference-count presence instead of exact reference counts.
- 2026-06-27 — #968/#974 Audio ffprobe duration parse failures now report stdout/error lengths and stable reason codes instead of raw ffprobe stdout or parser diagnostics.
- 2026-06-27 — #968/#974 Audio transcription adapter setup/read/backend errors now report path/model/MIME/error length metadata plus stable IO/reason codes instead of raw paths, labels, or backend diagnostics.
- 2026-06-27 — #968/#974 PDF text, PDF OCR, and Office conversion temp IO failures now report stable phase and IO-kind metadata instead of raw OS diagnostics, filesystem paths, or token-shaped temp details.
- 2026-06-27 — #968/#974 Video multimodal temp setup, duration parse, and local audio/frame read errors now report stable phase/path-length/error-length metadata and reason codes instead of raw OS diagnostics, paths, or parser text.
- 2026-06-27 — #968/#974 Audio transcription handler job failure/retry text now reports stable phases, error lengths, reason codes, and IO-kind/path-length metadata instead of prose-only backend or OS diagnostics.
- 2026-06-27 — #968/#974 Audio utility chunked and single-pass transcription backend failures now report phase, backend-name length, audio-size, error-length, and stable reason metadata instead of raw backend names or backend diagnostics.
- 2026-06-27 — #968/#974 SSE and Redis inbound connector transient errors now store connector, phase, error-length, and reason-code metadata instead of raw config, client, connect, read, or ack diagnostics.
- 2026-06-27 — #968/#974 Kafka inbound connector transient errors now store connector, phase, error-length, and reason-code metadata instead of raw config, subscribe, producer, commit, receive, or malformed-offset diagnostics.
- 2026-06-27 — #968/#974 Email adapter parse failures now report parser diagnostic length and stable reason metadata instead of raw mail parser diagnostics that can contain headers, addresses, paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Archive adapter ZIP/TAR parser failures now report phase, diagnostic length, and stable reason metadata instead of raw archive parser diagnostics that can contain paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Media optimizer ffprobe JSON parse failures now report parser diagnostic length and stable reason metadata instead of raw serde parser diagnostics that can contain paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Job pause-state serialization failures now report diagnostic length and stable reason metadata instead of raw serde diagnostics that can contain paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Core tokenizer initialization failures now report tokenizer family, diagnostic length, and stable reason metadata instead of raw third-party tokenizer diagnostics that can contain paths, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 DB archive clone duplicate-name errors now report archive-name length metadata instead of raw destination archive names that can contain paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 Twilio recording attachment filenames now use call-id length metadata and audio extension only instead of raw call/provider identifiers that can contain paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 OAuth registration and token grant/scope validation errors now report submitted-value length metadata and allowed-count metadata instead of raw `grant_type` or scope strings that can contain paths, emails, URLs, DSNs, or token-shaped values.
- 2026-06-27 — #968/#974 Jobs extraction registry missing-adapter errors now report extraction-strategy length metadata instead of raw `Debug`/display strategy labels in support-safe internal diagnostics.
- 2026-06-27 — #968/#974 GLB/3D inline vision prompts now send uploaded filename length metadata to the vision backend instead of raw filenames that can contain paths, emails, or token-shaped values.
- 2026-06-27 — #968/#974 GLB renderer health warnings now report renderer-unavailable reason class and reason length metadata instead of raw renderer diagnostic strings.
- 2026-06-27 — #968/#974 Incoming webhook idempotency Redis keys now use SHA-256 fingerprints instead of raw receiver slugs or caller-supplied idempotency keys.
- 2026-06-27 — #968/#974 Ingest stream token Redis keys and reverse-index values now use SHA-256 token fingerprints instead of raw bearer tokens.
- 2026-06-27 — #968/#974 Chat-stream replay/resumption Redis keys now use SHA-256 session fingerprints instead of raw session ids.
- 2026-06-27 — #968/#974 Ingest stream cursor Redis keys now use SHA-256 stream fingerprints instead of raw stream ids.
- 2026-06-27 — #968/#974 Note markdown export attachment filenames now use note-title length metadata plus note id instead of raw note titles in `Content-Disposition` headers.
- 2026-06-27 — #968/#974 Attachment media and subtitle `Content-Disposition` filenames now use filename-length metadata plus attachment ids instead of raw stored attachment filenames.
- 2026-06-27 — #968/#974 Public attachment and memory-backup docs now show metadata-only `Content-Disposition` filename examples instead of raw uploaded filenames or archive names.
- 2026-06-27 — #968/#974 Public operations docs now use `<MCP_CLIENT_SECRET>` instead of a scanner-hostile realistic OAuth/MCP client-secret example.
- 2026-06-27 — #968/#974 Public use-case, configuration, troubleshooting, `.env.example`, and bundle Dockerfile examples now use canonical MCP OAuth placeholders instead of inconsistent `xxx`/`xxxxx`/`xxxxxxxxxxxxx` client-secret examples.
- 2026-06-27 — #968/#974 Public config, image, and quickstart provider-key examples now use neutral `<OPENAI_API_KEY>`, `<OPENROUTER_API_KEY>`, and `<HF_TOKEN>` placeholders, reducing docs-contract known findings from 36 to 34.
- 2026-06-27 — #968/#974 MCP feature-chain secret fixture and extraction-service deployment docs now use neutral API-key/HuggingFace placeholders, reducing docs-contract known findings from 34 to 33.
- 2026-06-27 — #968/#974 MCP integration test runner usage now uses `<ACCESS_TOKEN>` instead of an `mm_at`-shaped token example, reducing docs-contract known findings from 33 to 32.
- 2026-06-27 — #968/#974 Production/test quick-start scripts and MCP API-error fixtures now avoid hardcoded local Postgres passwords and inline credential-bearing DSN literals, reducing docs-contract known findings from 32 to 27.
- 2026-06-27 — #968/#974 MCP agentic test docs and CI shard rebuild harness now use placeholder/generated database credentials, reducing docs-contract known findings from 27 to 24.
- 2026-06-27 — #968/#974 Gitea test workflow now uses generated per-run database passwords and runtime-constructed Postgres DSNs, reducing docs-contract known findings from 24 to 13.
- 2026-06-27 — #968/#974 Gitea CI builder workflow now uses generated per-run database passwords and runtime-constructed Postgres DSNs, reducing docs-contract known findings from 13 to 8.
- 2026-06-27 — #968/#974/#1004 Docker and local compose defaults now avoid hardcoded DB passwords and inline credential-bearing DSN literals, reducing hosted-strict docs-contract findings from 8 to 0; blocking mode passes.
- 2026-06-27 — #968/#974 Backup script database credentials now require explicit `PGPASSWORD` or `PGPASSFILE` instead of defaulting to the reusable `matric` password.
- 2026-06-27 — #968/#974 API backup, restore, post-restore maintenance, and psql size-query subprocesses now require explicit `PGPASSWORD` or `PGPASSFILE` instead of injecting the reusable `matric` password.
- 2026-06-27 — #968/#974 Backup script scratch handling now requires explicit private scratch, rejects shared `/tmp`, verifies mode/ownership, and requires tmpfs/ramfs or operator-marked encrypted storage for real backups.
- 2026-06-27 — #968/#974 Backup status/list/info path response metadata now uses structured length-only `path_metadata` tokens instead of raw absolute paths, DSNs, hostnames, or token-shaped path fragments.
- 2026-06-27 — #968/#974 Backup script dry-run, distribution, retention, verification, and subprocess verbose logs now use destination/path/output classes plus lengths instead of raw local paths, scratch paths, rsync/S3 URIs, or command output lines.
- 2026-06-27 — #968/#974 Backup script verbose config-file and encryption-key diagnostics now use filesystem path class/length metadata instead of raw operator config or key paths.
- 2026-06-27 — #968/#974 Hybrid search tracing now reports query class and character length instead of raw search query text in ordinary search spans.
- 2026-06-27 — #968/#974 PKE crypto file/header Debug output now reports lengths/counts/presence instead of raw addresses, filenames, encrypted key material, salts, or nonces.
- 2026-06-27 — #968/#974 PKE CLI keygen/decrypt now support stdin/file passphrase sources and public docs no longer show passphrases in argv.
- 2026-06-27 — #968/#974 PKE CLI legacy argv passphrase use now warns operators to use stdin/file sources without echoing the secret.
- 2026-06-27 — #968/#974 PKE Rust API docs now use passphrase placeholders and docs-contract rejects realistic helper-call passphrase literals.
- 2026-06-27 — #968/#974 Public `matric-crypto` Rustdoc PKE examples now use passphrase placeholders and are covered by docs-contract.
- 2026-06-27 — #968/#974 SSE EventSource API docs now use token placeholders instead of token-shaped query/header examples.
- 2026-06-27 — #968/#974 PKE CLI success JSON now uses metadata-only path/original-filename fields instead of raw local paths or embedded filenames.
- 2026-06-27 — #968/#974 PKE CLI invalid address verification now redacts arbitrary invalid input from JSON/stdout and stderr.
- 2026-06-27 — #968/#974 OAuth/API-key Debug metadata now uses character-count lengths for token introspection, OAuth errors, authorization-server metadata, API-key records, and API-key creation requests while preserving raw-value redaction.
- 2026-06-27 — #968/#974 Realtime call-detail Debug metadata now uses character-count provider and end-reason lengths while preserving provider/transcript redaction.
- 2026-06-27 — #968/#974 Core note/link/search Debug metadata now uses character-count lengths while preserving raw content, URL, tag, metadata, and identifier redaction.
- 2026-06-27 — #968/#974 Core embedding Debug metadata now uses character-count lengths while preserving raw embedding text, model, tag, query, provider-config, vector, and token-shaped value redaction.
- 2026-06-27 — #968/#974 Core document-type Debug metadata now uses character-count lengths while preserving raw prompt, pattern, config, URL, path, email, and token-shaped value redaction.
- 2026-06-27 — #968/#974 Core attachment/TUS/call/transcript Debug metadata now uses character-count lengths while preserving raw content, URL, path, phone, email, provider-id, and token-shaped value redaction.
- 2026-06-27 — #968/#974 Core entity/fine-tuning Debug metadata now uses character-count lengths while preserving raw content-derived strings, vector values, model identifiers, URLs, paths, emails, phone-like values, and token-shaped value redaction.
- 2026-06-27 — #968/#974 Core collection/archive/user-metadata Debug metadata now uses character-count lengths while preserving raw collection names, archive/schema names, labels, config keys/values, URLs, paths, emails, phone-like values, and token-shaped value redaction.
