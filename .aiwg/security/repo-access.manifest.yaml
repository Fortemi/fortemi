version: "1"
default_policy: deny
repos:
  - name: fortemi
    path: .
    actions:
      - read
      - write
      - commit
      - push
      - issue-comment
      - service-action
    notes: Authorized by the operator for Fortemi implementation, CI, release, and issue-comment work.
